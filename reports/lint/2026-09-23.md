# Nightly lint 2026-09-23 (SGT)

- **When:** 2026-09-23 03:17 SGT (**official nightly after Decision I stage-2 `vibe_pma_bnd` / PR130 tip**; date label Asia/Shanghai). Sibling [`2026-09-22.md`](2026-09-22.md) remains the PR123-era clean on historical DUT `302ac943` — not overwritten. [`2026-09-21-post115.md`](2026-09-21-post115.md) remains the first official post-#115 / PR118 idle-mark clean.
- **DUT / tip SHA:** `f001833b3c4507620eccd44895df06c218b1165f` (origin/main HEAD at launch = PR130 Decision I stage-2 `vibe_pma_bnd` merge). Lint used **tip tree as-is** (no freeze overlay). Tip **includes** Decision I leaves (`vibe_afifo`, `vibe_pma_bnd`) — expected. Functional RTL vs last historical pin is **comment-header only** (see diffs).
- **Written freeze:** **UNFROZEN** (Decision I). Last historical pin `302ac943c3737c288f3af6c4857bb3a9b1683a26` is **VOID as current**. **Not re-pinned tonight.** Do not edit SPEC/CHANGELOG/STATUS freeze text here. Path B **hold**.
- **`git diff 302ac943...HEAD -- rtl/ include/`:** **NON-EMPTY** — `rtl/cdc/vibe_afifo.sv` +7 and `rtl/pma/vibe_pma_bnd.sv` +7 (GENERATED/HAND-FINISHED pyCircuit headers only). `include/` **empty**.
- **`git diff 302ac943...HEAD -- rtl/port`:** **empty** (`vibe_port` / F1 `ovf_l` untouched)
- **`git diff 302ac943...HEAD -- rtl/cdc`:** **NON-EMPTY** — `vibe_afifo.sv` header comments only (gray-pointer / AFULL body unchanged)
- **`git diff a658d141...HEAD -- rtl/`:** **NON-EMPTY** — `vibe_afifo.sv` header + `vibe_pma_bnd.sv` (`PMA_IDLE_MARK` + Decision I header). `-- include/` **empty**. Historical vs old PMA-ECO freeze.
- **main HEAD checked:** `f001833b3c4507620eccd44895df06c218b1165f` (same RTL bits as lint DUT)
- **Tool:** Verilator 5.020 2024-01-01 (Debian 5.020-1) — same Ubuntu 24.04 package as [`2026-09-22.md`](2026-09-22.md)
- **Command:** `reports/lint/run_lint.sh` (`--lint-only -Wall -Wno-fatal --top-module vibe_ub_switch`) — **exit 0** (`test Error- codes = 0` passed)
- **Waivers:** `reports/lint/vibe_ub_switch.vlt` + `WAIVERS.md` (**unchanged**; no `.vlt` expansion). Existing UNSUPPORTED waivers remain PCS-only (`vibe_pcs_rx.sv:5`, `vibe_pcs_rx_fec.sv:14`, `vibe_pcs_rx_unpack.sv:17`).
- **Delta baseline:** [`2026-09-22.md`](2026-09-22.md) (0 `%Error`; NEW keys 0; warning lines 1075; unique keys 157; LATCH/UNOPTFLAT/BLKSEQ=0). That clean is PR123 / historical DUT `302ac943`. Nightly [`2026-09-08.md`](2026-09-08.md) DUT `32a7f5e0` is **VOID**.

## Result

Official `run_lint.sh` **completes a full `-Wall` sweep**. Verilator emits **0** `%Error-*` codes. Warning-line and unique-key totals match the 2026-09-22 official log. Decision I leaf headers added **no** warning keys (`vibe_pma_bnd.sv` still has **zero** lint keys; `vibe_afifo.sv` still has **zero** primary keys). Official log was **rewritten** only because `vibe_afifo` include-note line numbers shifted +7 (`:21`/`:22` → `:28`/`:29`). Those notes are not `file:line:rule` keys.

| | today (official) | prior tip clean (`2026-09-22`) |
|--|------:|-------------------------:|
| `%Error-*` | **0** | **0** |
| `%Warning-*` lines | 1075 | 1075 |
| unique `file:line:rule` keys | 157 | 157 |
| **NEW keys** | **0** | — |
| GONE keys | **0** | — |

### NEW keys (0 vs 2026-09-22)

None. Decision I `vibe_afifo` / `vibe_pma_bnd` headers are comments only. No new warning needs a per-item waiver. No RTL ECO requested.

### GONE keys (0 vs 2026-09-22)

None. The two pre-ECO `%Error-UNSUPPORTED` input defaults stay **GONE** (already cleared on 2026-09-21 / still GONE on post115 / 2026-09-22). Not re-listed as tonight's GONE.

### Warning classes

| Class | today (official) | prior tip clean (`2026-09-22`) |
|-------|------:|----------:|
| UNUSEDPARAM | 838 | 838 |
| UNUSEDSIGNAL | 148 | 148 |
| PINCONNECTEMPTY | 37 | 37 |
| WIDTHTRUNC | 30 | 30 |
| WIDTHEXPAND | 18 | 18 |
| VARHIDDEN | 4 | 4 |
| **UNOPTFLAT** | **0** | **0** |
| **LATCH** | **0** | **0** |
| **BLKSEQ** | **0** | **0** |

`vibe_ub_switch.lint.log` is tonight's official 5.020 run (0 Errors; full `-Wall`; include-note line-number churn only vs the 2026-09-22 committed log). No throwaway `.vlt` was needed.

No RTL/SPEC/TB/`include/` change in this reports PR. No `.vlt` / `WAIVERS.md` expansion. No issue opened (not for F1).
F1 `ovf_l` **untouched** / **not ECO'd**.
Decision I remains **UNFROZEN**. Path B **hold**.
No signoff claim. Not 4/3. Not new 1/3.
