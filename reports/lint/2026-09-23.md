# Nightly lint 2026-09-23 (Asia/Shanghai)

- **When:** 2026-09-23 03:21 CST / Asia/Shanghai (**official nightly after Decision I UNFROZEN + PR130 `vibe_pma_bnd` leaf**; date label Asia/Shanghai). Sibling [`2026-09-22.md`](2026-09-22.md) remains the official nightly after Xia pin + post-#115 / PR118 clean — not overwritten. [`2026-09-21-post115.md`](2026-09-21-post115.md) remains the first official post-#115 / PR116 idle-mark clean.
- **DUT / tip SHA:** `f001833b3c4507620eccd44895df06c218b1165f` (origin/main HEAD at launch = Merge PR130 Decision I stage-2 `vibe_pma_bnd`). `git rev-parse HEAD` **MATCHES** the requested checkout tip; `origin/main` did not move on fetch. Lint used **tip tree as-is** (no freeze overlay; no re-pin).
- **Written freeze:** **UNFROZEN** (Luke Decision I). Last historical pin `302ac943c3737c288f3af6c4857bb3a9b1683a26` is **VOID as current**. **Not re-pinned tonight.** Do not edit SPEC/CHANGELOG/STATUS freeze text here.
- **`git diff 302ac943...HEAD -- rtl/`:** **NON-EMPTY** — Decision I leaves only: `rtl/cdc/vibe_afifo.sv` (+7 comment header), `rtl/pma/vibe_pma_bnd.sv` (+7 comment header). Functional body vs `302ac943` is **unchanged** (comment-header only).
- **`git diff 302ac943...HEAD -- include/`:** **empty**
- **`git diff 302ac943...HEAD -- rtl/port`:** **empty** (`vibe_port` / F1 `ovf_l` untouched)
- **`git diff 302ac943...HEAD -- rtl/cdc`:** **NON-EMPTY** — `vibe_afifo.sv` comment header only (gray-pointer / AFULL body unchanged)
- **`git diff d88442d2...HEAD -- rtl/ include/`:** **NON-EMPTY** — same two Decision I header leaves as above (`+7/+7`). Yesterday's lint tip `d88442d2aad29ecaaabdb3f70826baa2d87999a9` had empty RTL vs `302ac943`; tonight's tip added only those comment banners.
- **main HEAD checked:** `f001833b3c4507620eccd44895df06c218b1165f` (same RTL bits as lint DUT)
- **Tool:** Verilator 5.020 2024-01-01 (Debian 5.020-1) — same Ubuntu 24.04 package as [`2026-09-22.md`](2026-09-22.md)
- **Command:** `reports/lint/run_lint.sh` (`--lint-only -Wall -Wno-fatal --top-module vibe_ub_switch`) — **exit 0** (`test Error- codes = 0` passed)
- **Waivers:** `reports/lint/vibe_ub_switch.vlt` + `WAIVERS.md` (**unchanged**; no `.vlt` expansion). Existing UNSUPPORTED waivers remain PCS-only (`vibe_pcs_rx.sv:5`, `vibe_pcs_rx_fec.sv:14`, `vibe_pcs_rx_unpack.sv:17`).
- **Delta baseline:** [`2026-09-22.md`](2026-09-22.md) (0 `%Error`; NEW keys 0; warning lines 1075; unique keys 157; LATCH/UNOPTFLAT/BLKSEQ=0). That clean is post-Xia-pin / PR123 official nightly. Nightly [`2026-09-08.md`](2026-09-08.md) DUT `32a7f5e0` is **VOID**.

## Result

Official `run_lint.sh` **completes a full `-Wall` sweep**. Verilator emits **0** `%Error-*` codes. Warning-line and unique-key totals match the 2026-09-22 official log. Primary `%Warning-*` / `%Error-*` lines are **identical** to yesterday's committed log. The rewritten [`vibe_ub_switch.lint.log`](vibe_ub_switch.lint.log) differs only in `vibe_afifo.sv` include-note line numbers (`:21/:22` → `:28/:29`, +7 from the Decision I comment banner). Those notes are **not** `file:line:rule` keys. `vibe_pma_bnd.sv` still has **zero** lint keys. Decision I headers added **none**.

| | today (official) | yesterday (`2026-09-22`) |
|--|------:|-------------------------:|
| `%Error-*` | **0** | **0** |
| `%Warning-*` lines | 1075 | 1075 |
| unique `file:line:rule` keys | 157 | 157 |
| **NEW keys** | **0** | — |
| GONE keys | **0** | — |

### NEW keys (0 vs 2026-09-22)

None. Decision I stage-1/2 comment headers on `vibe_afifo.sv` / `vibe_pma_bnd.sv` added **no** new warning keys. `vibe_pma_bnd.sv` still has **zero** lint keys (same as PR111 / post115 / 2026-09-22). Primary warning sites did not move. No new warning needs a per-item waiver. No brand-new Error.

### GONE keys (0 vs 2026-09-22)

None. The two pre-ECO `%Error-UNSUPPORTED` input defaults stay **GONE** (already cleared on 2026-09-21 / still GONE on post115 / 2026-09-22). Not re-listed as tonight's GONE.

### Warning classes

| Class | today (official) | yesterday (`2026-09-22`) |
|-------|------:|----------:|
| UNUSEDPARAM | 838 | 838 |
| UNUSEDSIGNAL | 148 | 148 |
| PINCONNECTEMPTY | 37 | 37 |
| WIDTHTRUNC | 30 | 30 |
| WIDTHEXPAND | 18 | 18 |
| VARHIDDEN | 4 | 4 |
| **UNOPTFLAT** | **0** | **0** |
| **LATCH** | **0** | **0** |
| **BLKSEQ** | **0** | **0** |

**LATCH / UNOPTFLAT / BLKSEQ = 0** (target 0; called out). Unchanged vs 2026-09-22.

`vibe_ub_switch.lint.log` is tonight's official 5.020 run (0 Errors; full `-Wall`; primary warning set identical to 2026-09-22; include-note line shift only). No throwaway `.vlt` was needed.

No RTL/SPEC/TB/`include/` change in this PR. No `.vlt` / `WAIVERS.md` expansion. No issue opened (not for F1).
No signoff claim. Not 4/3. Not new 1/3. F1 `ovf_l` untouched.
