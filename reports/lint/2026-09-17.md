# Nightly lint 2026-09-17 (SGT)

- **When:** 2026-09-17 04:21 SGT (first post-PRBS23 lint/CDC; date label Asia/Shanghai)
- **DUT / tip SHA:** `859450a467640e4b18f7b577818db4ebef3f217f` (origin/main HEAD at launch = PR97 health merge). RTL content = Luke PRBS23 `268d038f` plus later reports/docs. Lint used **tip tree as-is** (no freeze overlay).
- **Written freeze:** `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**. Decision **H not chosen**. **Not re-pinned to tip.**
- **`git diff 982ddd0a...HEAD -- rtl/`:** **NON-EMPTY** — `rtl/pma/vibe_pma_bnd.sv` (+75/−8), `rtl/port/vibe_port.sv` (+1/−1 `txrst_n`)
- **`git diff 982ddd0a...HEAD -- include/`:** **empty**
- **main HEAD checked:** `859450a467640e4b18f7b577818db4ebef3f217f` (same as lint DUT bits)
- **Tool:** Verilator 5.020 2024-01-01 (Debian 5.020-1) — same Ubuntu 24.04 package as [`2026-09-16.md`](2026-09-16.md)
- **Command:** `reports/lint/run_lint.sh` (`--lint-only -Wall -Wno-fatal --top-module vibe_ub_switch`) — **exit 1** (`test Error- codes = 0` failed)
- **Waivers:** `reports/lint/vibe_ub_switch.vlt` + `WAIVERS.md` (**unchanged**; no `.vlt` expansion). Existing UNSUPPORTED waivers remain PCS-only (`vibe_pcs_rx.sv:5`, `vibe_pcs_rx_fec.sv:14`, `vibe_pcs_rx_unpack.sv:17`).
- **Delta baseline:** [`2026-09-16.md`](2026-09-16.md) (0 `%Error`; NEW keys 0; LATCH/UNOPTFLAT/BLKSEQ=0; warning lines 1075; unique keys 157) on freeze `982ddd0a`. That baseline is **pre-PRBS23**. Nightly [`2026-09-08.md`](2026-09-08.md) DUT `32a7f5e0` is **VOID**.

## Result

Official `run_lint.sh` **does not complete a full `-Wall` sweep**. Verilator emits one `%Error-UNSUPPORTED` on the new `txrst_n` input default and **exits** (`%Error: Exiting due to 1 error(s)`). Warning-line and unique-key totals below are therefore **truncated**, not a cleanup.

| | today (official) | yesterday (`2026-09-16`, freeze) |
|--|------:|-------------------------:|
| `%Error-*` | **1** | 0 |
| `%Warning-*` lines | 41 (truncated) | 1075 |
| unique `file:line:rule` keys | 32 (truncated) | 157 |
| **NEW keys** | **1** | — |
| GONE keys (genuine) | **0** | — |

Raw key-set subtract on the truncated official log would show 126 missing yesterday keys. Those are **not GONE** — they were not re-emitted because the tool exited. Do not treat them as cleared debt.

### NEW keys (1)

| Key | Summary |
|-----|---------|
| `vibe_pma_bnd.sv:8:UNSUPPORTED` | `%Error-UNSUPPORTED`: default value on module input `txrst_n = 1'b1` (Luke PRBS23). Same Verilator 5.020 class already waived for three PCS RX inputs; **not waived tonight**. |

### Warning classes

Official truncated run only reached PINCONNECTEMPTY + VARHIDDEN (plus the Error) before exit:

| Class | today (official) | yesterday |
|-------|------:|----------:|
| UNUSEDPARAM | n/a (not reached) | 838 |
| UNUSEDSIGNAL | n/a (not reached) | 148 |
| PINCONNECTEMPTY | 37 | 37 |
| WIDTHTRUNC | n/a (not reached) | 30 |
| WIDTHEXPAND | n/a (not reached) | 18 |
| VARHIDDEN | 4 | 4 |
| **UNOPTFLAT** | **0** (in emitted log) | **0** |
| **LATCH** | **0** (in emitted log) | **0** |
| **BLKSEQ** | **0** (in emitted log) | **0** |

`vibe_ub_switch.lint.log` is tonight's official 5.020 run (Error + truncated warnings).

### Diagnostic continue-sweep (not official; waivers not changed)

A **throwaway** extra `.vlt` line for `vibe_pma_bnd.sv:8` UNSUPPORTED was used only in `/tmp` (not written under `reports/lint/`, not committed) so the PRBS23 body could be linted. That diagnostic sweep: **0** `%Error-*`; warning lines **1075**; unique keys **157**; **NEW keys 0**; **GONE keys 0**; UNUSEDPARAM 838 / UNUSEDSIGNAL 148 / PINCONNECTEMPTY 37 / WIDTHTRUNC 30 / WIDTHEXPAND 18 / VARHIDDEN 4; **LATCH=0 UNOPTFLAT=0 BLKSEQ=0**. PRBS23 LFSR / `prbs23_*` added **no** new warning keys. This does **not** make the official run green and is **not** a waiver expansion.

No RTL/SPEC/TB/`include/` change. No `.vlt` / `WAIVERS.md` expansion. No issue opened (not for F1, not for this UNSUPPORTED — PM / decision H).
No signoff claim. Not 4/3. F1 `ovf_l` untouched.
