# Nightly lint 2026-09-18 (SGT)

- **When:** 2026-09-18 03:25 SGT (second post-PRBS23 lint/CDC; date label Asia/Shanghai)
- **DUT / tip SHA:** `4e575b6aed50d6d4182e0fac5ceaa167d064d9e8` (origin/main HEAD at launch = PR101 health merge; re-fetched, tip **did not move**). RTL content = Luke PRBS23 `268d038f` plus later reports/docs/TB (PR99 TB-only Icarus-safe LMSM force — not linted). Lint used **tip tree as-is** (no freeze overlay; same policy as 2026-09-17 / PR98).
- **Written freeze:** `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**. Decision **H not chosen**. **Not re-pinned to tip.**
- **`git diff 982ddd0a...HEAD -- rtl/`:** **NON-EMPTY** — `rtl/pma/vibe_pma_bnd.sv` (+75/−8), `rtl/port/vibe_port.sv` (+1/−1 `txrst_n`)
- **`git diff 982ddd0a...HEAD -- include/`:** **empty**
- **`git diff 859450a4...HEAD -- rtl/` / `-- include/`:** **empty** (PR98–PR101 are reports / docs / TB only)
- **main HEAD checked:** `4e575b6aed50d6d4182e0fac5ceaa167d064d9e8` (same as lint DUT bits)
- **Tool:** Verilator 5.020 2024-01-01 (Debian 5.020-1) — same Ubuntu 24.04 package as [`2026-09-17.md`](2026-09-17.md)
- **Command:** `reports/lint/run_lint.sh` (`--lint-only -Wall -Wno-fatal --top-module vibe_ub_switch`) — **exit 1** (`test Error- codes = 0` failed)
- **Waivers:** `reports/lint/vibe_ub_switch.vlt` + `WAIVERS.md` (**unchanged**; no `.vlt` expansion). Existing UNSUPPORTED waivers remain PCS-only (`vibe_pcs_rx.sv:5`, `vibe_pcs_rx_fec.sv:14`, `vibe_pcs_rx_unpack.sv:17`).
- **Delta baseline:** [`2026-09-17.md`](2026-09-17.md) (first post-PRBS23; 1 `%Error`; NEW keys 1 vs 2026-09-16 = `txrst_n` UNSUPPORTED; official log truncated; LATCH/UNOPTFLAT/BLKSEQ=0; warning lines 41; unique keys 32). Nightly [`2026-09-08.md`](2026-09-08.md) DUT `32a7f5e0` is **VOID**.

## Result

Official `run_lint.sh` **does not complete a full `-Wall` sweep**. Verilator emits one `%Error-UNSUPPORTED` on the PRBS23 `txrst_n` input default and **exits** (`%Error: Exiting due to 1 error(s)`). Warning-line and unique-key totals below are therefore **truncated**, not a cleanup. Tonight's official log is **byte-identical** to the 2026-09-17 committed `vibe_ub_switch.lint.log`.

| | today (official) | yesterday (`2026-09-17`, official) |
|--|------:|-------------------------:|
| `%Error-*` | **1** | **1** |
| `%Warning-*` lines | 41 (truncated) | 41 (truncated) |
| unique `file:line:rule` keys | 32 (truncated) | 32 (truncated) |
| **NEW keys** | **0** | 1 (vs 2026-09-16) |
| GONE keys (genuine) | **0** | 0 |

Carry-forward (not NEW tonight): `vibe_pma_bnd.sv:8:UNSUPPORTED` — still **Error**, same key as 2026-09-17. Raw key-set subtract against the pre-PRBS23 2026-09-16 full sweep would still show 126 missing keys. Those remain **not GONE** — they were not re-emitted because the tool exited. Do not treat them as cleared debt.

### NEW keys (0)

None vs 2026-09-17. The known Error is unchanged:

| Key | Summary |
|-----|---------|
| `vibe_pma_bnd.sv:8:UNSUPPORTED` (carry-forward) | `%Error-UNSUPPORTED`: default value on module input `txrst_n = 1'b1` (Luke PRBS23). Same Verilator 5.020 class already waived for three PCS RX inputs; **not waived tonight**. Counted as Error; **not NEW**. |

### Warning classes

Official truncated run only reached PINCONNECTEMPTY + VARHIDDEN (plus the Error) before exit — same cut as 2026-09-17:

| Class | today (official) | yesterday (official) |
|-------|------:|----------:|
| UNUSEDPARAM | n/a (not reached) | n/a (not reached) |
| UNUSEDSIGNAL | n/a (not reached) | n/a (not reached) |
| PINCONNECTEMPTY | 37 | 37 |
| WIDTHTRUNC | n/a (not reached) | n/a (not reached) |
| WIDTHEXPAND | n/a (not reached) | n/a (not reached) |
| VARHIDDEN | 4 | 4 |
| **UNOPTFLAT** | **0** (in emitted log) | **0** (in emitted log) |
| **LATCH** | **0** (in emitted log) | **0** (in emitted log) |
| **BLKSEQ** | **0** (in emitted log) | **0** (in emitted log) |

`vibe_ub_switch.lint.log` is tonight's official 5.020 run (Error + truncated warnings; identical bytes to 2026-09-17, so the committed log was left untouched).

### Diagnostic continue-sweep (not official; waivers not changed)

A **throwaway** extra `.vlt` line for `vibe_pma_bnd.sv:8` UNSUPPORTED was used only in `/tmp` (not written under `reports/lint/`, not committed) so the PRBS23 body could be linted. That diagnostic sweep: **0** `%Error-*`; warning lines **1075**; unique keys **157**; **NEW keys 0** vs the 2026-09-16 full-sweep keyset; **GONE keys 0**; UNUSEDPARAM 838 / UNUSEDSIGNAL 148 / PINCONNECTEMPTY 37 / WIDTHTRUNC 30 / WIDTHEXPAND 18 / VARHIDDEN 4; **LATCH=0 UNOPTFLAT=0 BLKSEQ=0**. PRBS23 LFSR / `prbs23_*` added **no** new warning keys (same as 2026-09-17 diagnostic). This does **not** make the official run green and is **not** a waiver expansion.

No RTL/SPEC/TB/`include/` change. No `.vlt` / `WAIVERS.md` expansion. No issue opened (not for F1, not for this UNSUPPORTED — PM / decision H).
No signoff claim. Not 4/3. F1 `ovf_l` untouched.
