# Nightly lint 2026-09-25 (SGT)

- **When:** 2026-09-25 02:54 SGT (**official nightly after Decision I stage-36 `vibe_icrc` / PR200 tip**; date label Asia/Shanghai). Sibling [`2026-09-24.md`](2026-09-24.md) remains the PR203-era official on DUT `62cd3c88` — not overwritten. [`2026-09-23.md`](2026-09-23.md) remains the PR130-era official on DUT `f001833b`. [`2026-09-22.md`](2026-09-22.md) remains the PR123-era clean on historical DUT `302ac943`.
- **DUT / tip SHA:** `2b4a8408abba14e3fcd0208fadd2c54cc97c3c23` (origin/main HEAD at launch = PR200 Decision I stage-36 `vibe_icrc` merge). Expected pin at task start was this SHA. **Tip did not move.** Lint used **tip tree as-is** (no freeze overlay). RTL bits vs prior official DUT `62cd3c88` are **`rtl/nw/vibe_icrc.sv` only** (pyCircuit GENERATED/HAND-FINISHED header + footer; CRC body unchanged). Tip **includes** Decision I leaves through stage-36 (`vibe_icrc`) — expected.
- **Written freeze:** **UNFROZEN** (Decision I). Last historical pin `302ac943c3737c288f3af6c4857bb3a9b1683a26` is **VOID as current**. **Not re-pinned tonight.** Do not edit SPEC/CHANGELOG/STATUS freeze text here. Path B **hold**.
- **`git diff 62cd3c88...HEAD -- rtl/ include/`:** **NON-EMPTY** — 1 file, +6/−0. `rtl/nw/vibe_icrc.sv` header/footer comments only. `include/` **empty**.
- **`git diff 62cd3c88...HEAD -- rtl/port`:** **empty** (`vibe_port` / F1 `ovf_l` untouched)
- **`git diff 62cd3c88...HEAD -- rtl/cdc`:** **empty** (gray-pointer / AFULL / gear bodies and Decision I headers unchanged vs 09-24 DUT)
- **`git diff 302ac943...HEAD -- rtl/ include/`:** **NON-EMPTY** — 36 files, +255/−10. Same Decision I pyCircuit headers as 09-24 plus tonight's `vibe_icrc` leaf headers. `vibe_rs128_120_dec.sv` NBA reset unroll already on main. `include/` **empty**. `-- rtl/port` **empty**. `-- rtl/cdc` headers only (same five cells as 09-24).
- **main HEAD checked:** `2b4a8408abba14e3fcd0208fadd2c54cc97c3c23` (same RTL bits as lint DUT)
- **Tool:** Verilator 5.020 2024-01-01 (Debian 5.020-1) — same Ubuntu 24.04 package as [`2026-09-24.md`](2026-09-24.md)
- **Command:** `reports/lint/run_lint.sh` (`--lint-only -Wall -Wno-fatal --top-module vibe_ub_switch`) — **exit 0** (`test Error- codes = 0` passed)
- **Waivers:** `reports/lint/vibe_ub_switch.vlt` + `WAIVERS.md` (**unchanged**; no `.vlt` expansion). Existing UNSUPPORTED waivers remain PCS-only (`vibe_pcs_rx.sv:5`, `vibe_pcs_rx_fec.sv:14`, `vibe_pcs_rx_unpack.sv:17`) and still sit on those input-default lines.
- **Delta baseline:** [`2026-09-24.md`](2026-09-24.md) (0 `%Error`; NEW keys 41 vs 2026-09-23, all header line-number churn / **no substantive new diagnostics**; warning lines 1075; unique keys 157; LATCH/UNOPTFLAT/BLKSEQ=0). That clean is PR203 / DUT `62cd3c88`. Nightly [`2026-09-08.md`](2026-09-08.md) DUT `32a7f5e0` is **VOID**.

## Result

Official `run_lint.sh` **completes a full `-Wall` sweep**. Verilator emits **0** `%Error-*` codes. Warning-line and unique-key **totals** match the 2026-09-24 official log (1075 / 157). Class counts match. Stage-36 `vibe_icrc` headers are comments only: that file still has **zero** lint keys (same as 09-24, when the leaf body was already on main without headers). No other RTL file changed vs `62cd3c88`, so there is **no** `file:line:rule` subtract and **no** include-note line-number churn. `vibe_pma_bnd.sv` still has **zero** lint keys; `vibe_afifo.sv` still has **zero** primary keys. Official log was **rewritten** for tonight and is **byte-identical** to the 2026-09-24 committed log.

| | today (official) | prior tip clean (`2026-09-24`) |
|--|------:|-------------------------:|
| `%Error-*` | **0** | **0** |
| `%Warning-*` lines | 1075 | 1075 |
| unique `file:line:rule` keys | 157 | 157 |
| **NEW keys** (raw subtract) | **0** | — |
| GONE keys | **0** | — |
| **substantive new diagnostics** | **no** | — |

### NEW keys (0 vs 2026-09-24)

None. Raw NEW key count = **0**. No header line-number churn tonight (the only RTL delta is comment headers on a leaf that already had zero keys). **No new warning class. No new rule. No new diagnostic content. No waiver. No RTL ECO.** `file:line:rule` subtract (no column) is empty.

### GONE keys (0 vs 2026-09-24)

None. The two pre-ECO `%Error-UNSUPPORTED` input defaults stay **GONE** (already cleared on 2026-09-21 / still GONE on post115 / 2026-09-22 / 2026-09-23 / 2026-09-24). Not re-listed as tonight's GONE.

### Warning classes

| Class | today (official) | prior tip clean (`2026-09-24`) |
|-------|------:|----------:|
| UNUSEDPARAM | 838 | 838 |
| UNUSEDSIGNAL | 148 | 148 |
| PINCONNECTEMPTY | 37 | 37 |
| WIDTHTRUNC | 30 | 30 |
| WIDTHEXPAND | 18 | 18 |
| VARHIDDEN | 4 | 4 |
| **UNOPTFLAT** | **0** | **0** |
| **LATCH** | **0** | **0** |
| **BLKSEQ** | **0** | **0** |

`vibe_ub_switch.lint.log` is tonight's official 5.020 run (0 Errors; full `-Wall`; identical to the 2026-09-24 committed log). No throwaway `.vlt` was needed.

No RTL/SPEC/TB/`include/` change in this reports PR. No `.vlt` / `WAIVERS.md` expansion. No issue opened (not for F1).
F1 `ovf_l` **untouched** / **not ECO'd**.
Decision I remains **UNFROZEN**. Path B **hold**.
No signoff claim. Not 4/3. Not new 1/3.
