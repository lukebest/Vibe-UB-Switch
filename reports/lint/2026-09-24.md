# Nightly lint 2026-09-24 (SGT)

- **When:** 2026-09-24 03:17 SGT (**official nightly after Decision I leaves through stage-35 + PR201/PR203 TB-only tip**; date label Asia/Shanghai). Sibling [`2026-09-23.md`](2026-09-23.md) remains the PR130-era official on DUT `f001833b` — not overwritten. [`2026-09-22.md`](2026-09-22.md) remains the PR123-era clean on historical DUT `302ac943`.
- **DUT / tip SHA:** `62cd3c88b04ffc90429a38a9cd83bc89642ac1f2` (origin/main HEAD at launch = PR203 TB-only `vibe_saf_ing` merge). Expected pin at task start was `9a797225319aac1a982a18cc191713de8a5eab2a` (PR201 TB-only `vibe_voq_egr`); **tip moved** by PR203 (TB-only). Lint used **tip tree as-is** (no freeze overlay). RTL bits vs `9a79722` are **empty** (TB/docs only after that SHA). Tip **includes** Decision I leaves through stage-35 (`vibe_dll` wrap / PR198) — expected.
- **Written freeze:** **UNFROZEN** (Decision I). Last historical pin `302ac943c3737c288f3af6c4857bb3a9b1683a26` is **VOID as current**. **Not re-pinned tonight.** Do not edit SPEC/CHANGELOG/STATUS freeze text here. Path B **hold**.
- **`git diff 302ac943...HEAD -- rtl/ include/`:** **NON-EMPTY** — 35 files, +249/−10. Decision I pyCircuit GENERATED/HAND-FINISHED headers on CDC/DLL/fabric/PCS/PMA leaves; `vibe_rs128_120_dec.sv` also unrolls the reset `for` into NBA zeros (already on main; lint-safe). `include/` **empty**.
- **`git diff 302ac943...HEAD -- rtl/port`:** **empty** (`vibe_port` / F1 `ovf_l` untouched)
- **`git diff 302ac943...HEAD -- rtl/cdc`:** **NON-EMPTY** — headers only on `vibe_afifo.sv` / `vibe_sync2.sv` / `vibe_rst_sync.sv` / `vibe_gear_128_160.sv` / `vibe_gear_160_128.sv` (gray-pointer / AFULL / gear bodies unchanged)
- **`git diff f001833b...HEAD -- rtl/ include/`:** **NON-EMPTY** — 33 files (09-23 DUT already had `vibe_afifo` + `vibe_pma_bnd` headers). `-- include/` **empty**. `-- rtl/port` **empty**.
- **main HEAD checked:** `62cd3c88b04ffc90429a38a9cd83bc89642ac1f2` (same RTL bits as lint DUT)
- **Tool:** Verilator 5.020 2024-01-01 (Debian 5.020-1) — same Ubuntu 24.04 package as [`2026-09-23.md`](2026-09-23.md)
- **Command:** `reports/lint/run_lint.sh` (`--lint-only -Wall -Wno-fatal --top-module vibe_ub_switch`) — **exit 0** (`test Error- codes = 0` passed)
- **Waivers:** `reports/lint/vibe_ub_switch.vlt` + `WAIVERS.md` (**unchanged**; no `.vlt` expansion). Existing UNSUPPORTED waivers remain PCS-only (`vibe_pcs_rx.sv:5`, `vibe_pcs_rx_fec.sv:14`, `vibe_pcs_rx_unpack.sv:17`) and still sit on those input-default lines.
- **Delta baseline:** [`2026-09-23.md`](2026-09-23.md) (0 `%Error`; NEW keys 0 vs 2026-09-22; warning lines 1075; unique keys 157; LATCH/UNOPTFLAT/BLKSEQ=0). That clean is PR130 / DUT `f001833b`. Nightly [`2026-09-08.md`](2026-09-08.md) DUT `32a7f5e0` is **VOID**.

## Result

Official `run_lint.sh` **completes a full `-Wall` sweep**. Verilator emits **0** `%Error-*` codes. Warning-line and unique-key **totals** match the 2026-09-23 official log (1075 / 157). Class counts match. Decision I leaf **headers** shifted existing `file:line:rule` keys (`+3` on DLL/fabric wrap leaves, `+7` on several PCS/CDC leaves). Those are **NEW keys by subtract** and matching **GONE** of the pre-header lines — not new diagnostics. `vibe_pma_bnd.sv` still has **zero** lint keys; `vibe_afifo.sv` still has **zero** primary keys. Official log was **rewritten** for the header line-number churn (plus include-note line shifts). Those include-notes are not `file:line:rule` keys.

| | today (official) | prior tip clean (`2026-09-23`) |
|--|------:|-------------------------:|
| `%Error-*` | **0** | **0** |
| `%Warning-*` lines | 1075 | 1075 |
| unique `file:line:rule` keys | 157 | 157 |
| **NEW keys** | **41** | — |
| GONE keys | **41** | — |

### NEW keys (41 vs 2026-09-23)

All 41 are Decision I pyCircuit header line-number churn of warnings already in the 2026-09-23 official log. Same rule, same signal / same pin, new line. **No new warning class. No new rule. No waiver. No RTL ECO.** `file:line:rule` subtract (no column):

| File | Δ line | NEW keys (rule) |
|------|-------:|-----------------|
| `vibe_dll.sv` | +3 | `:36` `:40` `:41` `:47` `:48` UNUSEDSIGNAL; `:79` `:85` PINCONNECTEMPTY |
| `vibe_dll_credit.sv` | +3 | `:39` WIDTHTRUNC; `:53` UNUSEDSIGNAL |
| `vibe_dll_rx.sv` | +3 | `:33` UNUSEDSIGNAL |
| `vibe_dll_tx.sv` | +3 | `:65` `:66` `:148` `:149` `:150` `:155` `:159` `:163` WIDTHTRUNC; `:113` UNUSEDSIGNAL |
| `vibe_port_sel.sv` | +3 | `:16` `:17` `:18` UNUSEDSIGNAL |
| `vibe_route_lu.sv` | +3 | `:14` `:16` UNUSEDSIGNAL |
| `vibe_saf_ing.sv` | +3 | `:63` `:64` WIDTHTRUNC |
| `vibe_gear_128_160.sv` | +7 | `:20` UNUSEDSIGNAL |
| `vibe_pcs_rx_amctl_lock.sv` | +7 | `:38` UNUSEDSIGNAL |
| `vibe_pcs_rx_deskew.sv` | +7 | `:28` `:29` `:30` `:31` `:33` UNUSEDSIGNAL |
| `vibe_pcs_tx_amctl.sv` | +7 | `:11` `:12` `:14` UNUSEDSIGNAL |
| `vibe_pcs_tx_pack.sv` | +7 | `:30` `:31` UNUSEDSIGNAL; `:40` `:42` `:44` PINCONNECTEMPTY |

`vibe_dll.sv:41:UNUSEDSIGNAL` (`nfree`) pairs with GONE `:35` (`pending`) because `:38:UNUSEDSIGNAL` exists on both nights (`pending` today / `nfree` yesterday) and is therefore **not** NEW. Still header churn, not a new unused net.

### GONE keys (41 vs 2026-09-23)

The 41 pre-header lines listed in the 2026-09-23 official log (same files / rules as the NEW table, lines −3 or −7). Not a cleanup. The two pre-ECO `%Error-UNSUPPORTED` input defaults stay **GONE** (already cleared on 2026-09-21 / still GONE on post115 / 2026-09-22 / 2026-09-23). Not re-listed as tonight's GONE.

### Warning classes

| Class | today (official) | prior tip clean (`2026-09-23`) |
|-------|------:|----------:|
| UNUSEDPARAM | 838 | 838 |
| UNUSEDSIGNAL | 148 | 148 |
| PINCONNECTEMPTY | 37 | 37 |
| WIDTHTRUNC | 30 | 30 |
| WIDTHEXPAND | 18 | 18 |
| VARHIDDEN | 4 | 4 |
| **UNOPTFLAT** | **0** | **0** |
| **LATCH** | **0** | **0** |
| **BLKSEQ** | **0** | **0** |

`vibe_ub_switch.lint.log` is tonight's official 5.020 run (0 Errors; full `-Wall`; header + include-note line-number churn vs the 2026-09-23 committed log). No throwaway `.vlt` was needed.

No RTL/SPEC/TB/`include/` change in this reports PR. No `.vlt` / `WAIVERS.md` expansion. No issue opened (not for F1).
F1 `ovf_l` **untouched** / **not ECO'd**.
Decision I remains **UNFROZEN**. Path B **hold**.
No signoff claim. Not 4/3. Not new 1/3.
