# Nightly lint 2026-09-21 (SGT)

- **When:** 2026-09-21 02:28 SGT (**first official post-PR109 ECO clean**; date label Asia/Shanghai)
- **DUT / tip SHA:** `f234d4b036a1639fa9855de01743d04696bf5cb7` (origin/main HEAD at launch = PR109 merge). RTL bits = ECO tip `a658d14163b0e66b3875ee42a7dcc8a10024ba51`. Lint used **tip tree as-is** (no freeze overlay).
- **Written freeze:** `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**. **Not re-pinned to tip.** (Xia re-pin to `a658d141` after this clean report — not this PR.)
- **`git diff 982ddd0a...HEAD -- rtl/`:** **NON-EMPTY** — `rtl/pma/vibe_pma_bnd.sv` (+106/−11 vs freeze: PRBS23 + dest-domain resets + PR109 idle-PRBS drop), `rtl/port/vibe_port.sv` (+3/−3 `txrst_n` + `rxrst_n`). Combined **+109/−14**.
- **`git diff 982ddd0a...HEAD -- include/`:** **empty**
- **`git diff a658d141...HEAD -- rtl/ include/`:** **empty** (RTL unchanged vs ECO tip)
- **`git diff 90251fdf...HEAD -- rtl/`:** `vibe_pma_bnd.sv` only (+26/−19) — PR109 dropped `txrst_n`/`rxrst_n` input defaults and replaced the `txclk`→`rxclk` loopback `vld` mux with a local `rxclk` idle-PRBS check.
- **main HEAD checked:** `f234d4b036a1639fa9855de01743d04696bf5cb7` (same RTL bits as lint DUT)
- **Tool:** Verilator 5.020 2024-01-01 (Debian 5.020-1) — same Ubuntu 24.04 package as [`2026-09-20.md`](2026-09-20.md)
- **Command:** `reports/lint/run_lint.sh` (`--lint-only -Wall -Wno-fatal --top-module vibe_ub_switch`) — **exit 0** (`test Error- codes = 0` passed)
- **Waivers:** `reports/lint/vibe_ub_switch.vlt` + `WAIVERS.md` (**unchanged**; no `.vlt` expansion). Existing UNSUPPORTED waivers remain PCS-only (`vibe_pcs_rx.sv:5`, `vibe_pcs_rx_fec.sv:14`, `vibe_pcs_rx_unpack.sv:17`).
- **Delta baseline:** [`2026-09-20.md`](2026-09-20.md) (2 `%Error`; official log truncated on `txrst_n` + `rxrst_n`; LATCH/UNOPTFLAT/BLKSEQ=0 in the emitted log; warning lines 41; unique keys 33). Last full clean sweep shape: [`2026-09-16.md`](2026-09-16.md) (0 `%Error`; NEW keys 0; warning lines 1075; unique keys 157). Nightly [`2026-09-08.md`](2026-09-08.md) DUT `32a7f5e0` is **VOID**.

## Result

Official `run_lint.sh` **completes a full `-Wall` sweep**. Verilator emits **0** `%Error-*` codes. The two pre-ECO `%Error-UNSUPPORTED` input defaults are **GONE**. Warning-line and unique-key totals below are a complete sweep, not a truncated exit.

| | today (official) | yesterday (`2026-09-20`, official truncated) | last full sweep (`2026-09-16`) |
|--|------:|-------------------------:|-------------------------:|
| `%Error-*` | **0** | **2** | 0 |
| `%Warning-*` lines | 1075 | 41 (truncated) | 1075 |
| unique `file:line:rule` keys | 157 | 33 (truncated) | 157 |
| **NEW keys** vs last full-sweep shape | **0** | — | — |
| GONE keys (genuine vs 2026-09-20) | **2** | — | — |

### GONE keys (2 vs 2026-09-20)

These two Errors were carry-forward on 2026-09-19/20 and caused the official tool to exit (`%Error: Exiting due to 2 error(s)`). PR109 removed the `= 1'b1` defaults. They are **not** in tonight's log.

| Key | Summary |
|-----|---------|
| `vibe_pma_bnd.sv:8:UNSUPPORTED` (**GONE**) | Was `%Error-UNSUPPORTED`: default value on module input `txrst_n = 1'b1`. Port is now `input logic txrst_n` (no default). Parent `vibe_port` still ties `.txrst_n(txrst_n)` from dest-domain `u_txrst`. |
| `vibe_pma_bnd.sv:9:UNSUPPORTED` (**GONE**) | Was `%Error-UNSUPPORTED`: default value on module input `rxrst_n = 1'b1`. Port is now `input logic rxrst_n` (no default). Parent still ties `.rxrst_n(rxrst_n)` from dest-domain `u_rxrst`. |

Raw key-set subtract against the truncated 2026-09-20 official log also shows **126** keys that were missing last night. Those are **not NEW** — they are the rest of `-Wall` that Verilator never reached while Error=2. The same 126 keys exist in the 2026-09-16 full-sweep set. Do not treat them as new debt.

### NEW keys (0 vs last full-sweep shape)

None vs [`2026-09-16.md`](2026-09-16.md) / the 2026-09-19–20 diagnostic continue-sweep shape. Unique-key set is **identical** (157 keys; NEW 0; GONE 0 vs that shape). PRBS23 LFSR / dest-domain resets / idle-PRBS drop added **no** new warning keys.

### Warning classes

| Class | today (official) | yesterday (official, truncated) | last full sweep (`2026-09-16`) |
|-------|------:|----------:|----------:|
| UNUSEDPARAM | 838 | n/a (not reached) | 838 |
| UNUSEDSIGNAL | 148 | n/a (not reached) | 148 |
| PINCONNECTEMPTY | 37 | 37 | 37 |
| WIDTHTRUNC | 30 | n/a (not reached) | 30 |
| WIDTHEXPAND | 18 | n/a (not reached) | 18 |
| VARHIDDEN | 4 | 4 | 4 |
| **UNOPTFLAT** | **0** | **0** (in emitted log) | **0** |
| **LATCH** | **0** | **0** (in emitted log) | **0** |
| **BLKSEQ** | **0** | **0** (in emitted log) | **0** |

`vibe_ub_switch.lint.log` is tonight's official 5.020 run (0 Errors; full `-Wall`). No throwaway `.vlt` was needed.

No RTL/SPEC/TB/`include/` change. No `.vlt` / `WAIVERS.md` expansion. No issue opened (not for F1).
No signoff claim. Not 4/3. F1 `ovf_l` untouched.
