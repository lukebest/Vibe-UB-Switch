# Lint ECO 2026-09-08 — structural Verilator `-Wall` clear

- **When:** 2026-09-08 (Luke-approved Lint ECO)
- **Base:** `origin/main` `f0fc4df1` — `git diff 1ed4d35006848e6275e93d9bd2fc4e7f7af348f1 -- rtl/` empty (docs-only ahead of CR-B freeze)
- **Frozen RTL SHA:** `1ed4d35006848e6275e93d9bd2fc4e7f7af348f1`
- **Tool:** Verilator 5.020 2024-01-01 (Debian 5.020-1)
- **Command:** `reports/lint/run_lint.sh` (`--lint-only -Wall -Wno-fatal --top-module vibe_ub_switch`)
- **Waivers:** `reports/lint/vibe_ub_switch.vlt` unchanged; `WAIVERS.md` updated for cleared classes
- **F1:** `rtl/port/vibe_port.sv` `ovf_l` CDC path **not modified** (`git diff` empty vs freeze)

Before log: [`2026-09-08-eco-before.log`](2026-09-08-eco-before.log) (remeasured on freeze RTL).  
After log: [`vibe_ub_switch.lint.log`](vibe_ub_switch.lint.log).

Nightly [`2026-09-08.md`](2026-09-08.md) DUT `32a7f5e0` is **VOID** vs this freeze. Do not mix those class counts with this remasure.

## Before (freeze `1ed4d350`, 5.020 remasure)

| | count |
|--|------:|
| `%Error-*` | 0 |
| `%Warning-*` lines | 1081 |
| `UNUSEDPARAM` | 838 |
| `UNUSEDSIGNAL` | 148 |
| `PINCONNECTEMPTY` | 37 |
| `WIDTHTRUNC` | 30 |
| `WIDTHEXPAND` | 18 |
| `VARHIDDEN` | 4 |
| **`UNOPTFLAT`** | **2** |
| **`LATCH`** | **2** |
| **`BLKSEQ`** | **2** |

Verified file:line on this tree (pre-edit):

| Class | Location |
|-------|----------|
| LATCH | `rtl/fabric/vibe_xbar.sv:26` `req`, `win` |
| BLKSEQ | `rtl/fabric/vibe_saf_ing.sv:54-55` `plen`, `dflits` blocking `=` |
| UNOPTFLAT | `rtl/fabric/vibe_fabric.sv:223` `x_in_v`; `:50` `xb_r` |

`x_in_v` path: `x_in_v` → xbar combo → `xb_in_r` → fabric CFG6/`saf_r` `always @*` → `fab_mgmt_cfg6_hit` → `x_in_v`. False loop: hit does not depend on `xb_in_r`; they shared one process.

`xb_r` path: `xb_r` → xbar combo → `xb_d`/`xb_sop` → VOQ `wr_vl` → `wr_ready` → `xb_r`. Real loop: occupancy ready depends on combo VL from xbar data that was gated by `out_ready`.

## After (`reports/lint/run_lint.sh` on this ECO)

| | before | after |
|--|------:|------:|
| `%Error-*` | 0 | **0** |
| `%Warning-*` lines | 1081 | 1075 |
| `UNUSEDPARAM` | 838 | 838 |
| `UNUSEDSIGNAL` | 148 | 148 |
| `PINCONNECTEMPTY` | 37 | 37 |
| `WIDTHTRUNC` | 30 | 30 |
| `WIDTHEXPAND` | 18 | 18 |
| `VARHIDDEN` | 4 | 4 |
| **`UNOPTFLAT`** | 2 | **0** |
| **`LATCH`** | 2 | **0** |
| **`BLKSEQ`** | 2 | **0** |

PINCONNECTEMPTY / WIDTHTRUNC `file:line` keys shifted with the ECO (same counts, no new class). No `.vlt` expansion.

## Fixes (no Overlay B / fire / width change)

1. **LATCH** — `vibe_xbar.sv`: default `req`/`win` on every combo path (block + each egress iteration). RR still starts at `rr[e]`; locked/down ports still do not grant.
2. **UNOPTFLAT `x_in_v`** — `vibe_fabric.sv`: split CFG6 term/hit/data combo from `saf_r` combo so xbar ready is not in the same process as `fab_mgmt_cfg6_hit`.
3. **UNOPTFLAT `xb_r`** — `vibe_xbar.sv`: candidate grant (`out_data`/`out_sop`/`out_eop`) is independent of `out_ready`; accept (`out_vld` / `in_ready`) still requires `out_ready`. Same fire when ready=1; VOQ still writes only on `wr_en && wr_ready`.
4. **BLKSEQ** — `vibe_saf_ing.sv`: `plen`/`dflits` computed in combo `always @*`; clocked process uses `<=` only.

## Spot-check (existing TBs, no TB edits)

Icarus 12.0: `tc_xbar_unit`, `tc_saf_ing`, `tc_voq_rd`, `tc_fabric_line_holes`, `tc_fabric_g1` — all **PASS**.

## Non-goals

- No SPEC/TB change
- No `ovf_l` ECO (F1)
- No `$display` / `force`
- No `.vlt` expansion
- No mass UNUSEDPARAM clean
