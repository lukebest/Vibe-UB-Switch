# Nightly lint 2026-09-20 (SGT)

- **When:** 2026-09-20 03:08 SGT (post-PR107 health; date label Asia/Shanghai)
- **DUT / tip SHA:** `90251fdf33bdb3a9bdf5da03f72e381d46e63e71` (origin/main HEAD at launch = PR107 health merge). RTL bits = last official lint tip `83c0a1a05708ddc8e553ac0d5bb31efc3ec4af0f` (PR105). Only reports landed since (PR106 lint+CDC 2026-09-19, PR107 health). Lint used **tip tree as-is** (no freeze overlay).
- **Written freeze:** `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**. Decision **H not chosen**. **Not re-pinned to tip.**
- **`git diff 982ddd0a...HEAD -- rtl/`:** **NON-EMPTY** — `rtl/pma/vibe_pma_bnd.sv` (+99/−11), `rtl/port/vibe_port.sv` (+3/−3 `txrst_n` + `rxrst_n`). Combined **+102/−14**.
- **`git diff 982ddd0a...HEAD -- include/`:** **empty**
- **`git diff 83c0a1a0...HEAD -- rtl/ include/`:** **empty** (RTL unchanged vs last official lint tip)
- **main HEAD checked:** `90251fdf33bdb3a9bdf5da03f72e381d46e63e71` (same RTL bits as lint DUT)
- **Tool:** Verilator 5.020 2024-01-01 (Debian 5.020-1) — same Ubuntu 24.04 package as [`2026-09-19.md`](2026-09-19.md)
- **Command:** `reports/lint/run_lint.sh` (`--lint-only -Wall -Wno-fatal --top-module vibe_ub_switch`) — **exit 1** (`test Error- codes = 0` failed)
- **Waivers:** `reports/lint/vibe_ub_switch.vlt` + `WAIVERS.md` (**unchanged**; no `.vlt` expansion). Existing UNSUPPORTED waivers remain PCS-only (`vibe_pcs_rx.sv:5`, `vibe_pcs_rx_fec.sv:14`, `vibe_pcs_rx_unpack.sv:17`).
- **Delta baseline:** [`2026-09-19.md`](2026-09-19.md) (2 `%Error`; NEW keys 1 vs 2026-09-18 = `rxrst_n` UNSUPPORTED; official log truncated on `txrst_n` + `rxrst_n`; LATCH/UNOPTFLAT/BLKSEQ=0; warning lines 41; unique keys 33). RTL bits vs that baseline are **unchanged**. Nightly [`2026-09-08.md`](2026-09-08.md) DUT `32a7f5e0` is **VOID**.

## Result

Official `run_lint.sh` **does not complete a full `-Wall` sweep**. Verilator emits two `%Error-UNSUPPORTED` codes on adjacent PMA input defaults and **exits** (`%Error: Exiting due to 2 error(s)`). Warning-line and unique-key totals below are therefore **truncated**, not a cleanup. Tonight's official log is **byte-identical** to the 2026-09-19 committed `vibe_ub_switch.lint.log`.

| | today (official) | yesterday (`2026-09-19`, official) |
|--|------:|-------------------------:|
| `%Error-*` | **2** | **2** |
| `%Warning-*` lines | 41 (truncated) | 41 (truncated) |
| unique `file:line:rule` keys | 33 (truncated) | 33 (truncated) |
| **NEW keys** | **0** | 1 |
| GONE keys (genuine) | **0** | 0 |

Carry-forward (not NEW tonight): `vibe_pma_bnd.sv:8:UNSUPPORTED` (`txrst_n`) and `vibe_pma_bnd.sv:9:UNSUPPORTED` (`rxrst_n`). Raw key-set subtract against the pre-PRBS23 2026-09-16 full sweep would still show 126 missing yesterday-of-that-night keys plus the two Errors. Those remain **not GONE** — they were not re-emitted because the tool exited. Do not treat them as cleared debt.

### NEW keys (0)

None vs 2026-09-19. Both Errors are carry-forward:

| Key | Summary |
|-----|---------|
| `vibe_pma_bnd.sv:8:UNSUPPORTED` (carry-forward) | `%Error-UNSUPPORTED`: default value on module input `txrst_n = 1'b1` (Luke PRBS23). Unchanged vs 2026-09-18/19. |
| `vibe_pma_bnd.sv:9:UNSUPPORTED` (carry-forward) | `%Error-UNSUPPORTED`: default value on module input `rxrst_n = 1'b1` (Luke `0f783df6`). Was NEW on 2026-09-19; **not NEW** tonight. **Not waived tonight**. |

Unlike a single-Error night, the official log **does emit** both port defaults on consecutive lines before `%Error: Exiting due to 2 error(s)`. Truncation still applies to the rest of `-Wall`.

### Warning classes

Official truncated run only reached PINCONNECTEMPTY + VARHIDDEN (plus the two Errors) before exit — same warning cut as 2026-09-19:

| Class | today (official) | yesterday (official) |
|-------|------:|----------:|
| UNUSEDPARAM | n/a (not reached) | n/a (not reached) |
| UNUSEDSIGNAL | n/a (not reached) | n/a (not reached) |
| PINCONNECTEMPTY | 37 | 37 |
| WIDTHTRUNC | n/a (not reached) | n/a (not reached) |
| WIDTHEXPAND | n/a (not reached) | n/a (not reached) |
| VARHIDDEN | 4 | 4 |
| **UNOPTFLAT** | **0** (in emitted log) | **0** (in emitted log) |
| **LATCH** | **0** (in emitted log) | **0** (in emitted log) |
| **BLKSEQ** | **0** (in emitted log) | **0** (in emitted log) |

`vibe_ub_switch.lint.log` is tonight's official 5.020 run (2 Errors + truncated warnings; identical bytes to 2026-09-19).

### Diagnostic continue-sweep (not official; waivers not changed)

A **throwaway** extra `.vlt` for `vibe_pma_bnd.sv:8` and `:9` UNSUPPORTED was used only in `/tmp` (not written under `reports/lint/`, not committed) so the PRBS23 / `rxrst_n` body could be linted. That diagnostic sweep: **0** `%Error-*`; warning lines **1075**; unique keys **157**; **NEW keys 0** vs the 2026-09-16/17/19 full-sweep shape; **GONE keys 0**; UNUSEDPARAM 838 / UNUSEDSIGNAL 148 / PINCONNECTEMPTY 37 / WIDTHTRUNC 30 / WIDTHEXPAND 18 / VARHIDDEN 4; **LATCH=0 UNOPTFLAT=0 BLKSEQ=0**. The `rxrst_n` / loopback / `tx_pcs_d` body added **no** new warning keys. This does **not** make the official run green and is **not** a waiver expansion.

No RTL/SPEC/TB/`include/` change. No `.vlt` / `WAIVERS.md` expansion. No issue opened (not for F1, not for `txrst_n` / `rxrst_n` UNSUPPORTED — PM / decision H).
No signoff claim. Not 4/3. F1 `ovf_l` untouched.
