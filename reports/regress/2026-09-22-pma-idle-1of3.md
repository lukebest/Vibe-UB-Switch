# PMA idle-mark 新系列 1/3 — 2026-09-22 (Asia/Shanghai)

**这是「新系列 1/3」。** It is **NOT** gate-after health, **NOT** 4/3, **NOT** signoff, **NOT** the pyuvm/PR119 acceptance gate.

Xia 已把书面 DUT freeze 钉到 #115 idle-mark ECO tip（PR120 docs；DUT `302ac943`）。
本文件 **打开** 对 DUT=`302ac943` 的新连续全绿计数：**1/3**。

旧系列 on `a658d141` / PR114 FAIL（`reports/regress/2026-09-21-pma-eco-1of3.md`）是
**CLOSED 历史保留** — consecutive green **0/3**。**不要** 把它续成 2/3 或 4/3。
不要从那组重开计数。本跑是 **新系列**，不是 PR114 的下一拍。

闸后健康 `reports/regress/2026-09-22.md`（PR121；tip 当时 `932037f9`；书面 freeze
当时仍是 `a658d141`；DUT 内容已是 `302ac943`）是 **另一件事** — **不是** 本系列、
**不是** 1/3、**不是** 4/3。本文件才是 Xia 重钉之后的 **新系列 1/3**。

聊天 PASS 不算验收。本文件是给芯片开发PM 的 in-repo 记录。
`rtl/`、`include/`、SPEC、checkers **均未改**。无 overlay。只新增本报告。

| Item | Value |
|------|--------|
| Label | **PMA idle-mark 新系列 1/3**（**不是** 闸后健康 / **不是** 4/3 / **不是** 签核 / **不是** pyuvm/PR119 门） |
| Checkout tip SHA | `dababafdb0f3e54c4bef2a98cfd942e7e25ba6c7`（PR120 Xia freeze-pin merge；current main after PR121 night health + PR120；`git rev-parse HEAD` at start） |
| 书面 DUT freeze | `302ac943c3737c288f3af6c4857bb3a9b1683a26`（#115 idle-mark ECO；merge `df7c286e` 仅追溯） |
| `git diff 302ac943c3737c288f3af6c4857bb3a9b1683a26...HEAD -- rtl/` | **EMPTY** |
| `git diff 302ac943c3737c288f3af6c4857bb3a9b1683a26...HEAD -- include/` | **EMPTY** |
| `rtl/` / `include/` / SPEC / checkers 本跑 | **未改**。无 overlay |
| xsim / xvlog | **不可用** — `xvlog`/`xelab`/`xsim` 不在 PATH。记 **non-DUT**（缺工具） |
| 本跑功能门 | `make -C tb/vibe sim-icarus`（Icarus Verilog；历史 124-bucket / PR114 同口径）— **MEASUREMENT PENDING** |
| Cov | `make -C tb/vibe cov`（Verilator）— **MEASUREMENT PENDING** |
| `XSIM_MAKE_EXIT` | **n/a**（本跑不跑默认 `make sim`：那是 pyuvm/PR119 门；`xvlog` 仍不在 PATH） |
| `SIM_ICARUS_EXIT` | **PENDING** |
| `COV_EXIT` | **PENDING** |
| 通过率 | **PENDING**（历史对齐 124/124 buckets） |
| Result | **PENDING** — 本文件是「新系列 1/3」骨架；功能门 / cov 实测后回填 |
| `ovf_l` | F1 永久冻结 CDC-WARN — **不** 为此开 issue |

## Pin confirm

- Checkout: tip `dababafdb0f3e54c4bef2a98cfd942e7e25ba6c7`（PR120 Xia freeze-pin merge）。
  Start-of-run `git rev-parse HEAD` = 该值。
- 书面 freeze: `302ac943c3737c288f3af6c4857bb3a9b1683a26`（#115 idle-mark）。
- `git diff 302ac943c3737c288f3af6c4857bb3a9b1683a26...HEAD -- rtl/` **EMPTY**。
- `git diff 302ac943c3737c288f3af6c4857bb3a9b1683a26...HEAD -- include/` **EMPTY**。
- Tip-ahead vs DUT：docs / reports / `tb/` only（STATUS 仍可能滞后；PR119 pyuvm；PR120 pin；PR121 闸后健康）。**无** rtl/include。
- F1 `ovf_l` untouched（Decision F1 stands）。
- 本跑未改 `rtl/` / `include/` / SPEC / checkers。只新增本报告。
- 默认 `make -C tb/vibe sim` **不跑**（PR119 后是 pyuvm 门，不是本系列口径）。

## 本跑命令（stock Icarus 口径 = PR114）

历史夜间门曾经是 `make -C tb/vibe sim`（Icarus）然后 `make -C tb/vibe cov`。
**tip 已两改：** 先把默认 `sim` 指到 xsim+UVM，再被 PR119 指到 **uvm-python**。
README 仍把历史 124-bucket 写成 `make -C tb/vibe sim-icarus`。
`cov` 仍是 Verilator，不依赖 xsim / pyuvm。

本跑 **明确不跑** 默认 `make -C tb/vibe sim`（那是 pyuvm/PR119 门，不是本系列口径）。

```bash
# 1) 记录 xvlog / xsim 缺工具（non-DUT；本跑不跑默认 make sim）
command -v xvlog xelab xsim
# → not on PATH. XSIM miss = non-DUT

# 2) 仓库支持的 stock Icarus 全回归（本跑功能门；与 PR114 同口径）
make -C tb/vibe sim-icarus
# → SIM_ICARUS_EXIT=PENDING

# 3) 覆盖率
make -C tb/vibe cov
# → COV_EXIT=PENDING
```

未改 Makefile / 未加 overlay / 未 chase xsim / 未跑 pyuvm。
xsim/`xvlog` 缺工具 = **non-DUT fail**。

## 通过率

功能门 = **stock Icarus `sim-icarus`**（pyuvm / xsim 本跑不计入通过率）。
**MEASUREMENT PENDING** — 回填 suite / units / top / neg 桶计数。历史对齐 **124/124**。

## 失败列表

**MEASUREMENT PENDING。** 规则：先排除 TB / 工具；TB 排除后仍红 → 开 GitHub issue 给设计。
**不改** `rtl/`。F1 `ovf_l` **不开** issue。

## 代码覆盖 (code)

**MEASUREMENT PENDING。** 门槛：LINE ≥95% + 点名 waiver。不追 LINE 100%。

## 功能覆盖 (functional)

无 `covergroup`。官方 **159** 个 TP-0.3 ID。HOLE 九个保持 **非目标**。
**MEASUREMENT PENDING** — published 目标仍是 **159**。

## 断言覆盖 (assertion)

**N/A** — 本 TB / DUT 无 SVA `assert` / `cover property` / `covergroup`。

## 与连续全绿的关系

| Item | Note |
|------|------|
| 旧 3/3（`2026-09-03` ×3，freeze `32a7f5e0`） | **VOID** |
| CR-B re-gate 3/3（`2026-09-08-crb-*of3`，freeze `1ed4d350`） | **VOID** |
| Lint ECO 3/3（`2026-09-08-lint-eco-*of3`，freeze `982ddd0a`） | **CLOSED 历史保留**（PR68）。**不要** 续成 4/3 |
| 2026-09-09 … 2026-09-22 闸后健康 | **不是 4/3 / 不是 1/3**（含 PR110 `2026-09-21.md`、PR121 `2026-09-22.md`） |
| PMA ECO 新系列 1/3（`a658d141` / PR114） | **FAIL / CLOSED 历史保留** — **不要** 续成 2/3 或 4/3 |
| 本次 | **「新系列 1/3」**（tip `dababafd` + 书面 DUT freeze `302ac943`；rtl/include vs freeze **EMPTY**）— **不是闸后健康** — **不是 4/3** — **不是签核** — **不是 pyuvm/PR119 门** |
| 新系列 2/3、3/3 | **未开始**（等本 1/3 实测回填 + PM 验收） |

Path B impl gate stays **NOT PASS**. This report is PMA idle-mark **新系列 1/3**, not signoff.
Handoff: 验证 → 芯片开发PM 验收（新系列 1/3，DUT=302ac943，stock Icarus）。Leave merge for PM.
