# 闸后夜间健康检查 — 2026-09-17 (Asia/Shanghai)

**这是闸后夜间健康检查，不是新的连续全绿 / 不是 4/3 / 不是新的 1/3。**
不要记成 **4/3**，也不要重开 3 次计数，也不要把本夜算成新系列的 **1/3**。

**本夜是 PRBS23 后的第一次夜间健康快照**（tip `1ed3fbed` = PR96 docs-only merge；
RTL 内容与 Luke PRBS23 tip `268d038f` 相同）。

Lint ECO consecutive-green **3/3 已关闭**（`reports/regress/2026-09-08-lint-eco-1of3.md` /
`2026-09-08-lint-eco-2of3.md` / `2026-09-08-lint-eco-3of3.md`；PR68）。
书面 freeze 仍为 `982ddd0a`，**BROKEN / H hold-pin**。Decision **H 未选**。
本夜 **不** 把 freeze 重钉到 tip。该计数保持关闭。本文件只做闸后健康快照。

旧系列 **VOID**，不要对照：
- `32a7f5e0` 夜间健康 / 2026-09-03 3/3（DUT 端口已改名）
- CR-B `1ed4d350` 系列（`2026-09-08-crb-*of3`；Lint ECO PR62 落地后作废）

基线对照：`reports/regress/2026-09-08-lint-eco-3of3.md`（Lint ECO 3/3 关闭记录）。
上一夜闸后健康：`reports/regress/2026-09-16.md`（PR94；**也不是 4/3**；当时
`git diff 982ddd0a...HEAD -- rtl/` 仍 empty，本夜已 **非空**）。

聊天 PASS 不算验收。本文件是给芯片开发PM 的 in-repo 记录。
进仓后 PM 即时验。

| Item | Value |
|------|--------|
| Checkout tip SHA | `1ed3fbed15d3c0de3f1cacdfae98809e61468923`（PR96 docs-only；RTL = Luke PRBS23 `268d038f`） |
| 书面 DUT freeze | `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**。本夜 **未** 重钉到 tip |
| `git diff 982ddd0a...HEAD -- rtl/` | **NON-EMPTY** — `rtl/pma/vibe_pma_bnd.sv`（+75/−8）、`rtl/port/vibe_port.sv`（+1/−1 `txrst_n`） |
| `git diff 982ddd0a...HEAD -- include/` | **empty** |
| `rtl/` / `include/` / SPEC 本夜 | **未改**。无 overlay。默认 **不改 TB**（本夜零 TB 源码编辑） |
| TB on tip | UVM 1.2 + AMD Vivado **xsim** 现为 `make -C tb/vibe sim` 的默认路径。历史 Icarus 门仍是 `make -C tb/vibe sim-icarus`。tip 另含 Luke TB `466b40f0`（`force u_p.am_locked`） |
| xsim / UVM | **不可用** — `xvlog`/`xelab`/`xsim` 不在 PATH，无 Vivado Simulator license。`make -C tb/vibe sim` 于 suite 第一步失败：`xvlog: command not found`（`XSIM_MAKE_EXIT=2`） |
| 本夜功能门 | `make -C tb/vibe sim-icarus`（Icarus Verilog **12.0**）— **本夜实测**（2026-09-17 02:56–02:58 CST）。units 中 1 compile_fail 后 make 停；`top` / `neg` 本夜用 `Makefile.icarus` 单独补跑（02:59 CST） |
| Cov | `make -C tb/vibe cov`（Verilator **5.020**；与 xsim 无关）— **本夜实测**（2026-09-17 03:00–03:05 CST） |
| `SIM_ICARUS_EXIT` | **2**（1 unit compile_fail；见失败列表） |
| `XSIM_MAKE_EXIT` | **2**（缺工具，不是 DUT FAIL） |
| `COV_EXIT` | **0** |
| 通过率 | **123/124 buckets**（suite 27 + units 84 + top 1 + neg 11；1 compile_fail） |
| Result | **FAIL — TB compile only**（`tc_nw_pkt_pma_loopback`）。DUT 无功能红。LINE **721/755 = 95.5% ≥ 95% + waiver**；功能 published **159**（stock 未跑 TP-PHY-012；见下）；断言 **N/A** |
| `ovf_l` | F1 永久冻结 CDC-WARN — 本夜不为此开 issue |

## Pin confirm

- Checkout: tip `1ed3fbed15d3c0de3f1cacdfae98809e61468923`（PR96 docs-only）。
- 书面 freeze: `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**。
  **Do not re-pin freeze to tip.**
- `git diff 982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6...HEAD -- rtl/` **NON-EMPTY**：
  `rtl/pma/vibe_pma_bnd.sv`，`rtl/port/vibe_port.sv`。
- `git diff 982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6...HEAD -- include/` **empty**。
- F1 `ovf_l` untouched（Decision F1 stands）。
- `rtl/` 未搬家。本夜未改 `rtl/` / `tb/` / `include/` / SPEC / checkers。只新增本报告。

## 本夜命令（flow 与 2026-09-16 相同）

历史夜间门曾经是 `make -C tb/vibe sim`（Icarus）然后 `make -C tb/vibe cov`。
**tip 已改：** `tb/vibe/README.md` / `tb/vibe/Makefile` 把默认 `sim` 指到 xsim+UVM；
Icarus 全回归改名为 `sim-icarus`。`cov` 仍是 Verilator，不依赖 xsim。

本夜实际跑过：

```bash
# 1) 记录默认 UVM 路径不可用（无商业 license / 无 xvlog）
make -C tb/vibe sim
# → xvlog: command not found ; make suite Error 127 ; XSIM_MAKE_EXIT=2

# 2) 仓库支持的开源全回归（本夜功能门）
make -C tb/vibe sim-icarus
# → SIM_ICARUS_EXIT=2
#    SUITE 27/27 ; UNITS pass_files=84 fail_files=1 compile_fail=1
#    make 在 units 目标以 Error 1 停下（top/neg 未进这次 make）

# 2b) units 失败后补跑（同一 Icarus 树；非 TB 编辑）
make -C tb/vibe -f Makefile.icarus top
# → TOP_EXIT=0 ; PASS tc_top_smoke
make -C tb/vibe -f Makefile.icarus neg
# → NEG_EXIT=0 ; 11/11 PASS

# 3) 覆盖率（与历史相同）
make -C tb/vibe cov
# → COV_EXIT=0 ; LINE 721/755 = 95.5% ; TOGGLE 9733/17877 = 54.4%
#    clusters=58（本夜 rsdec 进 merge；port/top OOM skip）
```

工具：Icarus Verilog **12.0 (stable)**；Verilator **5.020 2024-01-01**（Debian 5.020-1）。
未改 Makefile / 未加 overlay / 未 chase xsim。xsim 缺工具 = **non-DUT fail**（与 2026-09-16 相同）。

## 通过率

功能门 = **Icarus `sim-icarus`**（xsim 本夜未跑通，不计入通过率）。

| Bucket | pass | fail | compile_fail | 通过率 |
|--------|------|------|----------------|--------|
| suite (`Makefile.icarus` suite) | **27** | 0 | 0 | **27/27 = 100%** |
| units (`run_units.sh`) | **84** | **1** | **1** | **84/85** |
| top (`tc_top_smoke`；units 失败后单独补跑） | **1** | 0 | 0 | **1/1 = 100%** |
| neg (`scan_absent.sh`；同上单独补跑） | **11** | 0 | 0 | **11/11 = 100%** |
| **合计（文件/桶）** | **123** | **1** | **1** | **123/124** |
| summarize | `TOTAL_PASS_LINES=157` | `TOTAL_FAIL_LINES=1` | — | 1 compile FAIL |

`SUITE pass=27 fail=0 ran=27` / `SUITE_RESULT PASS`.
`UNITS pass_files=84 fail_files=1 compile_fail=1`.
`PASS tc_top_smoke`.

（`TOTAL_PASS_LINES` 是本夜 `summarize.sh` 对 `results/*.log` 的 `^PASS ` 行计数，
含多 PASS 的 unit log；`summarize.sh` 排除 `cov.log`。相对 2026-09-16 的 158
少 1，就是未跑通的 `tc_nw_pkt_pma_loopback`。）

Icarus is the functional gate tonight. xsim/UVM is documented on tip but **not measured**
（工具缺失，不是 DUT 红）。

## 失败列表

**1 条（TB compile，不是 DUT）：** `tc_nw_pkt_pma_loopback`。

先排除 TB：

- Luke TB `466b40f0`（本 tip 祖先）把 `bring_link` 从
  `force u_p.u_lmsm.am_locked` / `u_p.u_lmsm.lid_bad` 改成
  `force u_p.am_locked` / `u_p.lid_bad`，并保持 force（不再 release）。
- `u_p.am_locked` / `u_p.lid_bad` 是 `vibe_port` 上同一根 net，同时接到
  `vibe_pcs_rx` **output**（`lid_bad` 为 `assign`；`am_locked[]` 来自 lock 子模块）
  和 `vibe_lmsm` input。
- Icarus 12.0 把对该 net 的 `force` 当成 `reg`，elaborator 拒绝
  `vibe_pcs_rx` 的 continuous/output 连接：

```
rtl/port/vibe_port.sv:104: error: reg am_locked; cannot be driven by primitives or continuous assignment.
rtl/port/vibe_port.sv:104:      : Port 14 (am_locked) of vibe_pcs_rx is connected to am_locked
rtl/port/vibe_port.sv:104: error: reg lid_bad; cannot be driven by primitives or continuous assignment.
rtl/port/vibe_port.sv:104:      : Port 15 (lid_bad) of vibe_pcs_rx is connected to lid_bad
4 error(s) during elaboration.
FAIL tc_nw_pkt_pma_loopback (compile)
```

- 同 PORT_RTL、同 Icarus：`tc_port_smoke` / `tc_nw_pkt_to_pma_tx` **PASS**
  （它们仍 force `u_p.u_lmsm.*`，不 back-drive PCS RX net）。
- 最小复现（与 `run_units.sh` 同一条 `iverilog -g2012` + PORT_RTL +
  `tb/vibe/tests/tc_nw_pkt_pma_loopback.sv`）：同上 4 error。
- 本夜 **未改 TB**。为排除 DUT，只在 `/tmp` 把 force 改回 `u_p.u_lmsm.*`
  （不进仓）后：compile OK，**PASS 100/100 packets**，`am_lock_end=1111`。
  PRBS23 空闲填充 **没有** 把 GOLDEN 环回打红。

结论：**TB / Icarus**（`466b40f0` force 打在 PCS 驱动网上）。**不是 DUT mismatch。**
无波形（compile 未进 vvp）。**不开 GitHub issue**（规则：先排除 TB；向设计开 issue
仅当 DUT）。F1 `ovf_l` 仍为永久冻结 WARN，本夜不为此开 issue。不叫醒 Luke。

xsim 路径因 `xvlog` 不存在而无法启动，**不是** DUT mismatch（与 2026-09-16 相同）。

Verilator coverage clusters still skip `vibe_suite` (VOQ OOM) and `port`/`top`
(`NOTE cov: cluster … compile rc=1 (timeout/OOM skipped, not waived)`)。
本夜 `rsdec` **进 merge**（58 clusters；相对 2026-09-16 的 57 / `rsdec` skip）。
Icarus `tc_rs_dec_syndrome.log` 本夜 **PASS**。

Verilator-only `FAIL` lines on `tc_lmsm_walk` / `tc_pcs_fec_dual_enc` /
`tc_pcs_fec_t2` / `tc_route_lu` / `tc_rs_dec_syndrome` during `make cov`
are the same tool/timing limits as Lint ECO 1/3–3/3 与 2026-09-16；
those TCs **PASS** on Icarus。

**PRBS23 后新增 Verilator-only FAIL（Icarus 门 PASS，不算功能红）：**
`tc_pma_512b_slice` / `tc_pma_922mhz` — Verilator 停在同一拍
`pcs_pma_txdata`（LFSR 不前进 / `vld` 包不切到 lane 字）。Icarus 对应
`.log` 均为 `PASS`（slice 检查了 idle PRBS 前进 + vld 切片）。**TOOL**。
Icarus is the functional gate.

## 代码覆盖 (code)

Tool: Verilator **5.020** `--coverage-line --coverage-toggle`。合并 **58** 个
cluster `.dat`（相对昨夜 57：本夜 `rsdec` 进 merge）。`vibe_suite` 未 bind
（VOQ OOM）。port/top compile rc=1（OOM skipped, not waived）— 0 records。
`vibe_rs128_120_dec.sv` 本夜 **13/13 LINE**。

LINE **721/755 = 95.5%** ≥ 95%。不追 LINE 100%。相对昨夜 **702/732** 的差 =
`rsdec` 回到分母 **加上** PRBS23 `vibe_pma_bnd` 新 LINE（`11/15`）。
相对 2026-09-15 **715/745**（含 rsdec、freeze 对齐）：分母 +10 / 命中 +6，
即 PMA PRBS23 新点。

| Metric | Hit/tot | % | Gate |
|--------|--------:|--:|------|
| **LINE** | **721/755** | **95.5** | **≥95% + waiver — PASS** |
| **TOGGLE** | **9733/17877** | **54.4** | 无 80% 门槛；wide-bus unused bits + PRBS23 宽总线 |
| **FSM** | n/a | — | 无 VCS FSM engine；LINE 打在 state `case`/`if` |

不追 LINE 100%。

Waiver（点名，与本夜 `cov_report.md` uncovered LINE + `COVERAGE_HOLES.md` 一致，未改分类）：

| File:line | Class |
|-----------|--------|
| `vibe_dll_tx.sv:98/:144` | **WAIVER / 防御** |
| `vibe_cfg_space.sv:96–99` | **WAIVER** — RW1C `hold_fall` HW-clear。设计 agreed，**不改** `rtl/` |
| `vibe_lmsm.sv:101–108` | **TOOL** (`tmr_load`) |
| `dec_lid` / dll_tx pad combo | **TOOL** |
| pcs rem/pend / g1 `:51` | **TB空洞 — WAIVE** |
| `vibe_xbar.sv:50 else` | **TB空洞 — WAIVE** — ECO lock-hold implicit else（`locked && !matching in_vld/dst`）。与 Lint ECO 1/3–3/3 相同。仍在本夜 uncovered LINE。Not a DUT fail。 |

本夜 uncovered LINE 与 Lint ECO 3/3 同类：`cfg_space` hold_fall `:96–99`、`dll_tx`
`:98/:144/:151/:155/:159`、`lmsm` `:101–108`、`pcs_rx` rem/pend、
`pcs_rx_amctl_lock` `:55–58`、`pcs_tx_g1` `:51`，plus ECO
`vibe_xbar.sv:50 else`（TB hole, waived）。未改 waiver 分类，未改 RTL。

### PRBS23 后新空洞（诚实分类；不追 LINE 100%）

`vibe_pma_bnd.sv` 本夜 **11/15 = 73.3%**（freeze 对齐夜为满命中小模块）。
新 uncovered LINE：

| File:line | Class |
|-----------|--------|
| `vibe_pma_bnd.sv:49/:55` | **TOOL** — `prbs23_adv128` function body。与 `tmr_load` / `dec_lid` 同类：Verilator 5.020 对 automatic function 内联后 LINE 打不进。Icarus `tc_pma_512b_slice` 已打 idle 前进。 |
| `vibe_pma_bnd.sv:80 if` / `:80 else` | **TOOL** — 同一行 `end else if (afifo_pma_lane_vld)` / `else`。Verilator PMA cluster 本夜 FAIL（LFSR 不前进、vld 不切 lane；见上）。Icarus 两 TC **PASS**（idle PRBS + vld pack 都打到）。**不是 DUT 死代码。** |

未 chase LINE 100%。未改 TB / 未改 RTL。

## 功能覆盖 (functional)

无 `covergroup`。官方 **159** 个 TP-0.3 ID（`TP_TC_MATRIX.md`）：
MAPPED=106 ADDED=16 HOLE=9 NEG=28。

| Class | Count | Status |
|-------|------:|--------|
| MAPPED + ADDED | 122 | 121 在本夜 stock `sim-icarus` 跑过；**TP-PHY-012** = `tc_nw_pkt_pma_loopback` stock **compile_fail（TB）**。`/tmp` Icarus-safe force **PASS 100/100** — **DUT 未 miss** |
| NEG | 28 | hit |
| HOLE（freeze SPEC **非目标**） | 9 | **waiver / 非目标** |
| **合计** | **159** | **100% of published（DUT）**；stock 门少执行 1 个 MAPPED TC |

HOLE 九个（非目标，不重开计数）：TP-HOLE-G2, G3, G4, G5, G6, G8, G9, 010, 012.

UVM 矩阵列已改指向 `tb/vibe/uvm/pkg/*.svh`，但本夜 **未跑 xsim**；功能分仍按
Icarus 同名 TC / 官方 159 ID。TP-PHY-012 的 stock 缺口是 **TB compile**，
不是 DUT 功能洞，**不算新 1/3 / 不算 reopen consecutive green**。

## 断言覆盖 (assertion)

**N/A** — 本 TB / DUT 无 SVA `assert` / `cover property` / `covergroup`。
仓库里仅有英文注释 “async assert”，不是断言 bins。

## 与连续全绿的关系

| Item | Note |
|------|------|
| 旧 3/3（`2026-09-03` ×3，freeze `32a7f5e0`） | **VOID** |
| CR-B re-gate 3/3（`2026-09-08-crb-*of3`，freeze `1ed4d350`） | **VOID** |
| Lint ECO 3/3（`2026-09-08-lint-eco-*of3`，freeze `982ddd0a`） | **CLOSED**（PR68） |
| 2026-09-09 … 2026-09-16 闸后健康 | **不是 4/3**（PR70 / PR75 / PR80 / PR84 / PR86 / PR88 / PR92 / PR94） |
| 本次 | **闸后夜间健康检查**（`1ed3fbed` + 书面 freeze `982ddd0a` **BROKEN / H hold-pin**）— **不是 4/3** — **不是新 1/3** |
| 新系列？ | **否** — 不要重开 3 次计数。Decision H 未选，freeze **不** 重钉 |

`rtl/` / `tb/` / `include/` / SPEC / checkers 均未改。只新增本报告。无 sidecar。
Handoff: 验证 → 芯片开发PM（进仓后即时验）。
