# 闸后夜间健康检查 — 2026-09-27 (Asia/Shanghai)

**这是闸后夜间健康检查，不是新的连续全绿 / 不是 4/3 / 不是新的 1/3。**
不要记成 **4/3**，也不要重开 3 次计数，也不要把本夜算成新系列的 **1/3**。
**不是签核。**
**stock Icarus 连续全绿系列 STOPPED（Decision I）。** 2/3 **does not start**。
PR122 1/3 PASS 是历史记录，不是本夜。

**本夜是 Decision I rewrite 落地到 tip 之后的闸后健康快照**
（相对上一夜已合入健康 PR211 / `reports/regress/2026-09-25.md`：
**RTL/TB 未前进（main 仅多 PR211 报告）**。
PR214 已合入 main `2b5c5388`（checkout `14de8115` 不含该报告文件，RTL/TB 相同），
以 **main tip** 实测。checkout 仍含
stages **1–35** 叶子 + stage-36 `vibe_icrc` PR200 + TB-only leaf TCs
PR201/203/205/206 + docs/lint PR204/PR207 + 已合入健康 PR211），
**不是** 新系列，**不是** 4/3，**不是** 新 1/3。
RTL freeze 现为 **UNFROZEN**。历史 pin `302ac943` **VOID as current**。
本夜 **未** 重钉 freeze。**未** checkout 任何 open draft
（当前 open PR208 stage-37 `vibe_nw_adapt` / PR209 stage-35 `vibe_dll` wrap TC /
PR210 STATUS/RISKS evening 2026-09-24 / PR212 lint+CDC 2026-09-25 /
PR213 STATUS/RISKS evening 2026-09-25 /
PR215 lint+CDC 2026-09-26 **未** overlay）。

相对历史 DUT `302ac943`，tip 前进了 Decision I **产品叶子**
（`rtl/` **36 files**, **+255 / −10**）。多数是 GENERATED/HAND-FINISHED header
（CDC/PMA/PCS/DLL/fabric/NW 叶子 +6/+7 行）。若干 PCS 文件另有叶子落地行
（`vibe_rs128_120_dec.sv` +23/−1；`vibe_pcs_tx_fec.sv` +10；
`vibe_pcs_tx_pack.sv` +10；`vibe_pcs_rx_unpack.sv` +7/−4；
`vibe_pcs_rx_fec.sv` +6/−3；`vibe_pcs_tx_g1.sv` +6/−2）。
相对上一夜：**RTL/TB 未前进（main 仅多 PR211 报告）**，`rtl/` vs pin 仍是 36 files +255/−10。
**`git diff 302ac943...HEAD -- rtl/` NON-EMPTY（expected）；`-- include/` EMPTY。**

Lint ECO consecutive-green **3/3 已关闭**（`reports/regress/2026-09-08-lint-eco-1of3.md` /
`2026-09-08-lint-eco-2of3.md` / `2026-09-08-lint-eco-3of3.md`；PR68）。
PMA ECO 旧系列 **1/3 FAIL**（`reports/regress/2026-09-21-pma-eco-1of3.md`；PR114）
保持 **FAIL / CLOSED 历史保留**。本夜 **不** 把它续成 2/3，也 **不** 重开 1/3。
stock Icarus vs `302ac943` 新系列 **1/3 PASS**（`reports/regress/2026-09-22-pma-idle-1of3.md`；PR122）
现为 **historical / STOPPED**。本夜 **不是** 它的 2/3，也 **不是** 新 1/3。

旧系列 **VOID**，不要对照：
- `32a7f5e0` 夜间健康 / 2026-09-03 3/3（DUT 端口已改名）
- CR-B `1ed4d350` 系列（`2026-09-08-crb-*of3`；Lint ECO PR62 落地后作废）

基线对照：`reports/regress/2026-09-08-lint-eco-3of3.md`（Lint ECO 3/3 关闭记录）。
上一夜已合入闸后健康：`reports/regress/2026-09-25.md`（PR211 已合入 main `14de8115`；
checkout `2b4a8408` + 报告；**不是 4/3 / 不是 1/3**；primary = uvm-python）。
PR214 已合入 main `2b5c5388`（checkout `14de8115` 不含该报告文件，RTL/TB 相同）。更早已合入：
`reports/regress/2026-09-24.md`（PR202；`186dde4e`）。
本夜量的是 **main tip `14de8115`**（**RTL/TB 未前进（main 仅多 PR211 报告）**），
**不** 重开连续全绿。
相对 PR211 / PR214：**RTL/TB 相同，四门数字相同**（本夜仍实测，未抄旧数）。

聊天 PASS 不算验收。本文件是给芯片开发PM 的 in-repo 记录。
进仓后 PM 即时验。Handoff: 验证 → 芯片开发PM 验收。

| Item | Value |
|------|--------|
| Label | **闸后夜间健康检查**（**不是** 新系列 1/3 / **不是** 4/3 / **不是** 签核 / stock Icarus 系列 **STOPPED**） |
| Checkout tip SHA | `14de8115a876cff0facf0c28e83503eaa3bdd122`（PR211 合入后；`git rev-parse HEAD`） |
| 产品叶子 SHA | `2b4a8408abba14e3fcd0208fadd2c54cc97c3c23`（PR200 Decision I stage-36 `vibe_icrc`；相对 tip 仅多 `reports/regress/2026-09-25.md`） |
| 相对上一夜 | **RTL/TB 未前进（main 仅多 PR211 报告）**。PR214 已合入 main `2b5c5388`（checkout `14de8115` 不含该报告文件，RTL/TB 相同） |
| Checkout vs main tip | checkout `14de8115`；main tip `2b5c5388`（PR214 合入）；两者只差 `reports/regress/2026-09-26.md` 一个 reports 文件，RTL/TB 相同，按 PM 规则不重跑 |
| RTL freeze | **UNFROZEN**（Decision I）。无 live pin。历史 pin `302ac943c3737c288f3af6c4857bb3a9b1683a26` **VOID as current**。本夜 **未** 重钉 |
| Decision I on tip | stages **1–34** 产品叶子 + stage-35 `vibe_dll` wrap（PR198）+ stage-36 `vibe_icrc`（PR200）+ TB-only leaf TCs PR201/203/205/206 + docs PR199/PR207 + lint PR204 + 已合入健康 PR202/PR211 |
| `git diff 302ac943...HEAD -- rtl/` | **NON-EMPTY** — 36 files, +255/−10（Decision I product leaves；expected） |
| `git diff 302ac943...HEAD -- include/` | **EMPTY** |
| `rtl/` / `include/` / SPEC / TB 本夜 | **未改**。无 overlay。零 TB / rtl 源码编辑。只新增本报告 |
| xsim / xvlog | **不可用** — `xvlog`/`xelab`/`xsim` 不在 PATH，无 Vivado Simulator license。记 **non-DUT**（缺工具） |
| **Primary 功能门** | `make -C tb/vibe sim`（uvm-python / PR119；默认 `SIM=verilator`）— **本夜实测**（2026-09-27 01:58–02:19 CST） |
| Primary complete path | `make -C tb/vibe sim SIM=icarus`（PR119：Icarus 是完整 no-xvlog uvm-python 门；port/top 在 Verilator Force 下不计分）— **本夜实测**（2026-09-27 02:19–02:24 CST） |
| **Secondary / compare** | `make -C tb/vibe sim-icarus`（Icarus Verilog **12.0**；历史 124-bucket）— **本夜实测**（2026-09-27 02:24–02:26 CST）。**不** 计入连续全绿 |
| Cov | `make -C tb/vibe cov`（Verilator **5.020**）— **本夜实测**（2026-09-27 02:26–02:31 CST） |
| `SIM_EXIT`（primary `make sim` / Verilator） | **2** — 4 TOOL fail（port/top LMSM Force；见失败列表）。**不是 DUT 红** |
| `SIM_UVM_ICARUS_EXIT` | **0** — uvm-python complete path 0 fail |
| `SIM_ICARUS_EXIT`（stock） | **0** |
| `XSIM_MAKE_EXIT` | **n/a**（`xvlog` 不在 PATH；本夜不跑 `sim-xsim`） |
| `COV_EXIT` | **0** |
| 通过率（primary complete = uvm-python Icarus） | **0 fail**（`TOTAL_PASS_LINES=189` / `TOTAL_FAIL_LINES=0`） |
| 通过率（secondary stock Icarus） | **124/124 buckets = 100%**（suite 27 + units 85 + top 1 + neg 11；stock-only `TOTAL_PASS_LINES=158`） |
| Result | **PASS** — 排除 TB/TOOL 后无 DUT 红；LINE **725/760 = 95.4% ≥ 95% + waiver**；功能 published **159/159**；断言 **N/A** |
| `ovf_l` | F1 永久冻结 CDC-WARN — 本夜不为此开 issue |
| 新 GitHub issue | **无**（open issues = 0；4 Verilator fail 已排除为 TOOL） |
| 本报告 PR | **https://github.com/lukebest/Vibe-UB-Switch/pull/216** — reports-only；Leave open for 芯片开发PM |

## Pin confirm

- Checkout: tip `14de8115a876cff0facf0c28e83503eaa3bdd122`
  （Merge PR211 闸后夜间健康 2026-09-25）。
  `git rev-parse HEAD` = 该值。产品叶子仍是
  `2b4a8408abba14e3fcd0208fadd2c54cc97c3c23`
  （PR200 Decision I stage-36 `vibe_icrc` pyCircuit leaf；
  `git diff 2b4a8408...HEAD -- rtl/ tb/` **EMPTY**）。
  `git log -5 --oneline` 起于
  `14de811` (PR211) / `2b4a840` (PR200) / `684b58c` (PR206 stage-34 `vibe_dll_tx` TC) /
  `f94b0ed` (PR207 STATUS/RISKS morning) / `af28045` (PR204 lint+CDC)。
- 自上一夜已合入健康 PR211（同一产品叶子 `2b4a8408`）起 **RTL/TB 未前进（main 仅多 PR211 报告）**。
  PR214 已合入 main `2b5c5388`（checkout `14de8115` 不含该报告文件，RTL/TB 相同）。
  自已合入健康 PR202 (`186dde4e`, stages 1–35 + PR199) 起 tip 仍含 Decision I
  stages **1–35** 叶子 + stage-36 `vibe_icrc` PR200 + TB-only leaf TCs
  PR201 (`vibe_voq_egr`) / PR203 (`vibe_saf_ing`) / PR205 (`vibe_xbar`) /
  PR206 (`vibe_dll_tx`) + docs/lint PR204/PR207。
  **无** `tc_vibe_icrc` 叶子 TC（stage-36 是 RTL/pyCircuit 叶子；ICRC 仍由
  `tc_icrc_txrx_vs_transit` 覆盖）。
- Freeze: **UNFROZEN**。历史 pin `302ac943c3737c288f3af6c4857bb3a9b1683a26`
  **VOID as current**。**Do not re-pin freeze in this reports PR.**
- `git diff 302ac943c3737c288f3af6c4857bb3a9b1683a26...HEAD -- rtl/` **NON-EMPTY**：
  36 files, +255/−10。Decision I 产品叶子（expected）。与 PR211 / PR214 夜 **相同**。
- `git diff 302ac943c3737c288f3af6c4857bb3a9b1683a26...HEAD -- include/` **EMPTY**。
- F1 `ovf_l` untouched（Decision F1 stands）。
- `rtl/` 未搬家。本夜未改 `rtl/` / `tb/` / `include/` / SPEC / checkers。只新增本报告。
- Decision I 叶子 TCs（`tc_vibe_afifo` / `tc_afifo_afull10` /
  `tc_vibe_sync2` / `tc_vibe_rst_sync` / gears / PCS / DLL / fabric 叶子 /
  `tc_pma_512b_slice` / `tc_pma_922mhz` / `tc_vibe_port_sel` /
  `tc_vibe_voq_egr` / `tc_vibe_saf_ing` / `tc_vibe_xbar` / `tc_vibe_dll_tx`）本夜在
  uvm-python Verilator + Icarus + stock Icarus **PASS**
  （port/top 四条只在 Verilator Force 上 FAIL，Icarus 两侧 PASS）。
  **不要** 把叶子 PASS 当成全片 consecutive-green。

## 本夜命令

**tip 已两改：** 默认 `sim` 先指到 xsim+UVM，再被 PR119 指到 **uvm-python**。
Decision I 之后 **primary** = `make -C tb/vibe sim`（uvm-python）。
README / PR119：`SIM=verilator` 是 fabric/unit baseline；port/top 的 LMSM
`Force` 在 Verilator 5.020 + cocotb 1.9.2 VPI 下不生效，完整 no-xvlog 门是
`SIM=icarus`。stock Icarus `sim-icarus` 是 **secondary/compare**，
**不是** consecutive-green。`cov` 仍是 Verilator，不依赖 xsim。

本夜实际跑过：

```bash
# 0) checkout tip only（无 overlay / 无 draft）
git fetch origin && git checkout main && git pull --ff-only
git rev-parse HEAD
# → 14de8115a876cff0facf0c28e83503eaa3bdd122
git diff 302ac943...HEAD -- rtl/     # NON-EMPTY 36 files +255/−10
git diff 302ac943...HEAD -- include/ # EMPTY

# 1) 记录 xvlog / xsim 缺工具（non-DUT）
command -v xvlog xelab xsim
# → not on PATH. XSIM miss = non-DUT（与 2026-09-16 … 2026-09-26 相同工具债）

# 2) Primary：默认 make sim = uvm-python SIM=verilator
make -C tb/vibe sim
# → SIM_EXIT=2
#    SUITE 28/28 PASS（tc_suite_all）
#    leaf units + static/neg + PCS + Decision I leaves PASS
#    FAIL tc_port_smoke / tc_nw_pkt_to_pma_tx / tc_nw_pkt_pma_loopback / tc_top_smoke
#    TOTAL_PASS_LINES=184 TOTAL_FAIL_LINES=4
#    失败原因：LMSM/DLL did not reach ACTIVE（Force am_locked）；top: u_peer 无 u_lmsm
#    = PR119 已记录的 Verilator TOOL，不是 DUT

# 3) Primary complete path（不静默替换 primary；只用来排除 TOOL）
make -C tb/vibe sim SIM=icarus
# → SIM_UVM_ICARUS_EXIT=0
#    TOTAL_PASS_LINES=189 TOTAL_FAIL_LINES=0
#    tc_port_smoke PASS；tc_nw_pkt_pma_loopback PASS 100/100；tc_top_smoke PASS
#    tc_vibe_afifo / tc_pma_512b_slice / tc_pma_922mhz / Decision I leaves PASS
#    含叶子 tc_vibe_voq_egr / tc_vibe_saf_ing / tc_vibe_xbar / tc_vibe_dll_tx PASS

# 4) Secondary / compare：历史 124-bucket stock Icarus（≠ consecutive-green）
make -C tb/vibe sim-icarus
# → SIM_ICARUS_EXIT=0
#    SUITE 27/27 ; UNITS pass_files=85 fail_files=0 compile_fail=0
#    PASS tc_top_smoke ; NEG 11/11
#    stock-only TOTAL_PASS_LINES=158 TOTAL_FAIL_LINES=0
#    （混有 pyuvm leftover 时 raw summarize=261；口径与 PR211 / PR214 / PR202 同：排除 leftover）
#    tc_nw_pkt_pma_loopback PASS
#      tx_n=100 rx_n=100 am_lock_end=1111 saw_fec_fail=0 saw_deskew=1
#    tc_port_smoke PASS ; tc_pma_512b_slice / tc_pma_922mhz PASS

# 5) 覆盖率
make -C tb/vibe cov
# → COV_EXIT=0 ; LINE 725/760 = 95.4% ; TOGGLE 9892/18391 = 53.8%
#    clusters=58（rsdec 进 merge；port/top OOM skip）
```

工具：Icarus Verilog **12.0 (stable)**（`12.0-2build2`）；
Verilator **5.020 2024-01-01**（Debian 5.020-1）；
cocotb **1.9.2** + uvm-python **0.4.0**（`lukebest/uvm-python`；`make -C tb/vibe/pyuvm venv`）。
未改 Makefile / 未加 overlay / 未 chase xsim / 未改 TB / 未改 RTL。
xsim/`xvlog` 缺工具 = **non-DUT fail**。

## 通过率

### Primary = uvm-python（`make -C tb/vibe sim`）

默认 `SIM=verilator`（Makefile `SIM ?= verilator`）。PR119：port/top **not scored**
on Verilator Force。完整 no-xvlog 门 = `SIM=icarus`。

| Path | Exit | summarize | 说明 |
|------|-----:|-----------|------|
| `make sim`（Verilator default） | **2** | PASS 184 / FAIL 4 | suite 28/28 + units PASS；port×3 + top×1 = TOOL Force |
| `make sim SIM=icarus`（complete） | **0** | PASS 189 / FAIL 0 | 同上 4 条 **PASS**（loopback **100/100**） |

相对 PR211 / PR214（PASS 184 / FAIL 4 与 complete 189/0）：**相同**
（**RTL/TB 未前进（main 仅多 PR211 报告）**；本夜实测，未抄旧数）。
相对已合入 PR202（PASS 180 / FAIL 4 与 complete 185/0）：仍多 4 条 Decision I
叶子 TCs（`tc_vibe_voq_egr` / `tc_vibe_saf_ing` / `tc_vibe_xbar` /
`tc_vibe_dll_tx`；PR201/203/205/206）。
**不是** 新 1/3 计数。

Verilator 默认路径 **exit=2** 记入本夜，**不** 静默改 primary 命令。
排除 TB/TOOL 后：**无 DUT 红**。验收口径用 Icarus complete path（PR119 表）。

### Secondary = stock Icarus（`make -C tb/vibe sim-icarus`）

历史 124-bucket / 与 PR114 / PR121 / PR122 / PR131 / PR202 / PR211 / PR214 同口径。**不** 计入连续全绿。

| Bucket | pass | fail | compile_fail | 通过率 |
|--------|------|------|----------------|--------|
| suite (`Makefile.icarus` suite） | **27** | 0 | 0 | **27/27 = 100%** |
| units (`run_units.sh`) | **85** | 0 | 0 | **85/85 = 100%** |
| top (`tc_top_smoke`) | **1** | 0 | 0 | **1/1 = 100%** |
| neg (`scan_absent.sh`) | **11** | 0 | 0 | **11/11 = 100%** |
| **合计（文件/桶）** | **124** | **0** | **0** | **124/124 = 100%** |
| summarize（stock-only） | `TOTAL_PASS_LINES=158` | `TOTAL_FAIL_LINES=0` | — | 0 fail |

`SUITE pass=27 fail=0 ran=27` / `SUITE_RESULT PASS`.
`UNITS pass_files=85 fail_files=0 compile_fail=0`.
`PASS tc_top_smoke`.

（`TOTAL_PASS_LINES=158` 与 2026-09-26 / 2026-09-25 / 2026-09-24 / 2026-09-23 / 2026-09-22 / 2026-09-21 闸后健康对齐：
`summarize.sh` 对 stock `results/*.log` 的 `^PASS ` 行；排除 `cov.log` 与
pyuvm leftover。本夜 raw `results/` 混 leftover 时为 261；stock-only = 158。）

stock Icarus 本夜是 **compare only**。不要把它续成 2/3 / 4/3 / 新 1/3。

## 失败列表

**DUT 失败列表：空。** 本夜排除 TB/TOOL 后 0 FAIL / 0 compile_fail。
无波形需归档，无新 GitHub issue。

先排除 TB / 工具：

- 默认 `make -C tb/vibe sim`（`SIM=verilator`）**exit=2**，4 条 FAIL：
  `tc_port_smoke` / `tc_nw_pkt_to_pma_tx` / `tc_nw_pkt_pma_loopback` /
  `tc_top_smoke`。
  现象：`LMSM/DLL did not reach ACTIVE/NRM`（stimulus = `lmsm_go` +
  `Force am_locked=1111`）；`tc_nw_pkt_to_pma_tx` / `tc_nw_pkt_pma_loopback`
  为 `stimulus : link bring-up` / `actual : 0`；top 另有
  `AttributeError: u_peer contains no object named u_lmsm`。
  **TOOL** — PR119 / `tb/vibe/pyuvm/README.md` 已写明：Verilator 5.020 +
  cocotb 1.9.2 VPI 下 hierarchical `Force` 不进 ACTIVE；port/top **not scored**。
  同 4 条在 `SIM=icarus` **PASS**（loopback **100/100**），stock Icarus 亦 **PASS**。
  **不是 DUT mismatch。不因此开 issue。**
- `xvlog` 仍不在 PATH — 记 **缺工具 / non-DUT**（与 2026-09-16 … 2026-09-26 相同）。
- stock `compile_fail=0`。本夜未改 `tb/`。
- Decision I 叶子：`tc_vibe_afifo` / `tc_afifo_afull10` / `tc_vibe_sync2` /
  `tc_vibe_rst_sync` / gears / PCS / DLL / fabric 叶子 /
  `tc_pma_512b_slice` / `tc_pma_922mhz` / `tc_vibe_port_sel` /
  `tc_vibe_voq_egr` / `tc_vibe_saf_ing` / `tc_vibe_xbar` / `tc_vibe_dll_tx`
  在 uvm-python Verilator、uvm-python Icarus、stock Icarus **均 PASS**。
  叶子 ≠ 全片门。无 `tc_vibe_icrc`（stage-36 RTL 叶子；`tc_icrc_txrx_vs_transit` **PASS**）。
- stock `tc_nw_pkt_pma_loopback` **PASS**，
  `tx_n=100 rx_n=100`，`am_lock_end=1111`，`saw_fec_fail=0`，`saw_deskew=1`。
  `tc_port_smoke` **PASS**。`tc_top_smoke` **PASS**。
  uvm-python Icarus loopback **PASS 100/100**。

结论：**本夜无新 TB / DUT 功能红。**
Verilator port/top Force = **已知 TOOL**（PR119），不是新 DUT。
xsim/`xvlog` 缺工具 **不是** DUT mismatch。
F1 `ovf_l` 仍为永久冻结 WARN，本夜不为此开 issue。
不 ECO rtl。不重钉 freeze。不重开 1/3。不续 2/3。

Verilator coverage clusters still skip `vibe_suite` (VOQ OOM；脚本从不 bind) and `port`/`top`
(`NOTE cov: cluster … compile rc=1 (timeout/OOM skipped, not waived)`)。
本夜 `rsdec` **进 merge**（58 clusters；与 2026-09-26 / 2026-09-25 / 2026-09-24 / 2026-09-23 / PR114 相同）。
Icarus `tc_rs_dec_syndrome.log` 本夜 **PASS**。cov 里 Verilator-only
`FAIL tc_rs_dec_syndrome` 与历史夜间相同 **TOOL/timing**。

Verilator-only `FAIL` lines on `tc_lmsm_walk` / `tc_pcs_fec_dual_enc` /
`tc_pcs_fec_t2` / `tc_route_lu` / `tc_rs_dec_syndrome` during `make cov`
是与 Lint ECO 1/3–3/3 及后续闸后健康相同的 tool/timing limits；
那些 TC **PASS** on Icarus。

**PRBS23 / idle-mark 后 cov-cluster Verilator-only FAIL（功能门 PASS，不算功能红）：**
`tc_pma_512b_slice` / `tc_pma_922mhz` — Verilator PMA **cov cluster** 仍停
（LFSR 不前进 / `vld` 包不切到 lane 字）。本夜 **uvm-python Verilator**
（cocotb `make sim`）这两条 **PASS**；Icarus 对应 `.log` 均为 `PASS`。
**TOOL**（cov custom C++ main ≠ cocotb）。功能分按 Icarus（uvm-python complete + stock compare）。

## 代码覆盖 (code)

Tool: Verilator **5.020** `--coverage-line --coverage-toggle`。合并 **58** 个
cluster `.dat`（与 2026-09-26 / 2026-09-25 / 2026-09-24 / 2026-09-23 / PR114 相同：`rsdec` 进 merge）。`vibe_suite` 未 bind
（VOQ OOM）。port/top compile rc=1（OOM skipped, not waived）— 0 records。
`vibe_rs128_120_dec.sv` 本夜 **12/12 LINE**（与 PR211 / PR214 / PR202 相同；相对 PR131 的 13/13：Decision I
stage-12 叶子改写 LINE bin，不是漏测）。
`vibe_afifo.sv` 本夜 **11/11 LINE**（Decision I stage-1 叶子；header 不加 LINE bin）。
`vibe_icrc.sv` 本夜 **12/12 LINE**（stage-36 +6 header 不加 LINE bin）。

LINE **725/760 = 95.4%** ≥ 95%。不追 LINE 100%。
相对 PR211 / PR214（LINE 725/760）：hit/tot **相同**（**RTL/TB 未前进（main 仅多 PR211 报告）**）。TOGGLE **9892/18391 = 53.8%** — 与 2026-09-26 / 2026-09-25 / 2026-09-24 相同。
`vibe_pma_bnd.sv` 本夜 **16/21 = 76.2%**（与 PR211 / PR214 / PR202 / PR131 / PR121 / PR114 相同命中/分母）。
`cov_report.py` 文案里残留的 `708/737 = 96.1%` 是历史硬编码，**不是** 本夜实测；
本夜以脚本打印的 `LINE 725/760 = 95.4%` 为准。

| Metric | Hit/tot | % | Gate |
|--------|--------:|--:|------|
| **LINE** | **725/760** | **95.4** | **≥95% + waiver — PASS** |
| **TOGGLE** | **9892/18391** | **53.8** | 无 80% 门槛；wide-bus unused bits + PRBS23 / idle-mark 宽总线 |
| **FSM** | n/a | — | 无 VCS FSM engine；LINE 打在 state `case`/`if` |

不追 LINE 100%。

Waiver（点名，与本夜 `cov_report.md` uncovered LINE + `COVERAGE_HOLES.md` 一致，未改分类；
行号随 Decision I header / 叶子下移）：

| File:line | Class |
|-----------|--------|
| `vibe_dll_tx.sv:101/:147` | **WAIVER / 防御**（PR131 记 `:98/:144`；stage-34 header 后下移） |
| `vibe_cfg_space.sv:96–99` | **WAIVER** — RW1C `hold_fall` HW-clear。设计 agreed，**不改** `rtl/` |
| `vibe_lmsm.sv:101–108` | **TOOL** (`tmr_load`) |
| `dec_lid` / dll_tx pad combo (`:154/:158/:162`) | **TOOL** |
| pcs rem/pend / g1 `:52` | **TB空洞 — WAIVE** |
| `vibe_xbar.sv:53 else` | **TB空洞 — WAIVE** — ECO lock-hold implicit else。Not a DUT fail。 |

本夜 uncovered LINE 与 Lint ECO 3/3 / PR114 / PR121 / PR131 / PR202 / PR211 / PR214 同类：`cfg_space` hold_fall `:96–99`、`dll_tx`
`:101/:147/:154/:158/:162`、`lmsm` `:101–108`、`pcs_rx` rem/pend、
`pcs_rx_amctl_lock` `:62–65`、`pcs_tx_g1` `:52`，plus ECO
`vibe_xbar.sv:53 else`（TB hole, waived）。未改 waiver 分类，未改 RTL。

### Decision I / idle-mark 后空洞（诚实分类；不追 LINE 100%）

`vibe_pma_bnd.sv` 本夜 **16/21 = 76.2%**。本夜 `cov_report.md` uncovered LINE
（行号相对历史 pin 随 Decision I **+7 header**；同类点，无新类别）：

| File:line | Class |
|-----------|--------|
| `vibe_pma_bnd.sv:58/:64` | **TOOL** — `prbs23_word` / `prbs23_adv128` function body。与 `tmr_load` / `dec_lid` 同类：Verilator 5.020 对 automatic function 内联后 LINE 打不进。Icarus `tc_pma_512b_slice` 已打 idle 前进。 |
| `vibe_pma_bnd.sv:103 if` / `:103 else` | **TOOL** — `end else if (afifo_pma_lane_vld)` / idle fill。Verilator PMA **cov cluster** 本夜 FAIL。Icarus 两 TC **PASS**；uvm-python Verilator cocotb 本夜亦 **PASS**。**不是 DUT 死代码。** |
| `vibe_pma_bnd.sv:133 else` | **TOOL** — RX 非复位支路。同一 Verilator PMA cov FAIL。Icarus loopback **PASS 100/100**。 |

未 chase LINE 100%。未改 TB / 未改 RTL。无本夜新增空洞**类别**（仍是 TOOL / 已有 waiver）。
不因此开 issue。

## 功能覆盖 (functional)

无 `covergroup`。官方 **159** 个 TP-0.3 ID（`TP_TC_MATRIX.md`）：
MAPPED=106 ADDED=16 HOLE=9 NEG=28。打分 TC 均在本夜 stock `sim-icarus`
**以及** uvm-python Icarus complete path 中跑过。

| Class | Count | Status |
|-------|------:|--------|
| MAPPED + ADDED | 122 | hit（含 **TP-PHY-001** = `tc_port_smoke` stock + uvm-python Icarus **PASS**；**TP-PHY-012** = `tc_nw_pkt_pma_loopback` stock + uvm-python Icarus **PASS 100/100**） |
| NEG | 28 | hit |
| HOLE（freeze SPEC **非目标**） | 9 | **waiver / 非目标** |
| **合计** | **159** | **100% of published** |

HOLE 九个（非目标，不重开计数）：TP-HOLE-G2, G3, G4, G5, G6, G8, G9, 010, 012.

UVM / pyuvm 矩阵列本夜 **已跑**（primary）。功能分：Icarus 同名 TC / 官方 159 ID
全 hit。Verilator 默认路径上 TP-PHY-001 / TP-PHY-012 **not scored**（Force TOOL），
Icarus 两侧 **PASS** — 不算 published miss。不算新 1/3 / 不算 reopen consecutive green。

## 断言覆盖 (assertion)

**N/A** — 本 TB / DUT 无 SVA `assert` / `cover property` / `covergroup`。
仓库里仅有英文注释 “async assert”，不是断言 bins。

## 与连续全绿的关系

| Item | Note |
|------|------|
| 旧 3/3（`2026-09-03` ×3，freeze `32a7f5e0`） | **VOID** |
| CR-B re-gate 3/3（`2026-09-08-crb-*of3`，freeze `1ed4d350`） | **VOID** |
| Lint ECO 3/3（`2026-09-08-lint-eco-*of3`，freeze `982ddd0a`） | **CLOSED**（PR68） |
| 2026-09-09 … 2026-09-26 闸后健康 | **不是 4/3 / 不是 1/3**（PR70 / PR75 / PR80 / PR84 / PR86 / PR88 / PR92 / PR94 / PR97 / PR101 / PR105 / PR107 / PR110 / PR121 / PR131 / PR202 / PR211 / PR214） |
| PMA ECO 旧系列 1/3 | **FAIL**（PR114；DUT `a658d141`；121/124；3 DUT fail）— **CLOSED 历史保留**。本夜 **不是** 它的 2/3 |
| stock Icarus vs `302ac943` 新系列 1/3 | **PASS / historical**（PR122）— Decision I **STOPPED**。2/3 **does not start**。本夜 **不是** 它的 2/3，也 **不是** 新 1/3 |
| 本次 | **闸后夜间健康检查**（tip `14de8115` + freeze **UNFROZEN** + Decision I stages 1–36；primary = uvm-python；stock Icarus = compare；**RTL/TB 未前进（main 仅多 PR211 报告）**；PR214 已合入 main `2b5c5388`（checkout `14de8115` 不含该报告文件，RTL/TB 相同））— **不是 4/3** — **不是新 1/3** — **不是签核** |
| 新系列？ | **否** — 不要重开 3 次计数。freeze 未重钉。stock Icarus 系列 **STOPPED** |

`rtl/` / `tb/` / `include/` / SPEC / checkers 均未改。只新增本报告。无 sidecar。
本报告 PR：**https://github.com/lukebest/Vibe-UB-Switch/pull/216**（reports-only）。
Handoff: 验证 → 芯片开发PM（进仓后即时验）。Leave merge for PM.
