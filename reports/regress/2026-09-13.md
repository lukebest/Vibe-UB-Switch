# 闸后夜间健康检查 — 2026-09-13 (Asia/Shanghai)

**这是闸后夜间健康检查，不是新的连续全绿 / 不是 4/3。**
不要记成 **4/3**，也不要重开 3 次计数。

Lint ECO consecutive-green **3/3 已关闭**（`reports/regress/2026-09-08-lint-eco-1of3.md` /
`2026-09-08-lint-eco-2of3.md` / `2026-09-08-lint-eco-3of3.md`；PR68）。
DUT freeze 仍为 `982ddd0a`，该计数保持关闭。本文件只做闸后健康快照。

旧系列 **VOID**，不要对照：
- `32a7f5e0` 夜间健康 / 2026-09-03 3/3（DUT 端口已改名）
- CR-B `1ed4d350` 系列（`2026-09-08-crb-*of3`；Lint ECO PR62 落地后作废）

基线对照：`reports/regress/2026-09-08-lint-eco-3of3.md`（Lint ECO 3/3 关闭记录）。
上一夜闸后健康：`reports/regress/2026-09-12.md`（PR84；**也不是 4/3**）。

聊天 PASS 不算验收。本文件是给芯片开发PM 的 in-repo 记录。
进仓后 PM 即时验。

| Item | Value |
|------|--------|
| `origin/main` SHA | `b1aa413ee8ff76af28ede26c12637c953b61ea8c`（PR85，nightly lint+CDC reports-only；SPEC 仍冻结） |
| DUT RTL SHA | `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6`（Lint ECO freeze；与 1/3–3/3 相同） |
| `git diff` `rtl/` vs `982ddd0a` | **empty** |
| `git diff` `include/` vs `982ddd0a` | **empty** |
| `rtl/` / `tb/` / `include/` / SPEC 本夜 | **未改**。相对本夜 checkout 的 `origin/main` `b1aa413e` 无 TB/RTL/SPEC 源码变更。无 overlay，不重写 TB。无 `scripts/regress.sh`；走 `make -C tb/vibe sim` / `cov` |
| TB | main 已有 `5ee96f9a66f771aba45c4dcb2e5a146fc9c54b5e`（PR73 waves dump）。本次 **不改** |
| Sim | `make -C tb/vibe sim`（Icarus Verilog **12.0**）— **本夜实测**（2026-09-13 02:11–02:13 CST） |
| Cov | `make -C tb/vibe cov`（Verilator **5.020**）— **本夜实测**（2026-09-13 02:13–02:18 CST） |
| `SIM_EXIT` | **0** |
| `COV_EXIT` | **0** |
| 通过率 | **124/124 buckets = 100%**（suite 27 + units 85 + top 1 + neg 11；0 fail） |
| Result | **PASS** — LINE **715/745 = 96.0% ≥ 95% + waiver**；功能 **159/159**；断言 **N/A** |
| `ovf_l` | F1 永久冻结 CDC-WARN — 本夜不为此开 issue |

## Pin confirm

- Checkout: `origin/main` `b1aa413ee8ff76af28ede26c12637c953b61ea8c`（PR85 reports-only）。
- `git diff 982ddd0a -- rtl/` **empty**。`git diff 982ddd0a -- include/` **empty**。
- F1 `ovf_l` untouched（Decision F1 stands）。
- `rtl/` 未搬家。本夜未改 `rtl/` / `tb/` / `include/` / SPEC / checkers。

## 通过率

| Bucket | pass | fail | compile_fail | 通过率 |
|--------|------|------|----------------|--------|
| suite (`make suite`) | **27** | 0 | 0 | **27/27 = 100%** |
| units (`make units`) | **85** | 0 | 0 | **85/85 = 100%** |
| top (`make top`) | **1** (`tc_top_smoke`) | 0 | 0 | **1/1 = 100%** |
| neg (`make neg`) | **11** | 0 | 0 | **11/11 = 100%** |
| **合计（文件/桶）** | **124** | **0** | **0** | **124/124 = 100%** |
| summarize | `TOTAL_PASS_LINES=158` | `TOTAL_FAIL_LINES=0` | — | 0 fail |

`SUITE pass=27 fail=0 ran=27` / `SUITE_RESULT PASS`.
`UNITS pass_files=85 fail_files=0 compile_fail=0`.
`PASS tc_top_smoke`.

（`TOTAL_PASS_LINES` 是本夜 `summarize.sh` 对 `results/*.log` 的 `^PASS ` 行计数，
含多 PASS 的 unit log；`summarize.sh` 排除 `cov.log`。不是从 3/3 或 2026-09-12 抄的。）

Icarus is the functional gate.

## 失败列表

**空。** 本夜 Icarus 门 0 FAIL / 0 compile_fail。无失败 TC，无波形，无 GitHub issue。
F1 `ovf_l` 仍为永久冻结 WARN（Luke 决策），本夜不为此开 issue。

Verilator coverage clusters still skip `vibe_suite` (VOQ OOM) and `port`/`top`
(`NOTE cov: cluster … compile rc=1 (timeout/OOM skipped, not waived)`)。
Verilator-only `FAIL` lines on `tc_lmsm_walk` / `tc_pcs_fec_dual_enc` /
`tc_pcs_fec_t2` / `tc_rs_dec_syndrome` / `tc_route_lu` during `make cov` are
the same tool/timing limits as Lint ECO 1/3–3/3；those TCs **PASS** on Icarus
（`85/85`；本夜 `tc_lmsm_walk.log` / `tc_pcs_fec_dual_enc.log` /
`tc_pcs_fec_t2.log` / `tc_rs_dec_syndrome.log` / `tc_route_lu.log` 均为 `PASS`）。
Icarus is the functional gate.

## 代码覆盖 (code)

Tool: Verilator **5.020** `--coverage-line --coverage-toggle`。合并 **58** 个
cluster `.dat`。`vibe_suite` 未 bind（VOQ OOM）。port/top compile rc=1
（OOM skipped, not waived）— 0 records。

LINE matches Lint ECO 3/3（`715/745 = 96.0%`）。分母相对 voided `32a7f5e0` /
`708/737` 是 ECO combo defaults / grant-accept split / SAF combo
（`vibe_xbar` 34/35, `vibe_fabric` 50/50, `vibe_saf_ing` 17/17）。

| Metric | Hit/tot | % | Gate |
|--------|--------:|--:|------|
| **LINE** | **715/745** | **96.0** | **≥95% + waiver — PASS** |
| **TOGGLE** | **9537/17272** | **55.2** | 无 80% 门槛；wide-bus unused bits |
| **FSM** | n/a | — | 无 VCS FSM engine；LINE 打在 state `case`/`if` |

不追 LINE 100%。

Waiver（点名，与本夜 `cov_report.md` uncovered LINE + `COVERAGE_HOLES.md` 一致，未改分类）：

| File:line | Class |
|-----------|--------|
| `vibe_dll_tx.sv:98/:144` | **WAIVER / 防御** |
| `vibe_cfg_space.sv:96–99` | **WAIVER** — RW1C `hold_fall` HW-clear。设计 agreed，**不改** `rtl/` |
| `vibe_lmsm.sv:101–108` | **TOOL** (`tmr_load`) |
| `dec_lid` / dll_tx pad combo | **TOOL** |
| pcs rem/pend / g1 `:51` | **TB空洞 — WAIVE** |
| `vibe_xbar.sv:50 else` | **TB空洞 — WAIVE** — ECO lock-hold implicit else（`locked && !matching in_vld/dst`）。与 Lint ECO 1/3–3/3 相同。Not a DUT fail。 |

本夜 uncovered LINE 与 Lint ECO 3/3 同类：`cfg_space` hold_fall `:96–99`、`dll_tx`
`:98/:144/:151/:155/:159`、`lmsm` `:101–108`、`pcs_rx` rem/pend、
`pcs_rx_amctl_lock` `:55–58`、`pcs_tx_g1` `:51`，plus ECO
`vibe_xbar.sv:50 else`（TB hole, waived）。未改 waiver 分类，未改 RTL。无新增 uncovered LINE。

## 功能覆盖 (functional)

无 `covergroup`。官方 **159** 个 TP-0.3 ID（`TP_TC_MATRIX.md`）：
MAPPED=106 ADDED=16 HOLE=9 NEG=28。打分 TC 均在本夜 `make sim` 中跑过。

| Class | Count | Status |
|-------|------:|--------|
| MAPPED + ADDED | 122 | hit |
| NEG | 28 | hit |
| HOLE（freeze SPEC **非目标**） | 9 | **waiver / 非目标** |
| **合计** | **159** | **100% of published** |

HOLE 九个（非目标，不重开计数）：TP-HOLE-G2, G3, G4, G5, G6, G8, G9, 010, 012.

## 断言覆盖 (assertion)

**N/A** — 本 TB / DUT 无 SVA `assert` / `cover property` / `covergroup`。
仓库里仅有英文注释 “async assert”，不是断言 bins。

## 与连续全绿的关系

| Item | Note |
|------|------|
| 旧 3/3（`2026-09-03` ×3，freeze `32a7f5e0`） | **VOID** |
| CR-B re-gate 3/3（`2026-09-08-crb-*of3`，freeze `1ed4d350`） | **VOID** |
| Lint ECO 3/3（`2026-09-08-lint-eco-*of3`，freeze `982ddd0a`） | **CLOSED**（PR68） |
| 2026-09-09 闸后健康（`0c33e0d` + DUT `982ddd0a`） | **不是 4/3**（PR70） |
| 2026-09-10 闸后健康（`f3b26d9` + DUT `982ddd0a`） | **不是 4/3**（PR75） |
| 2026-09-11 闸后健康（`2a49f8c` + DUT `982ddd0a`） | **不是 4/3**（PR80） |
| 2026-09-12 闸后健康（`7bc8d793` + DUT `982ddd0a`） | **不是 4/3**（PR84） |
| 本次 | **闸后夜间健康检查**（`b1aa413e` + DUT `982ddd0a`）— **不是 4/3** |
| 新系列？ | **否** — 不要重开 3 次计数 |

`rtl/` / `tb/` / `include/` / SPEC / checkers 均未改。只新增本报告。无 sidecar。
Handoff: 验证 → 芯片开发PM（进仓后即时验）。
