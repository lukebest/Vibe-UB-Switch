# 闸后夜间健康检查 — 2026-09-20 (Asia/Shanghai)

**这是闸后夜间健康检查，不是新的连续全绿 / 不是 4/3 / 不是新的 1/3。**
不要记成 **4/3**，也不要重开 3 次计数，也不要把本夜算成新系列的 **1/3**。
**不是签核。**

**本夜是 Luke `rxrst_n` 直推 `0f783df6` 之后的又一次闸后健康快照，不是新系列。**
相对上一夜 tip `60444716`（PR104；`reports/regress/2026-09-19.md` 的 checkout），
tip 只前进了报告：PR105（闸后健康 2026-09-19）+ PR106（夜间 lint+CDC 2026-09-19）。
**RTL 与上一夜完全相同。** 本夜 tip `d1e6f3a2` = PR106 merge。

Lint ECO consecutive-green **3/3 已关闭**（`reports/regress/2026-09-08-lint-eco-1of3.md` /
`2026-09-08-lint-eco-2of3.md` / `2026-09-08-lint-eco-3of3.md`；PR68）。
书面 freeze 仍为 `982ddd0a`，**BROKEN / H hold-pin**。Decision **H 未选**。
本夜 **不** 把 freeze 重钉到 tip。该计数保持关闭。本文件只做闸后健康快照。

旧系列 **VOID**，不要对照：
- `32a7f5e0` 夜间健康 / 2026-09-03 3/3（DUT 端口已改名）
- CR-B `1ed4d350` 系列（`2026-09-08-crb-*of3`；Lint ECO PR62 落地后作废）

基线对照：`reports/regress/2026-09-08-lint-eco-3of3.md`（Lint ECO 3/3 关闭记录）。
上一夜闸后健康：`reports/regress/2026-09-19.md`（PR105；**也不是 4/3 / 不是 1/3**；
first post-`rxrst_n`，tip `60444716`）。本夜再量同一 RTL，**不** 重开连续全绿。

聊天 PASS 不算验收。本文件是给芯片开发PM 的 in-repo 记录。
进仓后 PM 即时验。

| Item | Value |
|------|--------|
| Checkout tip SHA | `d1e6f3a2a4a36b82587bf4d08a76500d0d0206e5`（PR106 lint+CDC reports merge；RTL = Luke PRBS23/`txrst_n` + `rxrst_n` `0f783df6`，与 2026-09-19 相同） |
| 书面 DUT freeze | `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**。本夜 **未** 重钉到 tip |
| `git diff 982ddd0a...HEAD -- rtl/` | **NON-EMPTY** — `rtl/pma/vibe_pma_bnd.sv`（+99/−11）、`rtl/port/vibe_port.sv`（+3/−3 `txrst_n` + `rxrst_n`）。合计 +102/−14 |
| `git diff 982ddd0a...HEAD -- include/` | **empty** |
| `git diff 60444716...HEAD -- rtl/` | **empty**（相对上一夜 tip 无 RTL 增量；仅 reports） |
| `rtl/` / `include/` / SPEC 本夜 | **未改**。无 overlay。默认 **不改 TB**（本夜零 TB 源码编辑） |
| Tip lint Error | `vibe_pma_bnd.sv:8` `txrst_n = 1'b1` default（PR98；Error）。`rxrst_n = 1'b1` 同形态（`:9`；PR106 记 NEW）。**设计 tip-debt**。H hold-pin 下 **不 ECO rtl** |
| TB on tip | UVM 1.2 + AMD Vivado **xsim** 现为 `make -C tb/vibe sim` 的默认路径。历史 Icarus 门仍是 `make -C tb/vibe sim-icarus`。Luke `0f783df6` 已改 matching TB（本夜未再改） |
| xsim / UVM | **不可用** — `xvlog`/`xelab`/`xsim` 不在 PATH，无 Vivado Simulator license。`make -C tb/vibe sim` 于 suite 第一步失败：`xvlog: command not found`（`XSIM_MAKE_EXIT=2`） |
| 本夜功能门 | `make -C tb/vibe sim-icarus`（Icarus Verilog **12.0**）— **本夜实测**（2026-09-20 02:03–02:05 CST） |
| Cov | `make -C tb/vibe cov`（Verilator **5.020**；与 xsim 无关）— **本夜实测**（2026-09-20 02:05–02:10 CST） |
| `SIM_ICARUS_EXIT` | **0** |
| `XSIM_MAKE_EXIT` | **2**（缺工具，不是 DUT FAIL） |
| `COV_EXIT` | **0** |
| 通过率 | **124/124 buckets = 100%**（suite 27 + units 85 + top 1 + neg 11；0 fail） |
| Result | **PASS** — LINE **724/762 = 95.0% ≥ 95% + waiver**；功能 published **159/159**；断言 **N/A** |
| `ovf_l` | F1 永久冻结 CDC-WARN — 本夜不为此开 issue |

## Pin confirm

- Checkout: tip `d1e6f3a2a4a36b82587bf4d08a76500d0d0206e5`（PR106 reports-only）。
  `git rev-parse HEAD` = 该值。
- 书面 freeze: `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**。
  **Do not re-pin freeze to tip.**
- `git diff 982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6...HEAD -- rtl/` **NON-EMPTY**：
  `rtl/pma/vibe_pma_bnd.sv`（PRBS23 / `txrst_n` + Luke `rxrst_n`），
  `rtl/port/vibe_port.sv`（`.txrst_n(txrst_n), .rxrst_n(rxrst_n)`）。
- `git diff 982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6...HEAD -- include/` **empty**。
- 相对上一夜 tip `6044471693cb64978b9d6a05f305bb8cbdf9180b`：
  `git diff 60444716...HEAD -- rtl/` **empty**；`-- include/` **empty**。
  tip 增量只有 `reports/regress/2026-09-19.md`（PR105）与
  `reports/lint/` + `reports/cdc/` 2026-09-19（PR106）。
- F1 `ovf_l` untouched（Decision F1 stands）。
- `rtl/` 未搬家。本夜未改 `rtl/` / `tb/` / `include/` / SPEC / checkers。只新增本报告。
- Tip lint `vibe_pma_bnd.sv:8` `txrst_n` default **未 ECO**（H hold-pin）。
  不 chase `rxrst_n` 同形态 default。

## 本夜命令（flow 与 2026-09-16 / 2026-09-17 / 2026-09-18 / 2026-09-19 相同）

历史夜间门曾经是 `make -C tb/vibe sim`（Icarus）然后 `make -C tb/vibe cov`。
**tip 已改：** `tb/vibe/README.md` / `tb/vibe/Makefile` 把默认 `sim` 指到 xsim+UVM；
Icarus 全回归改名为 `sim-icarus`。`cov` 仍是 Verilator，不依赖 xsim。

本夜实际跑过：

```bash
# 1) 记录默认 UVM 路径不可用（无商业 license / 无 xvlog）
make -C tb/vibe sim
# → xvlog: command not found ; make suite Error 127 ; XSIM_MAKE_EXIT=2

# 2) 仓库支持的开源全回归（本夜功能门）
make -C tb/vibe sim-icarus
# → SIM_ICARUS_EXIT=0
#    SUITE 27/27 ; UNITS pass_files=85 fail_files=0 compile_fail=0
#    PASS tc_top_smoke ; NEG 11/11
#    TOTAL_PASS_LINES=158 TOTAL_FAIL_LINES=0
#    tc_nw_pkt_pma_loopback PASS 100/100（am_lock_end=1111 saw_fec_fail=0 saw_deskew=1）

# 3) 覆盖率（与历史相同）
make -C tb/vibe cov
# → COV_EXIT=0 ; LINE 724/762 = 95.0% ; TOGGLE 9712/17879 = 54.3%
#    clusters=58（rsdec 进 merge；port/top OOM skip）
```

工具：Icarus Verilog **12.0 (stable)**；Verilator **5.020 2024-01-01**（Debian 5.020-1）。
未改 Makefile / 未加 overlay / 未 chase xsim。xsim 缺工具 = **non-DUT fail**（与 2026-09-16 / 2026-09-17 / 2026-09-18 / 2026-09-19 相同）。

## 通过率

功能门 = **Icarus `sim-icarus`**（xsim 本夜未跑通，不计入通过率）。

| Bucket | pass | fail | compile_fail | 通过率 |
|--------|------|------|----------------|--------|
| suite (`Makefile.icarus` suite) | **27** | 0 | 0 | **27/27 = 100%** |
| units (`run_units.sh`) | **85** | 0 | 0 | **85/85 = 100%** |
| top (`tc_top_smoke`) | **1** | 0 | 0 | **1/1 = 100%** |
| neg (`scan_absent.sh`) | **11** | 0 | 0 | **11/11 = 100%** |
| **合计（文件/桶）** | **124** | **0** | **0** | **124/124 = 100%** |
| summarize | `TOTAL_PASS_LINES=158` | `TOTAL_FAIL_LINES=0` | — | 0 fail |

`SUITE pass=27 fail=0 ran=27` / `SUITE_RESULT PASS`.
`UNITS pass_files=85 fail_files=0 compile_fail=0`.
`PASS tc_top_smoke`.

（`TOTAL_PASS_LINES` 是本夜 `summarize.sh` 对 `results/*.log` 的 `^PASS ` 行计数，
含多 PASS 的 unit log；`summarize.sh` 排除 `cov.log`。与 2026-09-19 / 2026-09-18 /
2026-09-16 的 158 对齐。）

Icarus is the functional gate tonight. xsim/UVM is documented on tip but **not measured**
（工具缺失，不是 DUT 红）。

## 失败列表

**空。** 本夜 Icarus 门 0 FAIL / 0 compile_fail。无失败 TC，无波形，无 GitHub issue。

先排除 TB / 工具：

- 默认 `make -C tb/vibe sim`：`xvlog: command not found`，`XSIM_MAKE_EXIT=2`。
  **缺工具**，不是 DUT mismatch（与 2026-09-16 / 2026-09-17 / 2026-09-18 / 2026-09-19 相同）。
- Luke `0f783df6` 已把 `rxrst_n` 接到 `vibe_pma_bnd` / `vibe_port`，并改 matching TB。
  本夜 stock Icarus：`tc_nw_pkt_pma_loopback` **compile OK**，**PASS 100/100 packets**，
  `am_lock_end=1111`，`saw_fec_fail=0`，`saw_deskew=1`。
  `tc_pma_512b_slice` / `tc_pma_922mhz` / `tc_port_smoke` / `tc_top_smoke` 均为 **PASS**。
- PRBS23 空闲填充 + `rxrst_n` **没有** 把 GOLDEN 环回打红（与 2026-09-19 相同；RTL 未变）。

结论：**本夜无新 TB / DUT / TOOL 功能红。**
xsim 路径因 `xvlog` 不存在而无法启动，**不是** DUT mismatch。
F1 `ovf_l` 仍为永久冻结 WARN，本夜不为此开 issue。不叫醒 Luke。
不 ECO tip lint Error（`vibe_pma_bnd.sv:8` `txrst_n` default；也不 ECO `rxrst_n` default）。

Verilator coverage clusters still skip `vibe_suite` (VOQ OOM) and `port`/`top`
(`NOTE cov: cluster … compile rc=1 (timeout/OOM skipped, not waived)`)。
本夜 `rsdec` **进 merge**（58 clusters；与 2026-09-19 / 2026-09-18 相同）。
Icarus `tc_rs_dec_syndrome.log` 本夜 **PASS**。

Verilator-only `FAIL` lines on `tc_lmsm_walk` / `tc_pcs_fec_dual_enc` /
`tc_pcs_fec_t2` / `tc_route_lu` / `tc_rs_dec_syndrome` during `make cov`
are the same tool/timing limits as Lint ECO 1/3–3/3 与 2026-09-16 / 2026-09-17 /
2026-09-18 / 2026-09-19；those TCs **PASS** on Icarus。

**PRBS23 / `rxrst_n` 后 Verilator-only FAIL（Icarus 门 PASS，不算功能红）：**
`tc_pma_512b_slice` / `tc_pma_922mhz` — Verilator 停在同一拍
`pcs_pma_txdata`（LFSR 不前进 / `vld` 包不切到 lane 字）。Icarus 对应
`.log` 均为 `PASS`（slice 检查了 idle PRBS 前进 + vld 切片）。**TOOL**。
Icarus is the functional gate.

## 代码覆盖 (code)

Tool: Verilator **5.020** `--coverage-line --coverage-toggle`。合并 **58** 个
cluster `.dat`（与 2026-09-19 相同：`rsdec` 进 merge）。`vibe_suite` 未 bind
（VOQ OOM）。port/top compile rc=1（OOM skipped, not waived）— 0 records。
`vibe_rs128_120_dec.sv` 本夜 **13/13 LINE**。

LINE **724/762 = 95.0%** ≥ 95%。不追 LINE 100%。相对 2026-09-19 的
724/762 = 95.0%，**命中与分母均相同**（RTL 与上一夜相同）。
仍过 ≥95% + waiver 门。`cov_report.py` 文案里残留的 `708/737 = 96.1%`
是历史硬编码，**不是** 本夜实测；本夜以脚本打印的 `LINE 724/762 = 95.0%` 为准。

| Metric | Hit/tot | % | Gate |
|--------|--------:|--:|------|
| **LINE** | **724/762** | **95.0** | **≥95% + waiver — PASS** |
| **TOGGLE** | **9712/17879** | **54.3** | 无 80% 门槛；wide-bus unused bits + PRBS23 宽总线 |
| **FSM** | n/a | — | 无 VCS FSM engine；LINE 打在 state `case`/`if` |

不追 LINE 100%。

Waiver（点名，与本夜 `cov_report.md` uncovered LINE + `COVERAGE_HOLES.md` 一致，未改分类）：

| File:line | Class |
|-----------|--------|
| `vibe_dll_tx.sv:98/:144` | **WAIVER / 防御** |
| `vibe_cfg_space.sv:96–99` | **WAIVER** — RW1C `hold_fall` HW-clear。设计 agreed，**不改** `rtl/` |
| `vibe_lmsm.sv:101–108` | **TOOL** (`tmr_load`) |
| `dec_lid` / dll_tx pad combo | **TOOL** |
| pcs rem/pend / g1 `:51` | **TB空洞 — WAIVE** |
| `vibe_xbar.sv:50 else` | **TB空洞 — WAIVE** — ECO lock-hold implicit else（`locked && !matching in_vld/dst`）。与 Lint ECO 1/3–3/3 相同。仍在本夜 uncovered LINE。Not a DUT fail。 |

本夜 uncovered LINE 与 Lint ECO 3/3 同类：`cfg_space` hold_fall `:96–99`、`dll_tx`
`:98/:144/:151/:155/:159`、`lmsm` `:101–108`、`pcs_rx` rem/pend、
`pcs_rx_amctl_lock` `:55–58`、`pcs_tx_g1` `:51`，plus ECO
`vibe_xbar.sv:50 else`（TB hole, waived）。未改 waiver 分类，未改 RTL。

### PRBS23 / `rxrst_n` 后空洞（诚实分类；不追 LINE 100%）

`vibe_pma_bnd.sv` 本夜 **14/22 = 63.6%**（与 2026-09-19 相同）。
本夜 `cov_report.md` uncovered LINE：

| File:line | Class |
|-----------|--------|
| `vibe_pma_bnd.sv:50/:56` | **TOOL** — `prbs23_adv128` function body（2026-09-18 记为 `:49/:55`，行号随 `rxrst_n` 下移）。与 `tmr_load` / `dec_lid` 同类：Verilator 5.020 对 automatic function 内联后 LINE 打不进。Icarus `tc_pma_512b_slice` 已打 idle 前进。 |
| `vibe_pma_bnd.sv:81 if` / `:81 else` | **TOOL** — `end else if (afifo_pma_lane_vld)` / `else`（2026-09-18 记为 `:80`）。Verilator PMA cluster 本夜 FAIL（LFSR 不前进、vld 不切 lane；见上）。Icarus 两 TC **PASS**（idle PRBS + vld pack 都打到）。**不是 DUT 死代码。** |
| `vibe_pma_bnd.sv:98 else` | **TOOL** — `tx_pcs_d` 非复位支路（`0f783df6` 新增延迟 valid）。Verilator PMA cluster FAIL，只打到 `txrst_n` 复位。Icarus loopback **PASS 100/100**。 |
| `vibe_pma_bnd.sv:105 else` / `:116 if` / `:116 else` | **TOOL** — `rxrst_n` 后的 RX 切片 / 环回 `vld` mux（`0f783df6`）。同一 Verilator PMA FAIL。Icarus `tc_nw_pkt_pma_loopback` / slice **PASS**。**不是 DUT 死代码。** |

未 chase LINE 100%。未改 TB / 未改 RTL。无本夜新增空洞**类别**（仍是 TOOL / 已有 waiver）。
不因此开 issue。

## 功能覆盖 (functional)

无 `covergroup`。官方 **159** 个 TP-0.3 ID（`TP_TC_MATRIX.md`）：
MAPPED=106 ADDED=16 HOLE=9 NEG=28。打分 TC 均在本夜 stock `sim-icarus` 中跑过。

| Class | Count | Status |
|-------|------:|--------|
| MAPPED + ADDED | 122 | hit（含 **TP-PHY-012** = `tc_nw_pkt_pma_loopback` stock **PASS 100/100**） |
| NEG | 28 | hit |
| HOLE（freeze SPEC **非目标**） | 9 | **waiver / 非目标** |
| **合计** | **159** | **100% of published** |

HOLE 九个（非目标，不重开计数）：TP-HOLE-G2, G3, G4, G5, G6, G8, G9, 010, 012.

UVM 矩阵列已改指向 `tb/vibe/uvm/pkg/*.svh`，但本夜 **未跑 xsim**；功能分仍按
Icarus 同名 TC / 官方 159 ID。`rxrst_n` 后 TP-PHY-012 仍 stock **PASS**。
不算新 1/3 / 不算 reopen consecutive green。

## 断言覆盖 (assertion)

**N/A** — 本 TB / DUT 无 SVA `assert` / `cover property` / `covergroup`。
仓库里仅有英文注释 “async assert”，不是断言 bins。

## 与连续全绿的关系

| Item | Note |
|------|------|
| 旧 3/3（`2026-09-03` ×3，freeze `32a7f5e0`） | **VOID** |
| CR-B re-gate 3/3（`2026-09-08-crb-*of3`，freeze `1ed4d350`） | **VOID** |
| Lint ECO 3/3（`2026-09-08-lint-eco-*of3`，freeze `982ddd0a`） | **CLOSED**（PR68） |
| 2026-09-09 … 2026-09-16 闸后健康 | **不是 4/3**（PR70 / PR75 / PR80 / PR84 / PR86 / PR88 / PR92 / PR94） |
| 2026-09-17 闸后健康 | **不是 4/3 / 不是 1/3**（PR97；TB compile FAIL） |
| 2026-09-18 闸后健康 | **不是 4/3 / 不是 1/3**（PR101；pre-`rxrst_n` tip `989f4140`） |
| 2026-09-19 闸后健康 | **不是 4/3 / 不是 1/3**（PR105；first post-`rxrst_n` tip `60444716`） |
| 本次 | **闸后夜间健康检查**（`d1e6f3a2` + 书面 freeze `982ddd0a` **BROKEN / H hold-pin**；**another health after Luke `rxrst_n` `0f783df6`**；RTL = 2026-09-19）— **不是 4/3** — **不是新 1/3** — **不是签核** |
| 新系列？ | **否** — 不要重开 3 次计数。Decision H 未选，freeze **不** 重钉 |

`rtl/` / `tb/` / `include/` / SPEC / checkers 均未改。只新增本报告。无 sidecar。
Handoff: 验证 → 芯片开发PM（进仓后即时验）。
