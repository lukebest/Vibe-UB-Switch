# Lint ECO re-gate 2/3 — 2026-09-08 (Asia/Shanghai)

**This is Lint ECO re-gate 2/3.** It is **not** nightly health and **not** 4/3.

The previous consecutive-green **CR-B 3/3**
(`reports/regress/2026-09-08-crb-1of3.md` /
`2026-09-08-crb-2of3.md` /
`2026-09-08-crb-3of3.md`) against freeze `1ed4d350` is **void** after the
Lint ECO DUT landed (PR62). That void stands. This file is **2/3** of the
new consecutive-green series against the same DUT freeze `982ddd0a` as
`reports/regress/2026-09-08-lint-eco-1of3.md` (**1/3**, already on main via
PR64).

Chat PASS does not count. This file is the in-repo record for 芯片开发PM.
`rtl/`, `tb/`, and `include/` were **not** modified on this run.

| Item | Value |
|------|--------|
| Label | **Lint ECO re-gate 2/3** |
| `origin/main` SHA | `c500fdf05d41469aae9deae0deec547580d1d1da` (after PR64 1/3 merge) |
| DUT / freeze RTL SHA | `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` (same as 1/3) |
| TB SHA (already on main) | `76660cd2b4430835f32994e3b0484886f5cef7ce` (PR60 waves; this run did not edit `tb/`) |
| `git diff` `rtl/` vs `982ddd0a` | **empty** |
| `git diff` `include/` vs `982ddd0a` | **empty** |
| F1 `ovf_l` | **untouched** — `git diff 1ed4d350 -- rtl/port/vibe_port.sv` empty |
| Sim | `make -C tb/vibe sim` (Icarus Verilog **12.0**) — **this run, measured** |
| Cov | `make -C tb/vibe cov` (Verilator **5.020**) — **this run, measured** |
| `SIM_EXIT` | **0** |
| `COV_EXIT` | **0** |
| 通过率 | **124/124 buckets = 100%**（suite 27 + units 85 + top 1 + neg 11；0 fail） |
| Result | **PASS** — LINE **715/745 = 96.0% ≥ 95% + waiver**；功能 **159/159**；断言 **N/A** |
| `ovf_l` | F1 frozen CDC-WARN — no ECO in this series |

## Pin confirm

- Checkout: `origin/main` at PR64 merge `c500fdf0` (1/3 already on main).
- `git diff 982ddd0a -- rtl/` is **empty** vs this main tip.
- ECO files only (vs voided CR-B freeze `1ed4d350`): `rtl/fabric/vibe_xbar.sv`,
  `vibe_fabric.sv`, `vibe_saf_ing.sv`. Unchanged on this run.
- `rtl/port/vibe_port.sv` `ovf_l` / `ovf_s1` / `ovf_s2` / `afifo_ovf`
  **byte-identical** to `1ed4d350`. Decision **F1** stands.

## 通过率

| Bucket | pass | fail | compile_fail | 通过率 |
|--------|------|------|----------------|--------|
| suite (`make suite`) | **27** | 0 | 0 | **27/27 = 100%** |
| units (`make units`) | **85** | 0 | 0 | **85/85 = 100%** |
| top (`make top`) | **1** (`tc_top_smoke`) | 0 | 0 | **1/1 = 100%** |
| neg (`make neg`) | **11** | 0 | 0 | **11/11 = 100%** |
| **合计（文件/桶）** | **124** | **0** | **0** | **124/124 = 100%** |
| summarize | `TOTAL_PASS_LINES=158` | `TOTAL_FAIL_LINES=0` | — | 0 fail |

`SUITE pass=27 fail=0 ran=27` / `SUITE_RESULT PASS`.
`UNITS pass_files=85 fail_files=0 compile_fail=0`.
`PASS tc_top_smoke`.

Icarus is the functional gate. `summarize.sh` excludes `cov.log`.
No compile_fail against the ECO. No TB edit.

## 失败列表

**空。** 0 FAIL / 0 compile_fail on the Icarus gate. No DUT fail after TB
exclusion. No GitHub issue opened. No `rtl/` / `tb/` / `include/` edit.

F1 `ovf_l` remains the permanent frozen WARN (Luke). Not an ECO in this series.

Verilator coverage clusters still skip `vibe_suite` (VOQ OOM) and `port`/`top`
(`NOTE cov: cluster … compile rc=1 (timeout/OOM skipped, not waived)`).
Verilator-only `FAIL` lines on `tc_lmsm_walk` / `tc_pcs_fec_dual_enc` /
`tc_pcs_fec_t2` / `tc_rs_dec_syndrome` / `tc_route_lu` during `make cov` are
the same tool/timing limits as 1/3; those TCs **PASS** on Icarus (`85/85`).
Icarus is the functional gate.

## 代码覆盖 (code)

Tool: Verilator **5.020** `--coverage-line --coverage-toggle`. Merged **58**
cluster `.dat`. `vibe_suite` not bound (VOQ OOM). port/top 0 records (OOM).

LINE matches 1/3 (`715/745 = 96.0%`). Denominator vs voided CR-B 3/3
(`708/737`) is the ECO combo defaults / grant-accept split / SAF combo
(`vibe_xbar` 34/35, `vibe_fabric` 50/50, `vibe_saf_ing` 17/17).

| Metric | Hit/tot | % | Gate |
|--------|--------:|--:|------|
| **LINE** | **715/745** | **96.0** | **≥95% + waiver — PASS** |
| **TOGGLE** | **9537/17272** | **55.2** | 无 80% 门槛；wide-bus unused bits |
| **FSM** | n/a | — | 无 VCS FSM engine；LINE 打在 state `case`/`if` |

不追 LINE 100%。

Waiver（点名，与本跑 `cov_report.md` uncovered LINE + `COVERAGE_HOLES.md` 一致，未改分类、未改 RTL）：

| File:line | Class |
|-----------|--------|
| `vibe_dll_tx.sv:98/:144` | **WAIVER / 防御** |
| `vibe_cfg_space.sv:96–99` | **WAIVER** — RW1C `hold_fall` HW-clear。设计 agreed，**不改** `rtl/` |
| `vibe_lmsm.sv:101–108` | **TOOL** (`tmr_load`) |
| `dec_lid` / dll_tx pad combo | **TOOL** |
| pcs rem/pend / g1 `:51` | **TB空洞 — WAIVE** |
| `vibe_xbar.sv:50 else` | **TB空洞 — WAIVE** — ECO lock-hold implicit else (`locked && !matching in_vld/dst`). Icarus `tc_xbar_unit` keeps `in_vld` through lock. Not a DUT fail. Not cheap on this report-only run. |

Uncovered LINE vs 1/3 named set: same `cfg_space` hold_fall `:96–99`、`dll_tx`
`:98/:144/:151/:155/:159`、`lmsm` `:101–108`、`pcs_rx` rem/pend、
`pcs_rx_amctl_lock` `:55–58`、`pcs_tx_g1` `:51`，plus ECO
`vibe_xbar.sv:50 else` (TB hole, waived). No design issue opened.

## 功能覆盖 (functional)

无 `covergroup`。官方 **159** 个 TP-0.3 ID（`TP_TC_MATRIX.md`）：
MAPPED=106 ADDED=16 HOLE=9 NEG=28。打分 TC 均在本跑 `make sim` 中跑过。

| Class | Count | Status |
|-------|------:|--------|
| MAPPED + ADDED | 122 | hit |
| NEG | 28 | hit |
| HOLE（freeze SPEC **非目标**） | 9 | **waiver / 非目标** |
| **合计** | **159** | **100% of published** |

HOLE 九个（非目标，不重开计数）：TP-HOLE-G2, G3, G4, G5, G6, G8, G9, 010, 012.

## 断言覆盖 (assertion)

**N/A** — 本 TB / DUT 无 SVA `assert` / `cover property` / `covergroup`。
仓库里仅有英文注释 “async assert”，不是断言 bins。

## 与连续全绿的关系

| Item | Note |
|------|------|
| 旧 3/3 (`2026-09-03` ×3) | **void** — DUT ports renamed by CR-B / PR51 |
| CR-B re-gate 3/3 (`2026-09-08-crb-*of3`, freeze `1ed4d350`) | **void** — Lint ECO landed (PR62); DUT freeze is now `982ddd0a` |
| 1/3 | `reports/regress/2026-09-08-lint-eco-1of3.md` — **PASS** vs DUT `982ddd0a` (PR64 / `c500fdf0`) |
| 本次 | **Lint ECO re-gate 2/3**（DUT freeze `982ddd0a` + main after PR64 `c500fdf0`） |
| 夜间健康 / 4/3 | **否** |
| 新系列 3/3 | 未开始 |

Path B impl gate stays **NOT PASS**. This report is a Lint ECO re-gate, not signoff.
Handoff: 验证 → 芯片开发PM. Leave merge for PM.
