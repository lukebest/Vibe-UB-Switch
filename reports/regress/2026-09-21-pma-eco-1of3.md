# PMA ECO 新系列 1/3 — 2026-09-21 (Asia/Shanghai)

**这是「新系列 1/3」。** It is **not** gate-after health and **not** 4/3.

旧 freeze `982ddd0a` Lint ECO consecutive-green **3/3 已 CLOSED 历史保留**
（`reports/regress/2026-09-08-lint-eco-1of3.md` /
`2026-09-08-lint-eco-2of3.md` /
`2026-09-08-lint-eco-3of3.md`；PR68）。
不要从那组续 4/3，也不要把本跑记成闸后健康。

PMA ECO（PR109，RTL `a658d141`）清掉 tip lint Error=2 与 PMA loopback CDC NEW。
Xia 已把书面 DUT freeze 钉到 ECO tip（PR112 docs）。
本文件 **打开** 对 DUT=`a658d141` 的新连续全绿计数：**1/3**。

闸后健康 `reports/regress/2026-09-21.md`（PR110；tip `e1c55479`；书面 freeze
当时仍是 `982ddd0a` **BROKEN / H hold-pin**）是 **另一件事** — **不是** 本系列、
**不是** 1/3、**不是** 4/3。

聊天 PASS 不算验收。本文件是给芯片开发PM 的 in-repo 记录。
`rtl/`、`tb/`、`include/`、SPEC、checkers、Makefile、`.vlt` **均未改**。

| Item | Value |
|------|--------|
| Label | **PMA ECO 新系列 1/3**（**不是** 闸后健康 / **不是** 4/3） |
| Checkout tip SHA | `a0eaff51a13839e6553849af23363314f0db1ca6`（PR112 Xia freeze-pin merge；`git rev-parse HEAD`） |
| 书面 DUT freeze | `a658d14163b0e66b3875ee42a7dcc8a10024ba51`（PMA ECO RTL；merge `f234d4b0` 仅追溯） |
| `git diff a658d14163b0e66b3875ee42a7dcc8a10024ba51...HEAD -- rtl/` | **empty** |
| `git diff a658d14163b0e66b3875ee42a7dcc8a10024ba51...HEAD -- include/` | **empty** |
| `git diff e1c55479...HEAD -- tb/` | **empty**（与预 ECO 闸后健康同一份 TB） |
| `rtl/` / `tb/` / `include/` / SPEC / checkers / Makefile / `.vlt` 本跑 | **未改**。无 overlay |
| xsim / UVM | **不可用** — `xvlog` 不在 PATH。`make -C tb/vibe sim` 于 suite 第一步失败 |
| 本跑功能门 | `make -C tb/vibe sim-icarus`（Icarus Verilog **12.0**）— **本跑实测**（2026-09-21 09:16–09:22 CST） |
| Cov | `make -C tb/vibe cov`（Verilator **5.020**）— **本跑实测**（2026-09-21 09:28–09:33 CST） |
| `XSIM_MAKE_EXIT` | **2**（缺 `xvlog`，不是 DUT FAIL） |
| `SIM_ICARUS_EXIT` | **2**（units 2 FAIL 后 make 停；top 另测 1 FAIL） |
| `COV_EXIT` | **0** |
| 通过率 | **121/124 buckets**（suite 27 + units 83 + top 0 + neg 11；**3 DUT fail**） |
| Result | **FAIL（DUT）** — Icarus **不是** 124/124；LINE **726/761 = 95.4% ≥ 95% + waiver**；功能 published **159** 中 **2 MAPPED miss**；断言 **N/A** |
| `ovf_l` | F1 永久冻结 CDC-WARN — **不** 为此开 issue |

## Pin confirm

- Checkout: tip `a0eaff51a13839e6553849af23363314f0db1ca6`（PR112 docs freeze-pin）。
  `git rev-parse HEAD` = 该值。
- 书面 freeze: `a658d14163b0e66b3875ee42a7dcc8a10024ba51`（PMA ECO RTL）。
- `git diff a658d14163b0e66b3875ee42a7dcc8a10024ba51...HEAD -- rtl/` **empty**。
- `git diff a658d14163b0e66b3875ee42a7dcc8a10024ba51...HEAD -- include/` **empty**。
- 相对预 ECO tip `e1c55479`：`-- tb/` **empty**；RTL 增量只有 `a658d141` 的
  `rtl/pma/vibe_pma_bnd.sv`（去 input default + idle-PRBS 代 `tx_pcs_d`）。
- F1 `ovf_l` untouched（Decision F1 stands）。
- 本跑未改 `rtl/` / `tb/` / `include/` / SPEC / checkers / Makefile / `.vlt`。
  只新增本报告。

## 本跑命令

```bash
# 1) 记录默认 UVM 路径不可用（无商业 license / 无 xvlog）
make -C tb/vibe sim
# → xvlog: command not found ; make suite Error 127 ; XSIM_MAKE_EXIT=2

# 2) 仓库支持的开源全回归（本跑功能门）
make -C tb/vibe sim-icarus
# → SIM_ICARUS_EXIT=2
#    SUITE 27/27
#    UNITS pass_files=83 fail_files=2 compile_fail=0
#    FAIL tc_port_smoke
#    FAIL tc_nw_pkt_pma_loopback（packet 0/100 timeout）
#    make 在 units 停下，未进 top/neg
# 补测（同一棵树，未改 TB）：
#    make -C tb/vibe -f Makefile.icarus top  → FAIL tc_top_smoke（vvp/make exit 0，log FAIL）
#    make -C tb/vibe -f Makefile.icarus neg  → NEG 11/11
#    summarize: TOTAL_PASS_LINES=155 TOTAL_FAIL_LINES=3

# 3) 覆盖率
make -C tb/vibe cov
# → COV_EXIT=0 ; LINE 726/761 = 95.4% ; TOGGLE 9713/17879 = 54.3%
#    clusters=58（rsdec 进 merge；port/top OOM skip）
```

工具：Icarus Verilog **12.0 (stable)**（`12.0-2build2`）；
Verilator **5.020 2024-01-01**（Debian 5.020-1）。
未改 Makefile / 未加 overlay / 未 chase xsim。
xsim 缺工具 = **non-DUT fail**。

## 通过率

功能门 = **Icarus `sim-icarus`**（xsim 本跑未跑通，不计入通过率）。
套件文件数与历史相同（仍是 27+85+1+11），**不是** suite 变更；
诚实计数是 **121/124**，不是历史全绿 **124/124**。

| Bucket | pass | fail | compile_fail | 通过率 |
|--------|------|------|----------------|--------|
| suite (`Makefile.icarus` suite) | **27** | 0 | 0 | **27/27 = 100%** |
| units (`run_units.sh`) | **83** | **2** | 0 | **83/85 = 97.6%** |
| top (`tc_top_smoke`) | **0** | **1** | 0 | **0/1** |
| neg (`scan_absent.sh`) | **11** | 0 | 0 | **11/11 = 100%** |
| **合计（文件/桶）** | **121** | **3** | **0** | **121/124** |
| summarize | `TOTAL_PASS_LINES=155` | `TOTAL_FAIL_LINES=3` | — | 3 fail |

`SUITE pass=27 fail=0 ran=27` / `SUITE_RESULT PASS`.
`UNITS pass_files=83 fail_files=2 compile_fail=0`.
`FAIL tc_top_smoke`（补测；`Makefile.icarus` top 目标不按 log FAIL 置位 make）。

（`TOTAL_PASS_LINES` 是本跑 `summarize.sh` 对 `results/*.log` 的 `^PASS ` 行计数，
含多 PASS 的 unit log；`summarize.sh` 排除 `cov.log`。历史全绿夜是 158 / 0 fail；
本跑 155 / 3 fail = 那 3 条 DUT 红。）

Icarus is the functional gate. xsim/UVM is documented on tip but **not measured**
（工具缺失，不是 DUT 红）。

## 失败列表

先排除 TB / 工具：

- 默认 `make -C tb/vibe sim`：`xvlog: command not found`，`XSIM_MAKE_EXIT=2`。
  **缺工具**，不是 DUT mismatch。
- `compile_fail=0`。`git diff e1c55479...HEAD -- tb/` **empty**。
- TX-only `tc_nw_pkt_to_pma_tx` **PASS**（dll_tx===GOLDEN_TX；258 PMA pack；
  104 PCS-lane golden；258 gear golden）。
- PMA unit `tc_pma_512b_slice` / `tc_pma_922mhz` Icarus **PASS**。
- 仓外 PMA-only Icarus probe（不改仓内 TB）：TX idle `idle_prbs=1` 被丢；
  非 PRBS packed beat 下一拍 `rvld=1` 且 `r0` 对齐 pack。
- 预 ECO 闸后健康（`2026-09-21.md`，tip `e1c55479`，同一 TB）：
  `tc_nw_pkt_pma_loopback` **PASS 100/100**，`tc_port_smoke` **PASS**，
  `tc_top_smoke` **PASS**。

**TB 排除后仍红 — DUT（PMA ECO `a658d141`）：**

| TC | Bucket | 现象 |
|----|--------|------|
| `tc_port_smoke` | units | **FAIL** — PMA loopback；`nw_fab_vld=0` `last_rx=0` `fec_fail=0` `am_locked=1111`。GOLDEN_TX 未回收（TP-PHY-001） |
| `tc_nw_pkt_pma_loopback` | units | **FAIL** — packet **0/100** timeout；`pcs_dll_vld=0` `deskew=1` `am_lock=1111` `fec_fail=0`。GOLDEN vs 0（TP-PHY-012） |
| `tc_top_smoke` | top | **FAIL** — peer `pcs_pma_txdata` → `pma_pcs_rxdata_0`；`irq_logic` stayed 0（G1 未到 top 脚） |

DUT 增量（相对预 ECO）：`a658d141` 删掉 `tx_pcs_d` 与
`if (pma_pcs_rxdata === pcs_pma_txdata) vld <= tx_pcs_d`（`txclk`→`rxclk` 采样，
CDC NEW，已 GONE），改为 `rxclk` 本地 `idle_prbs`（四 lane 满足
`w[i]==w[i-23]^w[i-18]`，与 PMA idle fill / `vibe_pcs_scramble` 同 poly）后
`pma_afifo_lane_vld <= !idle_prbs`。

同钟 TB 仍 `assign pma_pcs_rxdata = pcs_pma_txdata` / `rxclk = txclk`。
`idle_prbs` **不等价** 于旧的 delayed TX-valid：全端口 PCS 链路上 Icarus
不再回收 NW GOLDEN / top G1。

本跑 **不改** `rtl/`。向 **设计** 开 issue（见下）。F1 `ovf_l` **不开** issue。

### GitHub issue → 设计

规则：TB 排除后 DUT fail → 开 issue 给设计。本跑 GitHub MCP
`issue_write` 返回 **403**（`Resource not accessible by personal access token`）；
`list_issues` 亦为 0（与 `docs/STATUS.md` 历史 “open=0 closed=0” 一致）。
**未能建票。** 下面是给芯片开发PM / 设计的现成正文（请人工开 issue）：

**Title:** `[设计] PMA ECO a658d141 idle-PRBS drop 打红 Icarus PMA loopback（TP-PHY-001 / TP-PHY-012 / top）`

**Body:** 见上表三 TC + pin `a658d141` / tip `a0eaff51`。不要重开 `txclk` 采样
`rxclk`（CDC）。恢复 packed-beat RX valid，使三 TC 回到 stock PASS。
F1 `ovf_l` 与 xsim 缺工具不在范围内。

Verilator coverage clusters still skip `vibe_suite` (VOQ OOM) and `port`/`top`
(`NOTE cov: cluster … compile rc=1 (timeout/OOM skipped, not waived)`)。
本跑 `rsdec` **进 merge**（58 clusters）。Icarus `tc_rs_dec_syndrome.log` **PASS**。

Verilator-only `FAIL` lines on `tc_lmsm_walk` / `tc_pcs_fec_dual_enc` /
`tc_pcs_fec_t2` / `tc_route_lu` / `tc_rs_dec_syndrome` / `tc_pma_512b_slice` /
`tc_pma_922mhz` during `make cov` 与 Lint ECO 1/3–3/3 及后续闸后健康同类
（tool/timing）；对应 Icarus **PASS**（PMA slice/922 本跑 Icarus 仍 PASS）。
**不算** 上面三条 Icarus DUT 红。Icarus is the functional gate.

## 代码覆盖 (code)

Tool: Verilator **5.020** `--coverage-line --coverage-toggle`。合并 **58** 个
cluster `.dat`。`vibe_suite` 未 bind（VOQ OOM）。port/top compile rc=1
（OOM skipped, not waived）— 0 records。`vibe_rs128_120_dec.sv` 本跑 **13/13 LINE**。

LINE **726/761 = 95.4%** ≥ 95%。不追 LINE 100%。
相对闸后健康 `2026-09-21.md` 的 724/762 = 95.0%：分母随 PMA ECO
（删 `tx_pcs_d` / 加 `prbs23_word_ok`）变为 761；命中 726。
`vibe_pma_bnd.sv` 本跑 **16/21 = 76.2%**（健康夜 14/22）。
`cov_report.py` 文案里残留的 `708/737 = 96.1%` 是历史硬编码，**不是** 本跑实测；
本跑以脚本打印的 `LINE 726/761 = 95.4%` 为准。

| Metric | Hit/tot | % | Gate |
|--------|--------:|--:|------|
| **LINE** | **726/761** | **95.4** | **≥95% + waiver — PASS** |
| **TOGGLE** | **9713/17879** | **54.3** | 无 80% 门槛；wide-bus unused bits + PRBS23 宽总线 |
| **FSM** | n/a | — | 无 VCS FSM engine；LINE 打在 state `case`/`if` |

不追 LINE 100%。

Waiver（点名，与本跑 `cov_report.md` uncovered LINE + `COVERAGE_HOLES.md` 一致，未改分类、未改 RTL）：

| File:line | Class |
|-----------|--------|
| `vibe_dll_tx.sv:98/:144` | **WAIVER / 防御** |
| `vibe_cfg_space.sv:96–99` | **WAIVER** — RW1C `hold_fall` HW-clear。设计 agreed，**不改** `rtl/` |
| `vibe_lmsm.sv:101–108` | **TOOL** (`tmr_load`) |
| `dec_lid` / dll_tx pad combo | **TOOL** |
| pcs rem/pend / g1 `:51` | **TB空洞 — WAIVE** |
| `vibe_xbar.sv:50 else` | **TB空洞 — WAIVE** — ECO lock-hold implicit else。Not a DUT fail。 |

本跑 uncovered LINE 与 Lint ECO 3/3 同类：`cfg_space` hold_fall `:96–99`、`dll_tx`
`:98/:144/:151/:155/:159`、`lmsm` `:101–108`、`pcs_rx` rem/pend、
`pcs_rx_amctl_lock` `:55–58`、`pcs_tx_g1` `:51`，plus ECO
`vibe_xbar.sv:50 else`（TB hole, waived）。未改 waiver 分类，未改 RTL。

### PMA ECO 后空洞（诚实分类；不追 LINE 100%）

`vibe_pma_bnd.sv` 本跑 **16/21 = 76.2%**。`cov_report.md` uncovered LINE：

| File:line | Class |
|-----------|--------|
| `vibe_pma_bnd.sv:50/:56` | **TOOL** — `prbs23_adv128` function body。与 `tmr_load` / `dec_lid` 同类：Verilator 5.020 对 automatic function 内联后 LINE 打不进。Icarus `tc_pma_512b_slice` **PASS**（idle 前进）。 |
| `vibe_pma_bnd.sv:81 if` / `:81 else` | **TOOL** — Verilator PMA cluster 仍 FAIL（LFSR / vld 切片，与健康夜同类）。Icarus 两 TC **PASS**。 |
| `vibe_pma_bnd.sv:115 else` | **TOOL** — RX 非复位支路（行号随 ECO 从旧 `:105/:116` 挪到 idle-PRBS 块后）。同一 Verilator PMA FAIL。**不是** 本跑新开的 waiver 类别。 |

未 chase LINE 100%。未改 TB / 未改 RTL。不因这些 TOOL 行开 issue。
功能红是 Icarus 三条 loopback，见失败列表。

## 功能覆盖 (functional)

无 `covergroup`。官方 **159** 个 TP-0.3 ID（`TP_TC_MATRIX.md`）：
MAPPED=106 ADDED=16 HOLE=9 NEG=28。**published 目标仍是 159**。
HOLE 九个保持 **非目标**（不重开、不改分类）。

| Class | Count | Status |
|-------|------:|--------|
| MAPPED + ADDED | 122 | **120 hit**；**2 miss**： **TP-PHY-001** = `tc_port_smoke` stock **FAIL**；**TP-PHY-012** = `tc_nw_pkt_pma_loopback` stock **FAIL**（0/100） |
| NEG | 28 | hit |
| HOLE（freeze SPEC **非目标**） | 9 | **waiver / 非目标** |
| **合计** | **159** | **157/159 published hit**（目标仍 159；不是 159/159） |

HOLE 九个（非目标，不重开计数）：TP-HOLE-G2, G3, G4, G5, G6, G8, G9, 010, 012.

UVM 矩阵列已改指向 `tb/vibe/uvm/pkg/*.svh`，但本跑 **未跑 xsim**；功能分仍按
Icarus 同名 TC / 官方 159 ID。

`tc_top_smoke` 的 `irq_logic` 本跑 FAIL，但同一 TP 已由 suite
`tc_rt10_must_drop` 等 **PASS** 打分；top 红记在 121/124 桶，不另减 published ID。

## 断言覆盖 (assertion)

**N/A** — 本 TB / DUT 无 SVA `assert` / `cover property` / `covergroup`。
仓库里仅有英文注释 “async assert”，不是断言 bins。

## 与连续全绿的关系

| Item | Note |
|------|------|
| 旧 3/3（`2026-09-03` ×3，freeze `32a7f5e0`） | **VOID** |
| CR-B re-gate 3/3（`2026-09-08-crb-*of3`，freeze `1ed4d350`） | **VOID** |
| Lint ECO 3/3（`2026-09-08-lint-eco-*of3`，freeze `982ddd0a`） | **CLOSED 历史保留**（PR68）。**不要** 续成 4/3 |
| 2026-09-09 … 2026-09-21 闸后健康 | **不是 4/3 / 不是 1/3**（含 PR110 `2026-09-21.md`） |
| 本次 | **「新系列 1/3」**（tip `a0eaff51` + 书面 DUT freeze `a658d141`；rtl/include vs freeze **empty**）— **不是闸后健康** — **不是 4/3** — **不是签核** |
| 新系列 2/3、3/3 | **未开始**（本 1/3 功能门 FAIL；等设计修 loopback 后再计） |

Path B impl gate stays **NOT PASS**. This report is PMA ECO 新系列 1/3, not signoff.
Handoff: 验证 → 芯片开发PM 验收（新系列 1/3，DUT=a658d141）。Leave merge for PM.
