# New-interface consecutive-green run 1 of 3 — 2026-09-03 (Asia/Shanghai)

**This is the accepted 1/3 start.** Chat PASS and overlay-`7a37cee0` runs do **not** count.

Tree: **origin/main SHA `32a7f5e0`** (PR21 RTL on main) + PR22 4-bit cmd checkers (cherry-pick `8d33e21`).
**Zero `rtl/` diffs** vs `32a7f5e0`. No overlay. Checkers unchanged after cherry-pick. CFG6 packing still **未知**.

| Item | Value |
|------|--------|
| `origin/main` | `32a7f5e0c3f04762aa27dae73b000e55773195da` |
| Subject | Merge pull request #21 from lukebest/cursor/as012-cfg-wr-rw1c-fd37 |
| TB | PR22 4-bit `cfg_wr_cmd` + Port Reset W1C (`data[0]==1`) |
| Gate | `make -C tb/vibe sim` on that tree (no `git checkout 7a37cee0 -- rtl`) |
| Tool | Icarus Verilog 12.0 |
| `SIM_EXIT` | **0** |
| Result | **PASS** |

## Pass / fail counts

| Bucket | pass | fail | compile_fail |
|--------|------|------|----------------|
| suite (`make suite`) | **27** | 0 | 0 |
| units (`make units`) | **85** | 0 | 0 |
| top (`make top`) | **1** (`tc_top_smoke`) | 0 | 0 |
| neg (`make neg`) | **11** | 0 | 0 |
| summarize | `TOTAL_PASS_LINES=164` | `TOTAL_FAIL_LINES=0` | — |

`SUITE pass=27 fail=0 ran=27` / `SUITE_RESULT PASS`.
`UNITS pass_files=85 fail_files=0 compile_fail=0`.
`PASS tc_top_smoke`. `PASS tc_port_rst_via_cfg` / `tc_identity_cfg_space` / `tc_mgmt`.

No FAIL. No GitHub issue opened.

New-interface consecutive-green is **1/3** (this file). Runs 2 and 3 are not started here.
