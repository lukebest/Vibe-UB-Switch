# 闸后夜间健康检查 — 2026-09-22 (Asia/Shanghai)

**这是闸后夜间健康检查，不是新的连续全绿 / 不是 4/3 / 不是新的 1/3。**
不要记成 **4/3**，也不要重开 3 次计数，也不要把本夜算成新系列的 **1/3**。
**不是签核。**
**不是 pyuvm / PR119 验收门。**

**本夜是 #115 idle-mark ECO（DUT `302ac943`）落地之后的第一次闸后健康快照，不是新系列。**
书面 freeze 在 docs 里仍写 `a658d141`，直到 draft **PR120** merge — **本夜未 checkout PR120**。
DUT 内容以 RTL tip **`302ac943`** 为准（#115 idle-mark）。
Checkout tip `932037f9` = PR118 post-#115 official lint+CDC clean。
相对 DUT `302ac943`，tip 只前进 docs / reports / `tb/`（含 PR119 uvm-python）。
**`git diff 302ac943...HEAD -- rtl/` EMPTY；`-- include/` EMPTY。**

Lint ECO consecutive-green **3/3 已关闭**（`reports/regress/2026-09-08-lint-eco-1of3.md` /
`2026-09-08-lint-eco-2of3.md` / `2026-09-08-lint-eco-3of3.md`；PR68）。
PMA ECO 新系列 **1/3 FAIL**（`reports/regress/2026-09-21-pma-eco-1of3.md`；PR114；DUT `a658d141`）
保持 **FAIL / consecutive green 0/3**。本夜 **不** 把它续成 2/3，也 **不** 重开 1/3。
该计数保持关闭/冻结。本文件只做闸后健康快照。

旧系列 **VOID**，不要对照：
- `32a7f5e0` 夜间健康 / 2026-09-03 3/3（DUT 端口已改名）
- CR-B `1ed4d350` 系列（`2026-09-08-crb-*of3`；Lint ECO PR62 落地后作废）

基线对照：`reports/regress/2026-09-08-lint-eco-3of3.md`（Lint ECO 3/3 关闭记录）。
上一夜闸后健康：`reports/regress/2026-09-21.md`（PR110；**ECO-before #115**；**不是 4/3 / 不是 1/3**）。
PMA ECO 1/3 FAIL：`reports/regress/2026-09-21-pma-eco-1of3.md`（PR114；**是** 1/3；**不是** 本夜）。
本夜量的是 **#115 修后** 的 DUT `302ac943`，**不** 重开连续全绿。

聊天 PASS 不算验收。本文件是给芯片开发PM 的 in-repo 记录。
进仓后 PM 即时验。Handoff: 验证 → 芯片开发PM 验收。

| Item | Value |
|------|--------|
| Label | **闸后夜间健康检查**（**不是** 新系列 1/3 / **不是** 4/3 / **不是** 签核 / **不是** pyuvm/PR119 门） |
| Checkout tip SHA | `932037f9dd1069a7ee7055f03822f599dbda234d`（PR118 post-#115 official lint+CDC clean；`git rev-parse HEAD`） |
| DUT RTL | `302ac943c3737c288f3af6c4857bb3a9b1683a26`（#115 idle-mark ECO；PR116 merge `df7c286e` 仅追溯） |
| 书面 DUT freeze | `a658d14163b0e66b3875ee42a7dcc8a10024ba51` — docs 仍钉在此，直到 draft **PR120** merge。本夜 **未** checkout PR120，**未** 重钉 freeze |
| `git diff 302ac943...HEAD -- rtl/` | **EMPTY** |
| `git diff 302ac943...HEAD -- include/` | **EMPTY** |
| `git diff a658d141...HEAD -- rtl/` | **NON-EMPTY** — 仅 `rtl/pma/vibe_pma_bnd.sv`（+27/−16：`PMA_IDLE_MARK` decorate/undecorate）。书面 freeze 尚未跟 DUT |
| `git diff a658d141...HEAD -- include/` | **empty** |
| Tip-ahead vs DUT | docs / reports / `tb/` only（STATUS/RISKS、PR114 1/3 报告、post115 lint+CDC、PR119 `tb/vibe/pyuvm`）。**无** rtl/include |
| `rtl/` / `include/` / SPEC 本夜 | **未改**。无 overlay。默认 **不改 TB**（本夜零 TB / rtl 源码编辑） |
| PR120 | **未 checkout**。draft `docs: Xia freeze-pin RTL SHA 302ac943` 仍 open |
| TB on tip | 默认 `make -C tb/vibe sim` 在 PR119 后指向 **uvm-python**（Verilator/Icarus cocotb）。本夜功能门 **不是** 那条路径 |
| xsim / xvlog | **不可用** — `xvlog`/`xelab`/`xsim` 不在 PATH，无 Vivado Simulator license。记 **non-DUT**（缺工具） |
| 本夜功能门 | `make -C tb/vibe sim-icarus`（Icarus Verilog **12.0**；历史 124-bucket / PR114 同口径）— **本夜实测**（2026-09-22 02:23–02:25 CST） |
| Cov | `make -C tb/vibe cov`（Verilator **5.020**；与 xsim / pyuvm 无关）— **本夜实测**（2026-09-22 02:25–02:30 CST） |
| `SIM_ICARUS_EXIT` | **0** |
| `XSIM_MAKE_EXIT` | **n/a**（本夜未跑 `make sim`：默认已是 pyuvm，不是本夜口径；`xvlog` 仍不在 PATH） |
| `COV_EXIT` | **0** |
| 通过率 | **124/124 buckets = 100%**（suite 27 + units 85 + top 1 + neg 11；0 fail） |
| Result | **PASS** — LINE **726/761 = 95.4% ≥ 95% + waiver**；功能 published **159/159**；断言 **N/A** |
| `ovf_l` | F1 永久冻结 CDC-WARN — 本夜不为此开 issue |
| 本报告 PR | **https://github.com/lukebest/Vibe-UB-Switch/pull/121** — reports-only；Leave open for 芯片开发PM |

## Pin confirm

- Checkout: tip `932037f9dd1069a7ee7055f03822f599dbda234d`（PR118 reports-only lint+CDC）。
  `git rev-parse HEAD` = 该值。
- DUT：`302ac943c3737c288f3af6c4857bb3a9b1683a26`（#115 idle-mark）。
  `git diff 302ac943c3737c288f3af6c4857bb3a9b1683a26...HEAD -- rtl/` **EMPTY**。
  `git diff 302ac943c3737c288f3af6c4857bb3a9b1683a26...HEAD -- include/` **EMPTY**。
- 书面 freeze: `a658d14163b0e66b3875ee42a7dcc8a10024ba51` — docs 仍写此 SHA。
  **Do not re-pin freeze to tip in this reports PR.**
  **Do not checkout PR120.**
- `git diff a658d14163b0e66b3875ee42a7dcc8a10024ba51...HEAD -- rtl/` **NON-EMPTY**：
  仅 `rtl/pma/vibe_pma_bnd.sv`（`PMA_IDLE_MARK` XOR idle decorate / RX un-XOR）。
- `git diff a658d14163b0e66b3875ee42a7dcc8a10024ba51...HEAD -- include/` **empty**。
- F1 `ovf_l` untouched（Decision F1 stands）。
- `rtl/` 未搬家。本夜未改 `rtl/` / `tb/` / `include/` / SPEC / checkers。只新增本报告。
- PR119 `tb/vibe/pyuvm` 已在 tip 上，**本夜不跑、不验收**。

## 本夜命令（stock Icarus 口径 = 历史夜间 / PR114 时代）

历史夜间门曾经是 `make -C tb/vibe sim`（Icarus）然后 `make -C tb/vibe cov`。
**tip 已两改：** 先把默认 `sim` 指到 xsim+UVM，再被 PR119 指到 **uvm-python**。
README 仍把历史 124-bucket 写成 `make -C tb/vibe sim-icarus`。
`cov` 仍是 Verilator，不依赖 xsim / pyuvm。

本夜 **明确不跑** 默认 `make -C tb/vibe sim`（那是 pyuvm/PR119 门，不是本报告口径）。

本夜实际跑过：

```bash
# 1) 记录 xvlog / xsim 缺工具（non-DUT；本夜不跑默认 make sim）
command -v xvlog xelab xsim
# → not on PATH. XSIM miss = non-DUT（与 2026-09-16 … 2026-09-21 相同工具债）

# 2) 仓库支持的 stock Icarus 全回归（本夜功能门；与 PR114 同口径）
make -C tb/vibe sim-icarus
# → SIM_ICARUS_EXIT=0
#    SUITE 27/27 ; UNITS pass_files=85 fail_files=0 compile_fail=0
#    PASS tc_top_smoke ; NEG 11/11
#    TOTAL_PASS_LINES=158 TOTAL_FAIL_LINES=0
#    tc_nw_pkt_pma_loopback PASS 100/100（am_lock_end=1111 saw_fec_fail=0 saw_deskew=1）
#    tc_port_smoke PASS ; tc_top_smoke PASS

# 3) 覆盖率（与历史夜间相同 companion）
make -C tb/vibe cov
# → COV_EXIT=0 ; LINE 726/761 = 95.4% ; TOGGLE 9892/18391 = 53.8%
#    clusters=58（rsdec 进 merge；port/top OOM skip）
```

工具：Icarus Verilog **12.0 (stable)**（`12.0-2build2`）；
Verilator **5.020 2024-01-01**（Debian 5.020-1）。
未改 Makefile / 未加 overlay / 未 chase xsim / 未跑 pyuvm。
xsim/`xvlog` 缺工具 = **non-DUT fail**。

## 通过率

功能门 = **stock Icarus `sim-icarus`**（pyuvm / xsim 本夜不计入通过率）。

| Bucket | pass | fail | compile_fail | 通过率 |
|--------|------|------|----------------|--------|
| suite (`Makefile.icarus` suite) | **27** | 0 | 0 | **27/27 = 100%** |
| units (`run_units.sh`) | **85** | 0 | 0 | **85/85 = 100%** |
| top (`tc_top_smoke`) | **1** | 0 | 0 | **1/1 = 100%** |
| neg (`scan_absent.sh`) | **11** | 0 | 0 | **11/11 = 100%** |
| **合计（文件/桶）** | **124** | **0** | **0** | **124/124 = 100%** |
| summarize | `TOTAL_PASS_LINES=158` | `TOTAL_FAIL_LINES=0` | — | 0 fail |

`SUITE pass=27 fail=0 ran=27` / `SUITE_RESULT PASS`.
`UNITS pass_files=85 fail_files=0 compile_fail=0`.
`PASS tc_top_smoke`.

（`TOTAL_PASS_LINES` 是本夜 `summarize.sh` 对 `results/*.log` 的 `^PASS ` 行计数，
含多 PASS 的 unit log；`summarize.sh` 排除 `cov.log`。与 2026-09-21 / 2026-09-20
闸后健康的 158 对齐；对比 PR114 1/3 的 155 / 3 fail。）

Icarus is the functional gate tonight. pyuvm/PR119 is on tip but **not measured**.
xsim/UVM is documented as deprecated/secondary and **not measured**
（`xvlog` 缺失，不是 DUT 红）。

## 失败列表

**空。** 本夜 Icarus 门 0 FAIL / 0 compile_fail。无失败 TC，无波形，无新 GitHub issue。

先排除 TB / 工具：

- 默认 `make -C tb/vibe sim`：**未跑**。PR119 后它是 uvm-python 门，不是本夜口径。
  `xvlog` 仍不在 PATH — 记 **缺工具 / non-DUT**（与 2026-09-16 … 2026-09-21 相同）。
- `compile_fail=0`。本夜未改 `tb/`。
- #115 三条在 PR114（DUT `a658d141`）曾是 DUT 红；本夜 DUT=`302ac943` stock Icarus：
  `tc_nw_pkt_pma_loopback` **compile OK**，**PASS 100/100 packets**，
  `am_lock_end=1111`，`saw_fec_fail=0`，`saw_deskew=1`。
  `tc_port_smoke` **PASS**。`tc_top_smoke` **PASS**。
  `tc_pma_512b_slice` / `tc_pma_922mhz` Icarus **PASS**。
- idle-mark ECO **恢复** 了 GOLDEN 环回（相对 PR114 1/3 FAIL）。这是闸后健康确认，
  **不是** 新系列 1/3。

结论：**本夜无新 TB / DUT / TOOL 功能红。**
xsim/`xvlog` 缺工具 **不是** DUT mismatch。
pyuvm 未跑 **不是** 本夜红。
F1 `ovf_l` 仍为永久冻结 WARN，本夜不为此开 issue。
不 ECO rtl。不 checkout PR120。不重开 1/3。

Verilator coverage clusters still skip `vibe_suite` (VOQ OOM) and `port`/`top`
(`NOTE cov: cluster … compile rc=1 (timeout/OOM skipped, not waived)`)。
本夜 `rsdec` **进 merge**（58 clusters；与 2026-09-21 / PR114 相同）。
Icarus `tc_rs_dec_syndrome.log` 本夜 **PASS**。

Verilator-only `FAIL` lines on `tc_lmsm_walk` / `tc_pcs_fec_dual_enc` /
`tc_pcs_fec_t2` / `tc_route_lu` / `tc_rs_dec_syndrome` during `make cov`
是与 Lint ECO 1/3–3/3 及后续闸后健康相同的 tool/timing limits；
那些 TC **PASS** on Icarus。

**PRBS23 / idle-mark 后 Verilator-only FAIL（Icarus 门 PASS，不算功能红）：**
`tc_pma_512b_slice` / `tc_pma_922mhz` — Verilator PMA cluster 仍停
（LFSR 不前进 / `vld` 包不切到 lane 字）。Icarus 对应 `.log` 均为 `PASS`。
**TOOL**。Icarus is the functional gate.

## 代码覆盖 (code)

Tool: Verilator **5.020** `--coverage-line --coverage-toggle`。合并 **58** 个
cluster `.dat`（与 2026-09-21 / PR114 相同：`rsdec` 进 merge）。`vibe_suite` 未 bind
（VOQ OOM）。port/top compile rc=1（OOM skipped, not waived）— 0 records。
`vibe_rs128_120_dec.sv` 本夜 **13/13 LINE**。

LINE **726/761 = 95.4%** ≥ 95%。不追 LINE 100%。
相对 PR114 1/3（同一 LINE 726/761；DUT 当时是 `a658d141`）：命中与 LINE 分母相同。
TOGGLE **9892/18391 = 53.8%**（1/3 为 9713/17879 = 54.3%）— 分母随 idle-mark
XOR / 宽总线点增加，不是 LINE 门。
`vibe_pma_bnd.sv` 本夜 **16/21 = 76.2%**（与 PR114 相同命中/分母）。
`cov_report.py` 文案里残留的 `708/737 = 96.1%` 是历史硬编码，**不是** 本夜实测；
本夜以脚本打印的 `LINE 726/761 = 95.4%` 为准。

| Metric | Hit/tot | % | Gate |
|--------|--------:|--:|------|
| **LINE** | **726/761** | **95.4** | **≥95% + waiver — PASS** |
| **TOGGLE** | **9892/18391** | **53.8** | 无 80% 门槛；wide-bus unused bits + PRBS23 / idle-mark 宽总线 |
| **FSM** | n/a | — | 无 VCS FSM engine；LINE 打在 state `case`/`if` |

不追 LINE 100%。

Waiver（点名，与本夜 `cov_report.md` uncovered LINE + `COVERAGE_HOLES.md` 一致，未改分类）：

| File:line | Class |
|-----------|--------|
| `vibe_dll_tx.sv:98/:144` | **WAIVER / 防御** |
| `vibe_cfg_space.sv:96–99` | **WAIVER** — RW1C `hold_fall` HW-clear。设计 agreed，**不改** `rtl/` |
| `vibe_lmsm.sv:101–108` | **TOOL** (`tmr_load`) |
| `dec_lid` / dll_tx pad combo | **TOOL** |
| pcs rem/pend / g1 `:51` | **TB空洞 — WAIVE** |
| `vibe_xbar.sv:50 else` | **TB空洞 — WAIVE** — ECO lock-hold implicit else。Not a DUT fail。 |

本夜 uncovered LINE 与 Lint ECO 3/3 / PR114 同类：`cfg_space` hold_fall `:96–99`、`dll_tx`
`:98/:144/:151/:155/:159`、`lmsm` `:101–108`、`pcs_rx` rem/pend、
`pcs_rx_amctl_lock` `:55–58`、`pcs_tx_g1` `:51`，plus ECO
`vibe_xbar.sv:50 else`（TB hole, waived）。未改 waiver 分类，未改 RTL。

### #115 idle-mark 后空洞（诚实分类；不追 LINE 100%）

`vibe_pma_bnd.sv` 本夜 **16/21 = 76.2%**。本夜 `cov_report.md` uncovered LINE
（行号相对 PR114 随 `PMA_IDLE_MARK` 下移）：

| File:line | Class |
|-----------|--------|
| `vibe_pma_bnd.sv:51/:57` | **TOOL** — `prbs23_adv128` function body（PR114 记为 `:50/:56`）。与 `tmr_load` / `dec_lid` 同类：Verilator 5.020 对 automatic function 内联后 LINE 打不进。Icarus `tc_pma_512b_slice` 已打 idle 前进。 |
| `vibe_pma_bnd.sv:96 if` / `:96 else` | **TOOL** — `end else if (afifo_pma_lane_vld)` / idle fill（PR114 记为 `:81`）。Verilator PMA cluster 本夜 FAIL。Icarus 两 TC **PASS**。**不是 DUT 死代码。** |
| `vibe_pma_bnd.sv:126 else` | **TOOL** — RX 非复位支路（PR114 记为 `:115`）。同一 Verilator PMA FAIL。Icarus loopback **PASS 100/100**。 |

未 chase LINE 100%。未改 TB / 未改 RTL。无本夜新增空洞**类别**（仍是 TOOL / 已有 waiver）。
不因此开 issue。

## 功能覆盖 (functional)

无 `covergroup`。官方 **159** 个 TP-0.3 ID（`TP_TC_MATRIX.md`）：
MAPPED=106 ADDED=16 HOLE=9 NEG=28。打分 TC 均在本夜 stock `sim-icarus` 中跑过。

| Class | Count | Status |
|-------|------:|--------|
| MAPPED + ADDED | 122 | hit（含 **TP-PHY-001** = `tc_port_smoke` stock **PASS**；**TP-PHY-012** = `tc_nw_pkt_pma_loopback` stock **PASS 100/100**） |
| NEG | 28 | hit |
| HOLE（freeze SPEC **非目标**） | 9 | **waiver / 非目标** |
| **合计** | **159** | **100% of published** |

HOLE 九个（非目标，不重开计数）：TP-HOLE-G2, G3, G4, G5, G6, G8, G9, 010, 012.

UVM / pyuvm 矩阵列已在 tip 上，但本夜 **未跑 xsim、未跑 pyuvm**；功能分仍按
Icarus 同名 TC / 官方 159 ID。#115 后 TP-PHY-001 / TP-PHY-012 回 stock **PASS**
（PR114 曾 2 MAPPED miss）。不算新 1/3 / 不算 reopen consecutive green。

## 断言覆盖 (assertion)

**N/A** — 本 TB / DUT 无 SVA `assert` / `cover property` / `covergroup`。
仓库里仅有英文注释 “async assert”，不是断言 bins。

## 与连续全绿的关系

| Item | Note |
|------|------|
| 旧 3/3（`2026-09-03` ×3，freeze `32a7f5e0`） | **VOID** |
| CR-B re-gate 3/3（`2026-09-08-crb-*of3`，freeze `1ed4d350`） | **VOID** |
| Lint ECO 3/3（`2026-09-08-lint-eco-*of3`，freeze `982ddd0a`） | **CLOSED**（PR68） |
| 2026-09-09 … 2026-09-21 闸后健康 | **不是 4/3 / 不是 1/3**（PR70 / PR75 / PR80 / PR84 / PR86 / PR88 / PR92 / PR94 / PR97 / PR101 / PR105 / PR107 / PR110） |
| PMA ECO 新系列 1/3 | **FAIL**（PR114；DUT `a658d141`；121/124；3 DUT fail）— consecutive green **0/3**。本夜 **不是** 它的 2/3 |
| 本次 | **闸后夜间健康检查**（tip `932037f9` + DUT `302ac943` + 书面 freeze 仍 `a658d141`；**PR120 未 checkout**；**#115 后第一次健康**）— **不是 4/3** — **不是新 1/3** — **不是签核** — **不是 pyuvm/PR119 门** |
| 新系列？ | **否** — 不要重开 3 次计数。书面 freeze 未重钉。2/3 **仍 held** 直到 Xia/PR120 重钉后由 PM 另开 |

`rtl/` / `tb/` / `include/` / SPEC / checkers 均未改。只新增本报告。无 sidecar。
本报告 PR：**https://github.com/lukebest/Vibe-UB-Switch/pull/121**（reports-only）。
Handoff: 验证 → 芯片开发PM（进仓后即时验）。Leave merge for PM.
