# Nightly regress — 2026-09-04 (Asia/Shanghai)

**Routine nightly snapshot.** Consecutive-green **3/3 already complete** on 2026-09-03
(`2026-09-03.md`, `-run2.md`, `-run3.md`). RTL unchanged vs freeze SHA `32a7f5e0`,
so that count stays closed. This file does **not** start a new 3-series.

Chat PASS is not acceptance. This file is the in-repo nightly record for 芯片开发PM.

| Item | Value |
|------|--------|
| `origin/main` | `54304a4ae974c8061b06076a00ee4ab32dc52be1` (PR28 SPEC freeze docs) |
| DUT RTL | freeze SHA `32a7f5e0c3f04762aa27dae73b000e55773195da` (PR21) |
| `git diff` `rtl/` vs `32a7f5e0` | **zero** |
| TB | main (PR22 / PR25 4-bit `cfg_wr_cmd` + Port Reset W1C). No overlay. |
| Sim | `make -C tb/vibe sim` (Icarus Verilog **12.0**) — **re-measured** |
| Cov | `make -C tb/vibe cov` (`run_verilator_cov.sh`, Verilator **5.020**) — **re-measured** |
| `SIM_EXIT` | **0** |
| `COV_EXIT` | **0** |
| Result | **PASS** — LINE **708/737 = 96.1% ≥ 95% + waiver**; FUNC **159/159 published** |

## Sim pass / fail

| Bucket | pass | fail | compile_fail |
|--------|------|------|----------------|
| suite (`make suite`) | **27** | 0 | 0 |
| units (`make units`) | **85** | 0 | 0 |
| top (`make top`) | **1** (`tc_top_smoke`) | 0 | 0 |
| neg (`make neg`) | **11** | 0 | 0 |
| summarize | `TOTAL_PASS_LINES=158` | `TOTAL_FAIL_LINES=0` | — |

`SUITE pass=27 fail=0 ran=27` / `SUITE_RESULT PASS`.
`UNITS pass_files=85 fail_files=0 compile_fail=0`.

(`TOTAL_PASS_LINES` is `summarize.sh` `^PASS ` across this run’s `results/*.log`,
including multi-PASS unit logs. Not copied from 2026-09-03.)

## 代码覆盖 (code)

Tool: Verilator **5.020** `--coverage-line --coverage-toggle`. 58 cluster `.dat`
merged. Per-module clusters (`vibe_suite` never bound — VOQ OOM). Port/top
compile rc=1 (OOM skipped, not waived) — 0 records.

| Metric | Hit/tot | % | Gate |
|--------|--------:|--:|------|
| **LINE** | **708/737** | **96.1** | **≥95% + waiver — PASS** |
| **TOGGLE** | **9538/17260** | **55.3** | no 80% gate; wide-bus unused bits |
| **FSM** | n/a | — | no VCS FSM engine; LINE on state `case`/`if` |

Do **not** chase LINE 100%.

Waivers: `tb/vibe/results/COVERAGE_HOLES.md` (unchanged). This run’s uncovered
LINE points match that file:

| File:line | Class |
|-----------|--------|
| `vibe_dll_tx.sv:98/:144` | **WAIVER / 防御** |
| `vibe_cfg_space.sv:96–99` | **WAIVER** — RW1C `hold_fall` HW-clear. 设计 agreed, **不改** `rtl/`. Not DUT dead. |
| `vibe_lmsm.sv:101–108` | **TOOL** (`tmr_load`) |
| `dec_lid` / dll_tx pad combo | **TOOL** |
| pcs rem/pend / g1 `:51` | **TB空洞 — WAIVE** |

## 功能覆盖 (functional)

No `covergroup`. Official **159 TP-0.3 IDs** (`TP_TC_MATRIX.md`):
MAPPED=106 ADDED=16 HOLE=9 NEG=28. Scoring TCs all ran under this `make sim`.

| Class | Count | Status |
|-------|------:|--------|
| MAPPED + ADDED | 122 | hit |
| NEG | 28 | hit |
| HOLE (freeze SPEC **非目标**) | 9 | **waiver / 非目标** |
| **Sum** | **159** | **100% of published** |

HOLE nine: TP-HOLE-G2, G3, G4, G5, G6, G8, G9, 010, 012.

## 断言覆盖 (assertion)

**N/A — no SVA `assert` / `cover property` / `covergroup` in this TB or DUT.**
Repo grep hits are English comments (“async assert”) only, not assertion bins.

## Nightly vs 3/3 series

| Item | Note |
|------|------|
| 3/3 closed | 2026-09-03 in-repo files; RTL still `32a7f5e0` |
| This file | nightly snapshot on `origin/main` `54304a4` + same RTL |
| New series? | **No** |

No FAIL. No GitHub issue. Checkers / `rtl/` / SPEC unchanged. Only this report
is added.

Handoff: 验证 → 芯片开发PM.
