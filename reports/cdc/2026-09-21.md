# Nightly CDC 2026-09-21 (SGT)

- **When:** 2026-09-21 02:28 SGT (**first official post-PR109 ECO clean**)
- **DUT / tip SHA:** `f234d4b036a1639fa9855de01743d04696bf5cb7` (origin/main HEAD = PR109 merge). RTL bits = ECO tip `a658d14163b0e66b3875ee42a7dcc8a10024ba51`. Audit used **tip tree as-is** (no freeze overlay).
- **Written freeze:** `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**. **Not re-pinned to tip.** (Xia re-pin to `a658d141` after this clean report — not this PR.)
- **`git diff 982ddd0a...HEAD -- rtl/`:** **NON-EMPTY** — `vibe_pma_bnd.sv` (PRBS23 + dest-domain resets + PR109 idle-PRBS drop, +106/−11 vs freeze), `vibe_port.sv` (`.txrst_n(txrst_n), .rxrst_n(rxrst_n)`, +3/−3). Combined **+109/−14**. `-- include/` **empty**.
- **`git diff a658d141...HEAD -- rtl/ include/`:** **empty**
- **`git diff 90251fdf...HEAD -- rtl/`:** `vibe_pma_bnd.sv` only (+26/−19) vs last official CDC tip (2026-09-20 / PR108).
- **main HEAD checked:** `f234d4b036a1639fa9855de01743d04696bf5cb7`
- **Tool:** structural RTL audit (no SpyGlass / Ascent / RDC on this box)
- **vs 2026-09-20 CDC:** same domain / AFIFO / F1 `ovf_l` WARN. PR109 **closed** the PMA loopback `txclk`→`rxclk` compare / `tx_pcs_d` sample (was NEW on 2026-09-19; carry-forward on 2026-09-20). **CDC NEW = 0**. Prior nightlies on DUT `32a7f5e0` are **VOID**.

## Clock domains

| Domain | Source | Who |
|--------|--------|-----|
| `clk_fab` | top | fabric, mgmt, LMSM/DLL/NW/PCS, AFIFO write-TX / read-RX |
| `txclk_0..3` | per-port PMA TX | AFIFO read-TX, 160→128 gear, PMA TX slice **+ PRBS23 LFSR / `pcs_pma_txdata`** |
| `rxclk_0..3` | per-port PMA RX | PMA RX slice **+ `rxrst_n` async reset / local idle-PRBS `vld` drop**, change-detect, AFIFO write-RX |

## Sanctioned CDC cells (`rtl/cdc/`)

| Cell | Role | Recognized |
|------|------|------------|
| `vibe_sync2` | 2-FF gray pointer | yes (`vibe_afifo` r2w + w2r) |
| `vibe_afifo` | gray-pointer AFIFO depth 16, AFULL occ>=10 | yes (4 TX + 4 RX per `vibe_port`) |
| `vibe_rst_sync` | async assert, sync deassert | yes (`vibe_port` `u_txrst`/`u_rxrst`) |
| `vibe_gear_160_128` / `vibe_gear_128_160` | single-clock ratio, not a CDC cell | used only after/before AFIFO in the dest domain |

## Data-path crossings (all through AFIFO)

- TX: PCS 160b @`clk_fab` → `vibe_afifo` (wclk=`clk_fab`, rclk=`txclk`) → gear @`txclk` → `vibe_pma_bnd` TX (`afifo_pma_lane*` / `afifo_pma_lane_vld` already `txclk`)
- RX: `vibe_pma_bnd` RX @`rxclk` → change-detect @`rxclk` → `vibe_afifo` (wclk=`rxclk`, rclk=`clk_fab`) → gear @`clk_fab` → PCS

Reset into PMA clocks: `rst_n && !port_rst` → `vibe_rst_sync` (`u_txrst` / `u_rxrst`). No other `txclk`/`rxclk` sequential fan-in from `clk_fab` except AFIFO gray pointers.

## PRBS23 / `txrst_n` (unchanged vs 2026-09-20)

- `vibe_port.sv:165` still ties PMA `.txrst_n(txrst_n)`. `txrst_n` is the `u_txrst` output (already dest-domain `txclk`). Same net already resets TX AFIFO `rrst_n` and 160→128 gears. **Sanctioned reset, not a new crossing.**
- `vibe_pma_bnd.sv` TX process remains `always @(posedge txclk or negedge txrst_n)` with PRBS23 LFSR / idle fill **local to `txclk`**. `afifo_pma_lane*` remain `txclk` data.
- Official lint `%Error-UNSUPPORTED` on `txrst_n = 1'b1` is **GONE** (defaults removed). That was a lint default-value issue, not a CDC-WARN.

## `rxrst_n` / idle-PRBS drop (post-PR109 — loopback crossing closed)

- `vibe_port.sv:165` still ties PMA `.rxrst_n(rxrst_n)`. `rxrst_n` is the `u_rxrst` output (already dest-domain `rxclk`). Same net already resets RX AFIFO `wrst_n` and the `rxclk` change-detect / `ovf_l` processes. **Sanctioned reset, not a new crossing.**
- `vibe_pma_bnd.sv` RX process remains `always @(posedge rxclk or negedge rxrst_n)` with dest-domain reset of the RX slice / `pma_afifo_lane_vld`. Reset itself is **not** a new crossing.
- Official lint `%Error-UNSUPPORTED` on `rxrst_n = 1'b1` is **GONE** (defaults removed). Lint only; not a CDC-WARN.
- **GONE structure (was CDC NEW on 2026-09-19):** `tx_pcs_d` (`txclk` delayed `afifo_pma_lane_vld`) and the `pma_pcs_rxdata === pcs_pma_txdata` compare inside the `rxclk` process are **removed**. `tx_pcs_d` does not exist as a net (comment only).
- **Replacement (local to `rxclk`):** `idle_prbs` is a combo check of `pma_pcs_rxdata` against the PRBS23 recurrence `w[i] == w[i-23] ^ w[i-18]` (same poly as the TX idle fill). The RX process loads `pma_afifo_lane_vld <= !idle_prbs`. Fan-in is the RX pin + dest-domain reset only. **No `txclk` sequential is sampled.** Same-clock loopback intent is no longer implemented as a crossing.

## Findings

### CDC-WARN (F1 frozen — no ECO, no issue)

- `rtl/port/vibe_port.sv:244` — same always as freeze: 2-FF (`ovf_s1`/`ovf_s2`) into `clk_fab` as `afifo_ovf`; `|ovf_l` sample still at **:249**. `|ovf_l` is a 1-cycle `rxclk` sticky/pulse OR. No pulse stretcher / req-ack. **F1** permanent frozen WARN. **Unchanged** vs 2026-09-20 / freeze `982ddd0a`. **Not ECO'd.**

### CDC-WARN (loopback `txclk`→`rxclk`) — **GONE**

- Pre-ECO `rtl/pma/vibe_pma_bnd.sv:116` (`pma_pcs_rxdata === pcs_pma_txdata` then `pma_afifo_lane_vld <= tx_pcs_d`) is not in this tree. Closed by PR109. **Not NEW.** **Not remaining.**

**CDC-WARN count:** 1 (`ovf_l` F1 only)  
**NEW CDC findings:** 0  
**GONE vs 2026-09-20:** loopback `txclk`→`rxclk`  
No CDC-ERROR. No new unsynchronized multi-bit bus from `clk_fab` into PMA clocks. No new pulse/2FF besides F1.

No RTL/SPEC/TB change. No issue opened.
No signoff claim. Not 4/3.
