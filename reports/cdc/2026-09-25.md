# Nightly CDC 2026-09-25 (SGT)

- **When:** 2026-09-25 02:54 SGT (**official nightly after Decision I stage-36 `vibe_icrc` / PR200 tip**)
- **DUT / tip SHA:** `2b4a8408abba14e3fcd0208fadd2c54cc97c3c23` (origin/main HEAD = PR200 Decision I stage-36 `vibe_icrc` merge). Expected pin at task start was this SHA. **Tip did not move.** Audit used **tip tree as-is** (no freeze overlay). RTL bits vs prior official DUT `62cd3c88` are **`rtl/nw/vibe_icrc.sv` headers only**. Functional PMA / port / AFIFO crossings unchanged vs last historical pin and vs 09-24. Tip **includes** Decision I leaves through stage-36 — expected.
- **Written freeze:** **UNFROZEN** (Decision I). Last historical pin `302ac943c3737c288f3af6c4857bb3a9b1683a26` is **VOID as current**. **Not re-pinned tonight.** Do not edit SPEC/CHANGELOG/STATUS freeze text here. Path B **hold**.
- **`git diff 62cd3c88...HEAD -- rtl/ include/`:** **NON-EMPTY** — 1 file, +6/−0 (`vibe_icrc` headers). `-- include/` **empty**. `-- rtl/port` **empty** (F1 `ovf_l` untouched). `-- rtl/cdc` **empty**.
- **`git diff 302ac943...HEAD -- rtl/ include/`:** **NON-EMPTY** — 36 files, +255/−10 (Decision I headers including tonight's `vibe_icrc`; `vibe_rs128_120_dec` NBA unroll already on main). `-- include/` **empty**. `-- rtl/port` **empty**. `-- rtl/cdc` headers only on all five cells (same as 09-24).
- **main HEAD checked:** `2b4a8408abba14e3fcd0208fadd2c54cc97c3c23`
- **Tool:** structural RTL audit (no SpyGlass / Ascent / RDC on this box) — same method as [`2026-09-24.md`](2026-09-24.md) / nightly (no project CDC runner; inspect `rtl/pma/vibe_pma_bnd.sv` + `rtl/port/vibe_port.sv` + `rtl/cdc/`)
- **vs 2026-09-24 CDC:** same domain / AFIFO / F1 `ovf_l` WARN. Loopback `txclk`→`rxclk` stays **GONE**. Idle-mark is still localparam + same-domain combo XOR. Stage-36 `vibe_icrc` is a single-clock `clk`/`rst_n` CRC leaf (used only by `cna_ep` @`clk_fab`) — **not** a CDC cell and **not** a new crossing. Decision I headers on that leaf did **not** reopen `txclk`→`rxclk` and did **not** change gray-pointer / dest-domain reset. `posedge txclk` / `posedge rxclk` sequentials remain only in `vibe_pma_bnd` + `vibe_port`. **CDC NEW = 0**. Prior nightlies on DUT `32a7f5e0` are **VOID**.

## Clock domains

| Domain | Source | Who |
|--------|--------|-----|
| `clk_fab` | top | fabric, mgmt, LMSM/DLL/NW/PCS, AFIFO write-TX / read-RX |
| `txclk_0..3` | per-port PMA TX | AFIFO read-TX, 160→128 gear, PMA TX slice **+ PRBS23 LFSR / `pcs_pma_txdata` / `PMA_IDLE_MARK` XOR idle fill** |
| `rxclk_0..3` | per-port PMA RX | PMA RX slice **+ `rxrst_n` async reset / local idle-PRBS `vld` drop (un-XOR mark then recurrence)**, change-detect, AFIFO write-RX |

## Sanctioned CDC cells (`rtl/cdc/`)

| Cell | Role | Recognized |
|------|------|------------|
| `vibe_sync2` | 2-FF gray pointer | yes (`vibe_afifo` r2w + w2r; unchanged vs `62cd3c88`) |
| `vibe_afifo` | gray-pointer AFIFO depth 16, AFULL occ>=10 | yes (4 TX + 4 RX per `vibe_port`; unchanged vs `62cd3c88`) |
| `vibe_rst_sync` | async assert, sync deassert | yes (`vibe_port` `u_txrst`/`u_rxrst`; unchanged vs `62cd3c88`) |
| `vibe_gear_160_128` / `vibe_gear_128_160` | single-clock ratio, not a CDC cell | used only after/before AFIFO in the dest domain; unchanged vs `62cd3c88` |

## Data-path crossings (all through AFIFO)

- TX: PCS 160b @`clk_fab` → `vibe_afifo` (wclk=`clk_fab`, rclk=`txclk`) → gear @`txclk` → `vibe_pma_bnd` TX (`afifo_pma_lane*` / `afifo_pma_lane_vld` already `txclk`)
- RX: `vibe_pma_bnd` RX @`rxclk` → change-detect @`rxclk` → `vibe_afifo` (wclk=`rxclk`, rclk=`clk_fab`) → gear @`clk_fab` → PCS

Reset into PMA clocks: `rst_n && !port_rst` → `vibe_rst_sync` (`u_txrst` / `u_rxrst`). No other `txclk`/`rxclk` sequential fan-in from `clk_fab` except AFIFO gray pointers.

## PRBS23 / `txrst_n` (unchanged vs 2026-09-24)

- `vibe_port.sv:165` still ties PMA `.txrst_n(txrst_n)`. `txrst_n` is the `u_txrst` output (already dest-domain `txclk`). Same net already resets TX AFIFO `rrst_n` and 160→128 gears. **Sanctioned reset, not a new crossing.**
- `vibe_pma_bnd.sv` TX process remains `always @(posedge txclk or negedge txrst_n)` with PRBS23 LFSR **local to `txclk`**. Idle fill is `prbs ^ PMA_IDLE_MARK` (localparam constant). `afifo_pma_lane*` remain `txclk` data. **Not a crossing.** Stage-36 `vibe_icrc` headers do not change fan-in.
- Official lint `%Error-*` stays **0**. No new lint key on the mark, the CDC cells, or `vibe_icrc` (still zero primary keys). See [`../lint/2026-09-25.md`](../lint/2026-09-25.md).

## `rxrst_n` / idle-PRBS drop (still no `txclk` sample)

- `vibe_port.sv:165` still ties PMA `.rxrst_n(rxrst_n)`. `rxrst_n` is the `u_rxrst` output (already dest-domain `rxclk`). Same net already resets RX AFIFO `wrst_n` and the `rxclk` change-detect / `ovf_l` processes. **Sanctioned reset, not a new crossing.**
- `vibe_pma_bnd.sv` RX process remains `always @(posedge rxclk or negedge rxrst_n)` with dest-domain reset of the RX slice / `pma_afifo_lane_vld`. Reset itself is **not** a new crossing.
- **Still GONE (not reopened):** `tx_pcs_d` and the `pma_pcs_rxdata === pcs_pma_txdata` compare are **absent**. `tx_pcs_d` does not exist as a net (comment only). Idle-mark / Decision I / stage-36 `vibe_icrc` headers did **not** restore a `txclk`→`rxclk` sample. Confirmed on tonight's tip (`2b4a8408`).
- **Idle-mark (local to `rxclk`):** `idle_prbs` is a combo check of `pma_pcs_rxdata ^ PMA_IDLE_MARK` against the PRBS23 recurrence `w[i] == w[i-23] ^ w[i-18]`. Fan-in is the RX pin + dest-domain reset only. **No `txclk` sequential is sampled.**
- **Not NEW:** pyCircuit GENERATED/HAND-FINISHED headers on `vibe_icrc` do not add a domain crossing. `vibe_icrc` is single-clock (`clk`/`rst_n`); not instantiated on PMA clocks.

## Findings

### CDC-WARN (F1 frozen — no ECO, no issue)

- `rtl/port/vibe_port.sv:244` — same always as 2026-09-24 / last historical pin: 2-FF (`ovf_s1`/`ovf_s2`) into `clk_fab` as `afifo_ovf`; `|ovf_l` sample still at **:249**. `|ovf_l` is a 1-cycle `rxclk` sticky/pulse OR. No pulse stretcher / req-ack. **F1** permanent frozen WARN. **Unchanged**. **Not ECO'd.**

### CDC-WARN (loopback `txclk`→`rxclk`) — **still GONE**

- Pre-ECO `pma_pcs_rxdata === pcs_pma_txdata` then `pma_afifo_lane_vld <= tx_pcs_d` is not in this tree. Closed by PR109. PR116 idle-mark / Decision I leaves / stage-36 `vibe_icrc` did not restore it. Tonight's re-audit on tip `2b4a8408` did not reopen it. **Not NEW.** **Not remaining.**

**CDC-WARN count:** 1 (`ovf_l` F1 only)  
**NEW CDC findings:** 0  
**GONE vs 2026-09-24:** none (loopback already GONE)  
No CDC-ERROR. No new unsynchronized multi-bit bus from `clk_fab` into PMA clocks. No new pulse/2FF besides F1. No new `txclk`→`rxclk` / unsanctioned PMA loopback CDC from Decision I / stage-36 headers.

No RTL/SPEC/TB change in this reports PR. No issue opened.
F1 `ovf_l` **untouched** / **not ECO'd**.
Decision I remains **UNFROZEN**. Path B **hold**.
No signoff claim. Not 4/3. Not new 1/3.
