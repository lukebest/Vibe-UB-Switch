# Nightly CDC 2026-09-23 (Asia/Shanghai)

- **When:** 2026-09-23 03:21 CST / Asia/Shanghai (**official nightly after Decision I UNFROZEN + PR130 `vibe_pma_bnd` leaf**)
- **DUT / tip SHA:** `f001833b3c4507620eccd44895df06c218b1165f` (origin/main HEAD = Merge PR130 Decision I stage-2 `vibe_pma_bnd`). `git rev-parse HEAD` **MATCHES** the requested checkout tip. Audit used **tip tree as-is** (no freeze overlay; no re-pin).
- **Written freeze:** **UNFROZEN** (Luke Decision I). Last historical pin `302ac943c3737c288f3af6c4857bb3a9b1683a26` is **VOID as current**. **Not re-pinned tonight.** Do not edit SPEC/CHANGELOG/STATUS freeze text here.
- **`git diff 302ac943...HEAD -- rtl/`:** **NON-EMPTY** — `rtl/cdc/vibe_afifo.sv` (+7 comment header), `rtl/pma/vibe_pma_bnd.sv` (+7 comment header). Functional AFIFO / PMA body vs `302ac943` is **unchanged**. `-- include/` **empty**. `-- rtl/port` **empty**.
- **`git diff d88442d2...HEAD -- rtl/ include/`:** **NON-EMPTY** — same two Decision I header leaves (`+7/+7`) vs yesterday's lint/CDC tip `d88442d2aad29ecaaabdb3f70826baa2d87999a9`.
- **main HEAD checked:** `f001833b3c4507620eccd44895df06c218b1165f`
- **Tool:** structural RTL audit (no SpyGlass / Ascent / RDC on this box) — same method as [`2026-09-22.md`](2026-09-22.md) / nightly (no project CDC runner; inspect `rtl/pma/vibe_pma_bnd.sv` + `rtl/port/vibe_port.sv` + `rtl/cdc/` + Decision I leaf diffs)
- **vs 2026-09-22 CDC:** same domain / AFIFO / F1 `ovf_l` WARN. Loopback `txclk`→`rxclk` stays **GONE**. Decision I headers are comments only — they did **not** reopen `txclk`→`rxclk` and did **not** add a crossing. **CDC NEW = 0**. Prior nightlies on DUT `32a7f5e0` are **VOID**.

## Clock domains

| Domain | Source | Who |
|--------|--------|-----|
| `clk_fab` | top | fabric, mgmt, LMSM/DLL/NW/PCS, AFIFO write-TX / read-RX |
| `txclk_0..3` | per-port PMA TX | AFIFO read-TX, 160→128 gear, PMA TX slice **+ PRBS23 LFSR / `pcs_pma_txdata` / `PMA_IDLE_MARK` XOR idle fill** |
| `rxclk_0..3` | per-port PMA RX | PMA RX slice **+ `rxrst_n` async reset / local idle-PRBS `vld` drop (un-XOR mark then recurrence)**, change-detect, AFIFO write-RX |

## Sanctioned CDC cells (`rtl/cdc/`)

| Cell | Role | Recognized |
|------|------|------------|
| `vibe_sync2` | 2-FF gray pointer | yes (`vibe_afifo` r2w + w2r) |
| `vibe_afifo` | gray-pointer AFIFO depth 16, AFULL occ>=10 | yes (4 TX + 4 RX per `vibe_port`). Decision I added a pyCircuit provenance banner only; pointers / `vibe_sync2` / AFULL `occ>=10` **unchanged** |
| `vibe_rst_sync` | async assert, sync deassert | yes (`vibe_port` `u_txrst`/`u_rxrst`) |
| `vibe_gear_160_128` / `vibe_gear_128_160` | single-clock ratio, not a CDC cell | used only after/before AFIFO in the dest domain |

## Data-path crossings (all through AFIFO)

- TX: PCS 160b @`clk_fab` → `vibe_afifo` (wclk=`clk_fab`, rclk=`txclk`) → gear @`txclk` → `vibe_pma_bnd` TX (`afifo_pma_lane*` / `afifo_pma_lane_vld` already `txclk`)
- RX: `vibe_pma_bnd` RX @`rxclk` → change-detect @`rxclk` → `vibe_afifo` (wclk=`rxclk`, rclk=`clk_fab`) → gear @`clk_fab` → PCS

Reset into PMA clocks: `rst_n && !port_rst` → `vibe_rst_sync` (`u_txrst` / `u_rxrst`). No other `txclk`/`rxclk` sequential fan-in from `clk_fab` except AFIFO gray pointers.

## Decision I leaves (comment headers only — not a crossing)

- `vibe_afifo.sv:1-7` and `vibe_pma_bnd.sv:1-7` are `GENERATED/HAND-FINISHED` banners (pyCircuit @ `43cc5918`). No port, clock, reset, or pointer change vs `302ac943`.
- Official lint `%Error-*` stays **0**. No new lint key on either leaf.

## PRBS23 / `txrst_n` (unchanged vs 2026-09-22)

- `vibe_port.sv:165` still ties PMA `.txrst_n(txrst_n)`. `txrst_n` is the `u_txrst` output (already dest-domain `txclk`). Same net already resets TX AFIFO `rrst_n` and 160→128 gears. **Sanctioned reset, not a new crossing.**
- `vibe_pma_bnd.sv` TX process remains `always @(posedge txclk or negedge txrst_n)` (now `:93` after the +7 header; same process as yesterday) with PRBS23 LFSR **local to `txclk`**. Idle fill is `prbs ^ PMA_IDLE_MARK` (localparam constant). `afifo_pma_lane*` remain `txclk` data. **Not a crossing.**

## `rxrst_n` / idle-PRBS drop (still no `txclk` sample)

- `vibe_port.sv:165` still ties PMA `.rxrst_n(rxrst_n)`. `rxrst_n` is the `u_rxrst` output (already dest-domain `rxclk`). Same net already resets RX AFIFO `wrst_n` and the `rxclk` change-detect / `ovf_l` processes. **Sanctioned reset, not a new crossing.**
- `vibe_pma_bnd.sv` RX process remains `always @(posedge rxclk or negedge rxrst_n)` (now `:132`) with dest-domain reset of the RX slice / `pma_afifo_lane_vld`. Reset itself is **not** a new crossing.
- **Still GONE (not reopened by Decision I):** `tx_pcs_d` and the `pma_pcs_rxdata === pcs_pma_txdata` compare are **absent**. `tx_pcs_d` does not exist as a net (comment only, `vibe_pma_bnd.sv:76`). Idle-mark + Decision I headers did **not** restore a `txclk`→`rxclk` sample. Confirmed on tonight's tip (`f001833b`).
- **Idle-mark (local to `rxclk`):** `idle_prbs` is a combo check of `pma_pcs_rxdata ^ PMA_IDLE_MARK` against the PRBS23 recurrence `w[i] == w[i-23] ^ w[i-18]`. Fan-in is the RX pin + dest-domain reset only. **No `txclk` sequential is sampled.** Same-clock loopback intent is still not implemented as a crossing.
- **Not NEW:** a comment banner on an already-local idle-PRBS / AFIFO cell does not add a domain crossing.

## Findings

### CDC-WARN (F1 frozen — no ECO, no issue)

- `rtl/port/vibe_port.sv:244` — same always as 2026-09-22 / historical pin `302ac943`: 2-FF (`ovf_s1`/`ovf_s2`) into `clk_fab` as `afifo_ovf`; `|ovf_l` sample still at **:249**. `|ovf_l` is a 1-cycle `rxclk` sticky/pulse OR. No pulse stretcher / req-ack. **F1** permanent frozen WARN. **Unchanged** vs 2026-09-22. `rtl/port` diff vs `302ac943` and vs `d88442d2` is **empty**. **Not ECO'd.** **Not filed.**

### CDC-WARN (loopback `txclk`→`rxclk`) — **still GONE**

- Pre-ECO `pma_pcs_rxdata === pcs_pma_txdata` then `pma_afifo_lane_vld <= tx_pcs_d` is not in this tree. Closed by PR109. PR116 idle-mark did not restore it. Decision I stage-1/2 headers did not restore it. Tonight's re-audit on tip `f001833b` did not reopen it. **Not NEW.** **Not remaining.**

**CDC-WARN count:** 1 (`ovf_l` F1 only)  
**NEW CDC findings:** 0  
**GONE vs 2026-09-22:** none (loopback already GONE)  
No CDC-ERROR. No new unsynchronized multi-bit bus from `clk_fab` into PMA clocks. No new pulse/2FF besides F1. No new `txclk`→`rxclk` / unsanctioned PMA loopback CDC from Decision I leaves.

No RTL/SPEC/TB change in this PR. No issue opened.
No signoff claim. Not 4/3. Not new 1/3.
