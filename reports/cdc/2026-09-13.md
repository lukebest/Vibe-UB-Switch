# Nightly CDC 2026-09-13 (SGT)

- **When:** 2026-09-13 03:07 SGT
- **DUT SHA:** `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` (Lint ECO freeze; same bits as 2026-09-12)
- **main HEAD checked:** `7da480b4f4943547e597e9d72409c264d024ca24` — `git diff 982ddd0a...origin/main -- rtl/` empty; `-- include/` empty. `tb/` has PR73 waves already on main (not touched).
- **Tool:** structural RTL audit (no SpyGlass / Ascent / RDC on this box)
- **vs 2026-09-12 CDC:** same domain / AFIFO / `ovf_l` WARN structure. Prior nightlies on DUT `32a7f5e0` are **VOID** for class mixing. Lint ECO (`vibe_xbar` / `vibe_fabric` / `vibe_saf_ing`) is `clk_fab`-only — no new crossing.

## Clock domains

| Domain | Source | Who |
|--------|--------|-----|
| `clk_fab` | top | fabric, mgmt, LMSM/DLL/NW/PCS, AFIFO write-TX / read-RX |
| `txclk_0..3` | per-port PMA TX | AFIFO read-TX, 160→128 gear, PMA TX slice |
| `rxclk_0..3` | per-port PMA RX | PMA RX slice, change-detect, AFIFO write-RX |

## Sanctioned CDC cells (`rtl/cdc/`)

| Cell | Role | Recognized |
|------|------|------------|
| `vibe_sync2` | 2-FF gray pointer | yes (`vibe_afifo` r2w + w2r) |
| `vibe_afifo` | gray-pointer AFIFO depth 16, AFULL occ>=10 | yes (4 TX + 4 RX per `vibe_port`) |
| `vibe_rst_sync` | async assert, sync deassert | yes (`vibe_port` `u_txrst`/`u_rxrst`) |
| `vibe_gear_160_128` / `vibe_gear_128_160` | single-clock ratio, not a CDC cell | used only after/before AFIFO in the dest domain |

## Data-path crossings (all through AFIFO)

- TX: PCS 160b @`clk_fab` → `vibe_afifo` (wclk=`clk_fab`, rclk=`txclk`) → gear @`txclk` → `vibe_pma_bnd` TX
- RX: `vibe_pma_bnd` RX @`rxclk` → change-detect @`rxclk` → `vibe_afifo` (wclk=`rxclk`, rclk=`clk_fab`) → gear @`clk_fab` → PCS

Reset into PMA clocks: `rst_n && !port_rst` → `vibe_rst_sync` (`u_txrst` / `u_rxrst`). No other `txclk`/`rxclk` sequential fan-in from `clk_fab` except AFIFO gray pointers.

## Findings

### CDC-WARN (F1 frozen — no ECO, no issue)

- `rtl/port/vibe_port.sv:244` — `|ovf_l` is a 1-cycle `rxclk` sticky/pulse OR, then 2-FF (`ovf_s1`/`ovf_s2`) into `clk_fab` as `afifo_ovf`. No pulse stretcher / req-ack. **F1** permanent frozen WARN.

**CDC-WARN count:** 1 (`ovf_l` F1)  
**NEW CDC findings:** 0  
No unsynchronized multi-bit data crossing found. No CDC-ERROR.

No RTL/SPEC/TB change. No issue opened.
