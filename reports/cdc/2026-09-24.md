# Nightly CDC 2026-09-24 (SGT)

- **When:** 2026-09-24 03:17 SGT (**official nightly after Decision I leaves through stage-35 + PR201/PR203 TB-only tip**)
- **DUT / tip SHA:** `62cd3c88b04ffc90429a38a9cd83bc89642ac1f2` (origin/main HEAD = PR203 TB-only `vibe_saf_ing` merge). Expected pin at task start was `9a797225319aac1a982a18cc191713de8a5eab2a` (PR201); **tip moved** by PR203 (TB-only). Audit used **tip tree as-is** (no freeze overlay). RTL bits vs `9a79722` are **empty**. Tip **includes** Decision I leaves through stage-35 — expected. Functional PMA / port / AFIFO crossings unchanged vs last historical pin (headers on CDC cells; `vibe_port` empty).
- **Written freeze:** **UNFROZEN** (Decision I). Last historical pin `302ac943c3737c288f3af6c4857bb3a9b1683a26` is **VOID as current**. **Not re-pinned tonight.** Do not edit SPEC/CHANGELOG/STATUS freeze text here. Path B **hold**.
- **`git diff 302ac943...HEAD -- rtl/ include/`:** **NON-EMPTY** — 35 files, +249/−10 (Decision I headers; `vibe_rs128_120_dec` NBA reset unroll already on main). `-- include/` **empty**. `-- rtl/port` **empty** (F1 `ovf_l` untouched). `-- rtl/cdc` **NON-EMPTY** (headers only on all five cells).
- **`git diff f001833b...HEAD -- rtl/`:** **NON-EMPTY** — 33 files (09-23 DUT already had `vibe_afifo` + `vibe_pma_bnd` headers). `-- include/` **empty**. `-- rtl/port` **empty**.
- **main HEAD checked:** `62cd3c88b04ffc90429a38a9cd83bc89642ac1f2`
- **Tool:** structural RTL audit (no SpyGlass / Ascent / RDC on this box) — same method as [`2026-09-23.md`](2026-09-23.md) / nightly (no project CDC runner; inspect `rtl/pma/vibe_pma_bnd.sv` + `rtl/port/vibe_port.sv` + `rtl/cdc/`)
- **vs 2026-09-23 CDC:** same domain / AFIFO / F1 `ovf_l` WARN. Loopback `txclk`→`rxclk` stays **GONE**. Idle-mark is still localparam + same-domain combo XOR. Decision I headers on later CDC/DLL/fabric/PCS leaves did **not** reopen `txclk`→`rxclk` and did **not** change gray-pointer / dest-domain reset. `posedge txclk` / `posedge rxclk` sequentials remain only in `vibe_pma_bnd` + `vibe_port`. **CDC NEW = 0**. Prior nightlies on DUT `32a7f5e0` are **VOID**.

## Clock domains

| Domain | Source | Who |
|--------|--------|-----|
| `clk_fab` | top | fabric, mgmt, LMSM/DLL/NW/PCS, AFIFO write-TX / read-RX |
| `txclk_0..3` | per-port PMA TX | AFIFO read-TX, 160→128 gear, PMA TX slice **+ PRBS23 LFSR / `pcs_pma_txdata` / `PMA_IDLE_MARK` XOR idle fill** |
| `rxclk_0..3` | per-port PMA RX | PMA RX slice **+ `rxrst_n` async reset / local idle-PRBS `vld` drop (un-XOR mark then recurrence)**, change-detect, AFIFO write-RX |

## Sanctioned CDC cells (`rtl/cdc/`)

| Cell | Role | Recognized |
|------|------|------------|
| `vibe_sync2` | 2-FF gray pointer | yes (`vibe_afifo` r2w + w2r; Decision I header vs `302ac943`) |
| `vibe_afifo` | gray-pointer AFIFO depth 16, AFULL occ>=10 | yes (4 TX + 4 RX per `vibe_port`; Decision I header only vs `302ac943`) |
| `vibe_rst_sync` | async assert, sync deassert | yes (`vibe_port` `u_txrst`/`u_rxrst`; Decision I header vs `302ac943`) |
| `vibe_gear_160_128` / `vibe_gear_128_160` | single-clock ratio, not a CDC cell | used only after/before AFIFO in the dest domain; Decision I headers only |

## Data-path crossings (all through AFIFO)

- TX: PCS 160b @`clk_fab` → `vibe_afifo` (wclk=`clk_fab`, rclk=`txclk`) → gear @`txclk` → `vibe_pma_bnd` TX (`afifo_pma_lane*` / `afifo_pma_lane_vld` already `txclk`)
- RX: `vibe_pma_bnd` RX @`rxclk` → change-detect @`rxclk` → `vibe_afifo` (wclk=`rxclk`, rclk=`clk_fab`) → gear @`clk_fab` → PCS

Reset into PMA clocks: `rst_n && !port_rst` → `vibe_rst_sync` (`u_txrst` / `u_rxrst`). No other `txclk`/`rxclk` sequential fan-in from `clk_fab` except AFIFO gray pointers.

## PRBS23 / `txrst_n` (unchanged vs 2026-09-23)

- `vibe_port.sv:165` still ties PMA `.txrst_n(txrst_n)`. `txrst_n` is the `u_txrst` output (already dest-domain `txclk`). Same net already resets TX AFIFO `rrst_n` and 160→128 gears. **Sanctioned reset, not a new crossing.**
- `vibe_pma_bnd.sv` TX process remains `always @(posedge txclk or negedge txrst_n)` with PRBS23 LFSR **local to `txclk`**. Idle fill is `prbs ^ PMA_IDLE_MARK` (localparam constant). `afifo_pma_lane*` remain `txclk` data. **Not a crossing.** Decision I header comments do not change fan-in.
- Official lint `%Error-*` stays **0**. No new lint key on the mark or the Decision I headers (PMA / AFIFO still have zero primary keys). Header line-number churn on other leaves is lint-only; see [`../lint/2026-09-24.md`](../lint/2026-09-24.md).

## `rxrst_n` / idle-PRBS drop (still no `txclk` sample)

- `vibe_port.sv:165` still ties PMA `.rxrst_n(rxrst_n)`. `rxrst_n` is the `u_rxrst` output (already dest-domain `rxclk`). Same net already resets RX AFIFO `wrst_n` and the `rxclk` change-detect / `ovf_l` processes. **Sanctioned reset, not a new crossing.**
- `vibe_pma_bnd.sv` RX process remains `always @(posedge rxclk or negedge rxrst_n)` with dest-domain reset of the RX slice / `pma_afifo_lane_vld`. Reset itself is **not** a new crossing.
- **Still GONE (not reopened):** `tx_pcs_d` and the `pma_pcs_rxdata === pcs_pma_txdata` compare are **absent**. `tx_pcs_d` does not exist as a net (comment only). Idle-mark / Decision I headers did **not** restore a `txclk`→`rxclk` sample. Confirmed on tonight's tip (`62cd3c8`).
- **Idle-mark (local to `rxclk`):** `idle_prbs` is a combo check of `pma_pcs_rxdata ^ PMA_IDLE_MARK` against the PRBS23 recurrence `w[i] == w[i-23] ^ w[i-18]`. Fan-in is the RX pin + dest-domain reset only. **No `txclk` sequential is sampled.**
- **Not NEW:** pyCircuit GENERATED/HAND-FINISHED headers on CDC cells and later Decision I leaves do not add a domain crossing.

## Findings

### CDC-WARN (F1 frozen — no ECO, no issue)

- `rtl/port/vibe_port.sv:244` — same always as 2026-09-23 / last historical pin: 2-FF (`ovf_s1`/`ovf_s2`) into `clk_fab` as `afifo_ovf`; `|ovf_l` sample still at **:249**. `|ovf_l` is a 1-cycle `rxclk` sticky/pulse OR. No pulse stretcher / req-ack. **F1** permanent frozen WARN. **Unchanged**. **Not ECO'd.**

### CDC-WARN (loopback `txclk`→`rxclk`) — **still GONE**

- Pre-ECO `pma_pcs_rxdata === pcs_pma_txdata` then `pma_afifo_lane_vld <= tx_pcs_d` is not in this tree. Closed by PR109. PR116 idle-mark / Decision I leaves did not restore it. Tonight's re-audit on tip `62cd3c8` did not reopen it. **Not NEW.** **Not remaining.**

**CDC-WARN count:** 1 (`ovf_l` F1 only)  
**NEW CDC findings:** 0  
**GONE vs 2026-09-23:** none (loopback already GONE)  
No CDC-ERROR. No new unsynchronized multi-bit bus from `clk_fab` into PMA clocks. No new pulse/2FF besides F1. No new `txclk`→`rxclk` / unsanctioned PMA loopback CDC from Decision I headers.

No RTL/SPEC/TB change in this reports PR. No issue opened.
F1 `ovf_l` **untouched** / **not ECO'd**.
Decision I remains **UNFROZEN**. Path B **hold**.
No signoff claim. Not 4/3. Not new 1/3.
