# Nightly CDC 2026-09-18 (SGT)

- **When:** 2026-09-18 03:25 SGT (second post-PRBS23 lint/CDC; date label Asia/Shanghai)
- **DUT / tip SHA:** `4e575b6aed50d6d4182e0fac5ceaa167d064d9e8` (origin/main HEAD = PR101; re-fetched, tip **did not move**). RTL = Luke PRBS23 `268d038f` plus later reports/docs/TB. Audit used **tip tree as-is** (no freeze overlay).
- **Written freeze:** `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**. Decision **H not chosen**. **Not re-pinned to tip.**
- **`git diff 982ddd0a...HEAD -- rtl/`:** **NON-EMPTY** — `vibe_pma_bnd.sv` (PRBS23 + `txrst_n` input), `vibe_port.sv` (`.txrst_n(txrst_n)`). `-- include/` **empty**.
- **`git diff 859450a4...HEAD -- rtl/` / `-- include/`:** **empty**
- **main HEAD checked:** `4e575b6aed50d6d4182e0fac5ceaa167d064d9e8`
- **Tool:** structural RTL audit (no SpyGlass / Ascent / RDC on this box)
- **vs 2026-09-17 CDC:** same domain / AFIFO / F1 `ovf_l` WARN structure. RTL bits vs [`2026-09-17.md`](2026-09-17.md) are **unchanged** (PR98–PR101 reports / docs / TB only). **CDC NEW = 0**. Prior nightlies on DUT `32a7f5e0` are **VOID**.

## Clock domains

| Domain | Source | Who |
|--------|--------|-----|
| `clk_fab` | top | fabric, mgmt, LMSM/DLL/NW/PCS, AFIFO write-TX / read-RX |
| `txclk_0..3` | per-port PMA TX | AFIFO read-TX, 160→128 gear, PMA TX slice **+ PRBS23 LFSR / `pcs_pma_txdata`** |
| `rxclk_0..3` | per-port PMA RX | PMA RX slice, change-detect, AFIFO write-RX |

## Sanctioned CDC cells (`rtl/cdc/`)

| Cell | Role | Recognized |
|------|------|------------|
| `vibe_sync2` | 2-FF gray pointer | yes (`vibe_afifo` r2w + w2r) |
| `vibe_afifo` | gray-pointer AFIFO depth 16, AFULL occ>=10 | yes (4 TX + 4 RX per `vibe_port`) |
| `vibe_rst_sync` | async assert, sync deassert | yes (`vibe_port` `u_txrst`/`u_rxrst`) |
| `vibe_gear_160_128` / `vibe_gear_128_160` | single-clock ratio, not a CDC cell | used only after/before AFIFO in the dest domain |

## Data-path crossings (all through AFIFO)

- TX: PCS 160b @`clk_fab` → `vibe_afifo` (wclk=`clk_fab`, rclk=`txclk`) → gear @`txclk` → `vibe_pma_bnd` TX (`afifo_pma_lane*` / `afifo_pma_lane_vld` already `txclk`)
- RX: `vibe_pma_bnd` RX @`rxclk` → change-detect @`rxclk` → `vibe_afifo` (wclk=`rxclk`, rclk=`clk_fab`) → gear @`clk_fab` → PCS (unchanged vs freeze)

Reset into PMA clocks: `rst_n && !port_rst` → `vibe_rst_sync` (`u_txrst` / `u_rxrst`). No other `txclk`/`rxclk` sequential fan-in from `clk_fab` except AFIFO gray pointers.

## PRBS23 / `txrst_n` (post-freeze delta — re-reviewed, unchanged vs 2026-09-17)

- `vibe_port.sv:165` ties PMA `.txrst_n(txrst_n)`. `txrst_n` is still the `u_txrst` output (already dest-domain `txclk`). Same net already resets TX AFIFO `rrst_n` and 160→128 gears. **Sanctioned reset, not a new crossing.**
- `vibe_pma_bnd.sv` TX process is `always @(posedge txclk or negedge txrst_n)` with PRBS23 LFSR / idle fill **local to `txclk`**. `afifo_pma_lane*` remain `txclk` data. RX process still `always @(posedge rxclk)` only — no new `clk_fab`/`txclk`/`rxclk` mix.
- No new unsynchronized multi-bit bus from `clk_fab` into PMA clocks. No new pulse/2FF besides F1. Verilator `%Error-UNSUPPORTED` on `txrst_n = 1'b1` is a **lint** default-value issue, not a CDC-WARN.

## Findings

### CDC-WARN (F1 frozen — no ECO, no issue)

- `rtl/port/vibe_port.sv:244` — same always as freeze: 2-FF (`ovf_s1`/`ovf_s2`) into `clk_fab` as `afifo_ovf`; `|ovf_l` sample still at **:249**. `|ovf_l` is a 1-cycle `rxclk` sticky/pulse OR. No pulse stretcher / req-ack. **F1** permanent frozen WARN. Unchanged vs 2026-09-17 / freeze `982ddd0a`.

**CDC-WARN count:** 1 (`ovf_l` F1)  
**NEW CDC findings:** 0  
No unsynchronized multi-bit data crossing found. No CDC-ERROR.

No RTL/SPEC/TB change. No issue opened.
No signoff claim. Not 4/3.
