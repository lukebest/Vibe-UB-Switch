# Nightly CDC 2026-09-19 (SGT)

- **When:** 2026-09-19 03:14 SGT (post-PR105 health; **first official post-`rxrst_n` tip CDC**)
- **DUT / tip SHA:** `83c0a1a05708ddc8e553ac0d5bb31efc3ec4af0f` (origin/main HEAD = PR105). RTL = Luke PRBS23/`txrst_n` + Luke `rxrst_n` `0f783df605ff98d9051d9f3b7eb95f7fa64e8a55` plus later reports/docs/TB. Audit used **tip tree as-is** (no freeze overlay).
- **Written freeze:** `982ddd0a54cf19dbeb39cf8f8f523c4e26e593f6` — **BROKEN / H hold-pin**. Decision **H not chosen**. **Not re-pinned to tip.**
- **`git diff 982ddd0a...HEAD -- rtl/`:** **NON-EMPTY** — `vibe_pma_bnd.sv` (PRBS23 + `txrst_n` + `rxrst_n`, +99/−11), `vibe_port.sv` (`.txrst_n(txrst_n), .rxrst_n(rxrst_n)`, +3/−3). Combined **+102/−14**. `-- include/` **empty**.
- **main HEAD checked:** `83c0a1a05708ddc8e553ac0d5bb31efc3ec4af0f`
- **Tool:** structural RTL audit (no SpyGlass / Ascent / RDC on this box)
- **vs 2026-09-18 CDC:** same domain / AFIFO / F1 `ovf_l` WARN structure. RTL bits vs [`2026-09-18.md`](2026-09-18.md) **changed** by Luke `0f783df6` (`rxrst_n` + RX async reset + near-end loopback `vld` mux). **CDC NEW = 1** (loopback `txclk`→`rxclk` compare / `tx_pcs_d`). Prior nightlies on DUT `32a7f5e0` are **VOID**.

## Clock domains

| Domain | Source | Who |
|--------|--------|-----|
| `clk_fab` | top | fabric, mgmt, LMSM/DLL/NW/PCS, AFIFO write-TX / read-RX |
| `txclk_0..3` | per-port PMA TX | AFIFO read-TX, 160→128 gear, PMA TX slice **+ PRBS23 LFSR / `pcs_pma_txdata` / `tx_pcs_d`** |
| `rxclk_0..3` | per-port PMA RX | PMA RX slice **+ `rxrst_n` async reset / loopback `vld` mux**, change-detect, AFIFO write-RX |

## Sanctioned CDC cells (`rtl/cdc/`)

| Cell | Role | Recognized |
|------|------|------------|
| `vibe_sync2` | 2-FF gray pointer | yes (`vibe_afifo` r2w + w2r) |
| `vibe_afifo` | gray-pointer AFIFO depth 16, AFULL occ>=10 | yes (4 TX + 4 RX per `vibe_port`) |
| `vibe_rst_sync` | async assert, sync deassert | yes (`vibe_port` `u_txrst`/`u_rxrst`) |
| `vibe_gear_160_128` / `vibe_gear_128_160` | single-clock ratio, not a CDC cell | used only after/before AFIFO in the dest domain |

## Data-path crossings (all through AFIFO)

- TX: PCS 160b @`clk_fab` → `vibe_afifo` (wclk=`clk_fab`, rclk=`txclk`) → gear @`txclk` → `vibe_pma_bnd` TX (`afifo_pma_lane*` / `afifo_pma_lane_vld` already `txclk`)
- RX: `vibe_pma_bnd` RX @`rxclk` → change-detect @`rxclk` → `vibe_afifo` (wclk=`rxclk`, rclk=`clk_fab`) → gear @`clk_fab` → PCS

Reset into PMA clocks: `rst_n && !port_rst` → `vibe_rst_sync` (`u_txrst` / `u_rxrst`). No other `txclk`/`rxclk` sequential fan-in from `clk_fab` except AFIFO gray pointers.

## PRBS23 / `txrst_n` (unchanged vs 2026-09-18)

- `vibe_port.sv:165` still ties PMA `.txrst_n(txrst_n)`. `txrst_n` is the `u_txrst` output (already dest-domain `txclk`). Same net already resets TX AFIFO `rrst_n` and 160→128 gears. **Sanctioned reset, not a new crossing.**
- `vibe_pma_bnd.sv` TX process remains `always @(posedge txclk or negedge txrst_n)` with PRBS23 LFSR / idle fill **local to `txclk`**. `afifo_pma_lane*` remain `txclk` data.
- Verilator `%Error-UNSUPPORTED` on `txrst_n = 1'b1` is a **lint** default-value issue, not a CDC-WARN.

## `rxrst_n` / loopback (post-`0f783df6` — first review)

- `vibe_port.sv:165` now also ties PMA `.rxrst_n(rxrst_n)`. `rxrst_n` is the `u_rxrst` output (already dest-domain `rxclk`). Same net already resets RX AFIFO `wrst_n` and the `rxclk` change-detect / `ovf_l` processes. **Sanctioned reset, not a new crossing.** Same class as last night's `txrst_n` review.
- `vibe_pma_bnd.sv` RX process is now `always @(posedge rxclk or negedge rxrst_n)` with dest-domain reset of the RX slice / `pma_afifo_lane_vld`. Reset itself is **not** a new crossing.
- Verilator `%Error-UNSUPPORTED` on `rxrst_n = 1'b1` is a **lint** default-value issue (official lint **NEW**), not a CDC-WARN.
- **NEW structure:** `tx_pcs_d` is a `txclk` FF (delayed `afifo_pma_lane_vld`). The RX process compares `pma_pcs_rxdata === pcs_pma_txdata` (`pcs_pma_txdata` is a `txclk` 512b FF) and on match loads `pma_afifo_lane_vld <= tx_pcs_d`. RTL comment documents **near-end loopback, same `txclk`/`rxclk`**. Lane data still come from `pma_pcs_rxdata` (RX pin). This is **not** a sanctioned CDC cell and **not** a `clk_fab` crossing. On independent product clocks the 512b `===` plus `tx_pcs_d` sample are unsynchronized. Counted as **CDC NEW** below. **No ECO** (H hold-pin). **No issue.**

## Findings

### CDC-WARN (F1 frozen — no ECO, no issue)

- `rtl/port/vibe_port.sv:244` — same always as freeze: 2-FF (`ovf_s1`/`ovf_s2`) into `clk_fab` as `afifo_ovf`; `|ovf_l` sample still at **:249**. `|ovf_l` is a 1-cycle `rxclk` sticky/pulse OR. No pulse stretcher / req-ack. **F1** permanent frozen WARN. **Unchanged** vs 2026-09-18 / freeze `982ddd0a`.

### CDC-WARN (NEW — post-`rxrst_n` loopback; no ECO, no issue)

- `rtl/pma/vibe_pma_bnd.sv:116` — `pma_pcs_rxdata === pcs_pma_txdata` then `pma_afifo_lane_vld <= tx_pcs_d` inside the `rxclk` process (`:104`). `pcs_pma_txdata` / `tx_pcs_d` are `txclk` sequentials. Same-clock loopback intent is in the comment; no 2-FF / AFIFO / `vibe_sync2` on that control path. Not F1.

**CDC-WARN count:** 2 (`ovf_l` F1 + loopback `txclk`→`rxclk`)  
**NEW CDC findings:** 1  
No CDC-ERROR. No new unsynchronized multi-bit bus from `clk_fab` into PMA clocks. No new pulse/2FF besides F1.

No RTL/SPEC/TB change. No issue opened.
No signoff claim. Not 4/3.
